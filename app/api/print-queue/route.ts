import { NextResponse } from "next/server"
import { getSettings, getUnprintedOrders, markOrderPrinted } from "@/lib/db"
function authorized(request: Request) { const configured = process.env.PRINT_AGENT_TOKEN; if (!configured) return false; const token = request.headers.get("x-print-token") || new URL(request.url).searchParams.get("token"); return token === configured }
export async function GET(request: Request) { if (!authorized(request)) return NextResponse.json({ error: "Não autorizado." }, { status: 401 }); const [orders, settings] = await Promise.all([getUnprintedOrders(), getSettings()]); return NextResponse.json({ orders, settings: { storeName: settings.storeName, address: settings.address, printerName: settings.printerName, printCopies: settings.printCopies, autoPrintNewOrders: settings.autoPrintNewOrders, printKitchenTicket: settings.printKitchenTicket, printCustomerTicket: settings.printCustomerTicket } }) }
export async function POST(request: Request) { if (!authorized(request)) return NextResponse.json({ error: "Não autorizado." }, { status: 401 }); const body = await request.json().catch(() => null) as { orderId?: number } | null; if (!body?.orderId) return NextResponse.json({ error: "Pedido inválido." }, { status: 400 }); return NextResponse.json({ order: await markOrderPrinted(Number(body.orderId)) }) }
