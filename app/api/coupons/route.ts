import { NextResponse } from "next/server"
import { createCoupon, getCoupons, validateCoupon } from "@/lib/db"
import { isAdminAuthenticated } from "@/lib/auth"
export async function GET(request: Request) { const url = new URL(request.url); const code = url.searchParams.get("code"); const subtotal = Number(url.searchParams.get("subtotal") || 0); if (code) { try { const result = await validateCoupon(code, subtotal); return NextResponse.json({ valid: true, discount: result.discount, coupon: result.coupon }) } catch (error) { return NextResponse.json({ valid: false, error: error instanceof Error ? error.message : "Cupom inválido." }, { status: 400 }) } } if (!(await isAdminAuthenticated())) return NextResponse.json({ error: "Não autorizado." }, { status: 401 }); return NextResponse.json({ coupons: await getCoupons({ includeInactive: true }) }) }
export async function POST(request: Request) { if (!(await isAdminAuthenticated())) return NextResponse.json({ error: "Não autorizado." }, { status: 401 }); const body = await request.json().catch(() => null) as any; try { return NextResponse.json({ coupon: await createCoupon({ code: String(body?.code || ""), description: String(body?.description || ""), type: body?.type === "fixed" ? "fixed" : "percent", value: Number(body?.value || 0), minimumOrder: Number(body?.minimumOrder || 0), active: body?.active !== false, ...(body?.expiresAt ? { expiresAt: String(body.expiresAt) } : {}) }) }, { status: 201 }) } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Erro ao criar cupom." }, { status: 400 }) } }
