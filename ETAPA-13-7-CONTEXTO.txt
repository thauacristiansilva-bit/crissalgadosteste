SABORFLOW - ETAPA 13.7 - CONTEXTO RBAC
Gerado em: 2026-09-07 20:46:12
Projeto: C:\Users\thaua\Downloads\cris-salgados-agendamento-entrega-pronto

==========================================================================================
GIT
==========================================================================================
HEAD: 6a42eaede0268ae8d15e3afd7cf5a1abfca5da14
?? APLICAR-ETAPA-13-2-2FA.ps1
?? APLICAR-ETAPA-13-3-RECUPERACAO-2FA.ps1
?? APLICAR-ETAPA-13-4-PASSKEYS-CORRIGIDO.ps1
?? APLICAR-ETAPA-13-4-PASSKEYS.ps1
?? APLICAR-ETAPA-13-5-SENHAS-BLOQUEIO.ps1
?? APLICAR-ETAPA-13-6-SESSOES-DISPOSITIVOS-CORRIGIDO.ps1
?? APLICAR-ETAPA-13-6-SESSOES-DISPOSITIVOS.ps1
?? APLICAR-ETAPA-2-NO-RAILWAY.md
?? APLICAR-ETAPA-5-NO-RAILWAY.md
?? APLICAR-ETAPA-6-1-NO-RAILWAY.md
?? APLICAR-ETAPA-6-NO-RAILWAY.md
?? APLICAR-SESSAO-10-DIAS.ps1
?? CORRIGIR-ETAPA-13-3-SECURITY-PANEL.ps1
?? CORRIGIR-ETAPA-13-6-CALLS-SESSAO.ps1
?? CORRIGIR-ETAPA-13-6-LISTA-SESSOES.ps1
?? ETAPA-13-2-2FA-AUDITORIA.txt
?? ETAPA-13-2-2FA-COMPLEMENTO.txt
?? ETAPA-13-3-CONTEXTO.txt
?? ETAPA-13-4-PASSKEY-CONTEXTO.txt
?? ETAPA-13-5-CONTEXTO.txt
?? ETAPA-13-6-CONTEXTO.txt
?? GERAR-ETAPA-13-7-CONTEXTO-CORRIGIDO.ps1
?? GERAR-ETAPA-13-7-CONTEXTO.ps1
?? HOTFIX-13-6-CONTEXTO.txt
?? LEIA-ME-HOTFIX-ETAPA-10-RLS-DOMINIOS.md
?? LEIA-ME-HOTFIX-ETAPA-8.md
?? LEIA-ME-HOTFIX.md
?? LEIA-ME-HOTFIX.txt
?? LEIA-ME.md
?? SaborFlow-HOTFIX-ETAPA-8-build/
?? docs/hotfixes/
?? {

==========================================================================================
ARQUIVOS RELACIONADOS A AUTORIZACAO / EQUIPE / TENANT
==========================================================================================
app\admin\crm\page.tsx:2: import { CrmDashboard } from "@/components/admin/crm-dashboard"
app\admin\crm\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\admin\crm\page.tsx:9: const session = await getVerifiedTenantSession()
app\admin\crm\page.tsx:11: if (!canAccessCrm(session)) redirect("/admin")
app\admin\grupo\page.tsx:2: import { CorporateGroupDashboard } from "@/components/admin/corporate-group-dashboard"
app\admin\grupo\page.tsx:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\admin\grupo\page.tsx:8: const session = await getVerifiedTenantSession()
app\admin\integracoes\page.tsx:2: import { IntegrationsDashboard } from "@/components/admin/integrations-dashboard"
app\admin\integracoes\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\admin\integracoes\page.tsx:9: const session = await getVerifiedTenantSession()
app\admin\integracoes\page.tsx:11: if (!canAccessIntegrations(session)) redirect("/admin")
app\admin\nova-empresa\page.tsx:2: import { getAdminSession } from "@/lib/auth"
app\admin\nova-empresa\page.tsx:5: import { OrganizationOnboardingForm } from "@/components/admin/organization-onboarding-form"
app\admin\nova-empresa\page.tsx:11: const session = await getAdminSession()
app\admin\nova-empresa\page.tsx:14: if (session.role !== "owner") redirect("/admin")
app\admin\operacao-alimentar\page.tsx:2: import { FoodOperationsDashboard } from "@/components/admin/food-operations-dashboard"
app\admin\operacao-alimentar\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\admin\operacao-alimentar\page.tsx:9: const session = await getVerifiedTenantSession()
app\admin\operacao-alimentar\page.tsx:11: if (!canAccessFoodOperations(session)) redirect("/admin")
app\admin\relatorios\page.tsx:2: import { ReportsDashboard } from "@/components/admin/reports-dashboard"
app\admin\relatorios\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\admin\relatorios\page.tsx:9: const session = await getVerifiedTenantSession()
app\admin\relatorios\page.tsx:11: if (!canAccessManagementReports(session)) redirect("/admin")
app\admin\page.tsx:3: import { AdminDashboard } from "@/components/admin/admin-dashboard"
app\admin\page.tsx:4: import { getAdminEmail, getAdminSession } from "@/lib/auth"
app\admin\page.tsx:5: import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
app\admin\page.tsx:14: const session = await getAdminSession()
app\admin\page.tsx:29: if (session.mode === "tenant" && session.role === "owner") {
app\admin\page.tsx:40: session.role,
app\admin\page.tsx:41: access.permissions,
app\admin\page.tsx:43: if (operationalHome !== "/admin") redirect(operationalHome)
app\admin\page.tsx:49: adminRole={session.mode === "tenant" ? session.role : "owner"}
app\admin\page.tsx:50: operationalPermissions={access?.permissions || []}
app\admin\page.tsx:56: initialData={await getTenantAwareAdminData(session, access?.permissions)}
app\api\admin\2fa\reconfigure\qr\route.ts:3: getVerifiedTenantSession,
app\api\admin\2fa\reconfigure\qr\route.ts:14: await getVerifiedTenantSession()
app\api\admin\2fa\reconfigure\route.ts:3: getVerifiedTenantSession,
app\api\admin\2fa\reconfigure\route.ts:152: 403,
app\api\admin\2fa\reconfigure\route.ts:157: await getVerifiedTenantSession()
app\api\admin\2fa\reconfigure\route.ts:246: `/api/admin/2fa/reconfigure/qr?v=${Date.now()}`,
app\api\admin\2fa\reconfigure\route.ts:280: 403,
app\api\admin\2fa\reconfigure\route.ts:285: await getVerifiedTenantSession()
app\api\admin\2fa\reconfigure\route.ts:403: 403,
app\api\admin\2fa\reconfigure\route.ts:408: await getVerifiedTenantSession()
app\api\admin\2fa\recovery-codes\route.ts:3: getVerifiedTenantSession,
app\api\admin\2fa\recovery-codes\route.ts:65: 403,
app\api\admin\2fa\recovery-codes\route.ts:70: await getVerifiedTenantSession()
app\api\admin\2fa\security\route.ts:3: getVerifiedTenantSession,
app\api\admin\2fa\security\route.ts:14: await getVerifiedTenantSession()
app\api\admin\after-hours-scheduling-health\route.ts:5: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\after-hours-scheduling-health\route.ts:10: const session = await getVerifiedTenantSession()
app\api\admin\billing\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\billing\route.ts:8: const session = await getVerifiedTenantSession()
app\api\admin\billing\route.ts:10: if (session.role !== "owner") {
app\api\admin\billing\route.ts:11: return NextResponse.json({ error: "Somente o proprietário pode consultar a assinatura comercial." }, { status: 403 })
app\api\admin\billing-health\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\billing-health\route.ts:21: const session = await getVerifiedTenantSession()
app\api\admin\billing-health\route.ts:23: if (session.role !== "owner" && session.role !== "admin") {
app\api\admin\billing-health\route.ts:24: return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\admin\catalog-health\route.ts:10: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\catalog-health\route.ts:16: const session = await getVerifiedTenantSession()
app\api\admin\catalog-health\route.ts:52: role: session.role,
app\api\admin\commercial-onboarding\route.ts:9: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\commercial-onboarding\route.ts:15: const session = await getVerifiedTenantSession()
app\api\admin\commercial-onboarding\route.ts:17: if (session.role !== "owner") {
app\api\admin\commercial-onboarding\route.ts:20: status: 403 as const,
app\api\admin\corporate-group\route.ts:17: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\corporate-group\route.ts:23: const session = await getVerifiedTenantSession()
app\api\admin\corporate-group\route.ts:61: role?: CorporateGroupRole
app\api\admin\corporate-group\route.ts:71: const session = await getVerifiedTenantSession()
app\api\admin\corporate-group\route.ts:77: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\admin\corporate-group\route.ts:120: if (!body.groupId || !body.email || !body.role) throw new Error("Membro inválido.")
app\api\admin\corporate-group\route.ts:121: await addCorporateMember(session, body.groupId, { email: body.email, role: body.role }, ip)
app\api\admin\corporate-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\corporate-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\corporate-health\route.ts:23: operationalAccessRequiresTenantMembership: true,
app\api\admin\crm\actions\route.ts:14: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\crm\actions\route.ts:44: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\admin\crm\actions\route.ts:47: const session = await getVerifiedTenantSession()
app\api\admin\crm\actions\route.ts:50: return NextResponse.json({ error: "Seu perfil não possui acesso ao CRM e fidelidade." }, { status: 403 })
app\api\admin\crm\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\crm\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\crm\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso ao CRM e fidelidade." }, { status: 403 })
app\api\admin\crm-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\crm-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\crm-health\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso ao CRM e fidelidade." }, { status: 403 })
app\api\admin\customers\route.ts:19: getVerifiedTenantSession,
app\api\admin\customers\route.ts:24: import { canManageCustomers } from "@/lib/tenant-permissions"
app\api\admin\customers\route.ts:64: const session = await getVerifiedTenantSession()
app\api\admin\customers\route.ts:74: !canManageCustomers(session.role)
app\api\admin\customers\route.ts:78: { status: 403 },
app\api\admin\customers-health\route.ts:10: getVerifiedTenantSession,
app\api\admin\customers-health\route.ts:20: const session = await getVerifiedTenantSession()
app\api\admin\customers-health\route.ts:70: role: session.role,
app\api\admin\database-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\database-health\route.ts:4: import { canManageSecurity } from "@/lib/admin-access"
app\api\admin\database-health\route.ts:10: const session = await getVerifiedTenantSession().catch(() => null)
app\api\admin\database-health\route.ts:18: if (!canManageSecurity(session.role, session.operationalPermissions)) {
app\api\admin\database-health\route.ts:21: { status: 403 },
app\api\admin\database-structure\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\database-structure\route.ts:4: import { canManageSecurity } from "@/lib/admin-access"
app\api\admin\database-structure\route.ts:12: "sf_memberships",
app\api\admin\database-structure\route.ts:19: const session = await getVerifiedTenantSession().catch(() => null)
app\api\admin\database-structure\route.ts:27: if (!canManageSecurity(session.role, session.operationalPermissions)) {
app\api\admin\database-structure\route.ts:30: { status: 403 },
app\api\admin\delivery-dispatch-health\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\delivery-dispatch-health\route.ts:4: import { permissionListHas } from "@/lib/operational-permissions"
app\api\admin\delivery-dispatch-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\delivery-dispatch-health\route.ts:19: session.role === "courier" ||
app\api\admin\delivery-dispatch-health\route.ts:20: permissionListHas(session.operationalPermissions, "delivery.manage")
app\api\admin\delivery-dispatch-health\route.ts:25: { status: 403 },
app\api\admin\delivery-dispatch-health\route.ts:33: role: session.role,
app\api\admin\delivery-dispatch-health\route.ts:38: phase: "25.7.2-courier-rls-session-scope",
app\api\admin\delivery-dispatch-health\route.ts:78: phase: "25.7.2-courier-rls-session-scope",
app\api\admin\delivery-tracking-health\route.ts:4: import { permissionListHas } from "@/lib/operational-permissions"
app\api\admin\delivery-tracking-health\route.ts:6: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\delivery-tracking-health\route.ts:11: const session = await getVerifiedTenantSession()
app\api\admin\delivery-tracking-health\route.ts:18: session.role === "courier" ||
app\api\admin\delivery-tracking-health\route.ts:19: permissionListHas(session.operationalPermissions, "delivery.manage")
app\api\admin\delivery-tracking-health\route.ts:24: { status: 403 },
app\api\admin\delivery-zones-health\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\delivery-zones-health\route.ts:11: const session = await getVerifiedTenantSession()
app\api\admin\demo-health\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\demo-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\domains\verify\route.ts:3: getVerifiedTenantSession,
app\api\admin\domains\verify\route.ts:7: } from "@/lib/admin-access"
app\api\admin\domains\verify\route.ts:17: await getVerifiedTenantSession()
app\api\admin\domains\verify\route.ts:21: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\domains\verify\route.ts:25: { status: session ? 403 : 401 },
app\api\admin\domains\route.ts:3: getVerifiedTenantSession,
app\api\admin\domains\route.ts:7: } from "@/lib/admin-access"
app\api\admin\domains\route.ts:20: await getVerifiedTenantSession()
app\api\admin\domains\route.ts:24: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\domains\route.ts:28: { status: session ? 403 : 401 },
app\api\admin\domains\route.ts:44: await getVerifiedTenantSession()
app\api\admin\domains\route.ts:48: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\domains\route.ts:52: { status: session ? 403 : 401 },
app\api\admin\domains\route.ts:94: await getVerifiedTenantSession()
app\api\admin\domains\route.ts:98: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\domains\route.ts:102: { status: session ? 403 : 401 },
app\api\admin\dre\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\dre\route.ts:5: import { canViewFinance } from "@/lib/tenant-permissions"
app\api\admin\dre\route.ts:28: const session = await getVerifiedTenantSession()
app\api\admin\dre\route.ts:36: if (!canViewFinance(session.role)) {
app\api\admin\dre\route.ts:39: { status: 403 },
app\api\admin\dre-health\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\dre-health\route.ts:5: import { canViewFinance } from "@/lib/tenant-permissions"
app\api\admin\dre-health\route.ts:12: const session = await getVerifiedTenantSession()
app\api\admin\dre-health\route.ts:20: if (!canViewFinance(session.role)) {
app\api\admin\dre-health\route.ts:23: { status: 403 },
app\api\admin\food-composition-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\food-composition-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\food-operations\actions\route.ts:11: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\food-operations\actions\route.ts:49: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\admin\food-operations\actions\route.ts:52: const session = await getVerifiedTenantSession()
app\api\admin\food-operations\actions\route.ts:55: return NextResponse.json({ error: "Seu perfil não possui acesso à operação alimentar avançada." }, { status: 403 })
app\api\admin\food-operations\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\food-operations\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\food-operations\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso à operação alimentar avançada." }, { status: 403 })
app\api\admin\food-operations-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\food-operations-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\food-operations-health\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso à operação alimentar avançada." }, { status: 403 })
app\api\admin\google-link\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\google-link\route.ts:10: const session = await getVerifiedTenantSession()
app\api\admin\google-link\route.ts:26: const session = await getVerifiedTenantSession()
app\api\admin\ingredients\[id]\movement\route.ts:7: import { canManageCatalog, getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\ingredients\[id]\movement\route.ts:27: const session = await getVerifiedTenantSession()
app\api\admin\ingredients\[id]\movement\route.ts:31: if (!canManageCatalog(session.role)) {
app\api\admin\ingredients\[id]\movement\route.ts:32: return NextResponse.json({ error: "Seu perfil não pode movimentar o estoque." }, { status: 403 })
app\api\admin\ingredients\[id]\route.ts:4: import { canManageCatalog, getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\ingredients\[id]\route.ts:18: const session = await getVerifiedTenantSession()
app\api\admin\ingredients\[id]\route.ts:22: if (!canManageCatalog(session.role)) {
app\api\admin\ingredients\[id]\route.ts:23: return NextResponse.json({ error: "Seu perfil não pode alterar o estoque." }, { status: 403 })
app\api\admin\ingredients\route.ts:8: import { canManageCatalog, getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\ingredients\route.ts:20: const session = await getVerifiedTenantSession()
app\api\admin\ingredients\route.ts:53: const session = await getVerifiedTenantSession()
app\api\admin\ingredients\route.ts:60: if (!canManageCatalog(session.role)) {
app\api\admin\ingredients\route.ts:63: { status: 403 },
app\api\admin\integrations\actions\route.ts:13: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\integrations\actions\route.ts:35: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\admin\integrations\actions\route.ts:37: const session = await getVerifiedTenantSession()
app\api\admin\integrations\actions\route.ts:40: return NextResponse.json({ error: "Seu perfil não possui acesso às integrações." }, { status: 403 })
app\api\admin\integrations\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\integrations\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\integrations\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso às integrações." }, { status: 403 })
app\api\admin\integrations-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\integrations-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\integrations-health\route.ts:12: return NextResponse.json({ error: "Seu perfil não possui acesso às integrações." }, { status: 403 })
app\api\admin\inventory-movements\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\inventory-movements\route.ts:5: import { canReadCatalog } from "@/lib/admin-access"
app\api\admin\inventory-movements\route.ts:11: const session = await getVerifiedTenantSession()
app\api\admin\inventory-movements\route.ts:15: if (!canReadCatalog(session.role, session.operationalPermissions)) {
app\api\admin\inventory-movements\route.ts:16: return NextResponse.json({ error: "Seu perfil não pode acessar o estoque." }, { status: 403 })
app\api\admin\legacy-health\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\legacy-health\route.ts:21: const session = await getVerifiedTenantSession()
app\api\admin\multiempresa-health\route.ts:7: } from "@/lib/admin-user-db"
app\api\admin\multiempresa-health\route.ts:12: listOrganizationMembershipsForUserId,
app\api\admin\multiempresa-health\route.ts:15: getVerifiedTenantSession,
app\api\admin\multiempresa-health\route.ts:23: await getVerifiedTenantSession()
app\api\admin\multiempresa-health\route.ts:45: listOrganizationMembershipsForUserId(
app\api\admin\multiempresa-health\route.ts:87: role: session.role,
app\api\admin\multiempresa-health\route.ts:101: role:
app\api\admin\multiempresa-health\route.ts:102: organization.role,
app\api\admin\onboarding-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\onboarding-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\operations-health\route.ts:14: getVerifiedTenantSession,
app\api\admin\operations-health\route.ts:41: const session = await getVerifiedTenantSession()
app\api\admin\operations-health\route.ts:128: role: session.role,
app\api\admin\order-flow-health\route.ts:2: import { canUpdateOrderStatus } from "@/lib/admin-access"
app\api\admin\order-flow-health\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\order-flow-health\route.ts:10: const session = await getVerifiedTenantSession()
app\api\admin\order-flow-health\route.ts:20: session.role,
app\api\admin\order-flow-health\route.ts:21: session.operationalPermissions,
app\api\admin\order-flow-health\route.ts:24: canChangeStatus && session.role !== "courier"
app\api\admin\order-flow-health\route.ts:27: session.role !== "courier" &&
app\api\admin\order-flow-health\route.ts:28: session.role !== "kitchen"
app\api\admin\order-flow-health\route.ts:40: role: session.role,
app\api\admin\order-flow-health\route.ts:42: session.role,
app\api\admin\order-flow-health\route.ts:43: session.operationalPermissions,
app\api\admin\orders-health\route.ts:10: getVerifiedTenantSession,
app\api\admin\orders-health\route.ts:28: const session = await getVerifiedTenantSession()
app\api\admin\orders-health\route.ts:77: role: session.role,
app\api\admin\organization-security\route.ts:3: getVerifiedTenantSession,
app\api\admin\organization-security\route.ts:7: } from "@/lib/admin-access"
app\api\admin\organization-security\route.ts:19: await getVerifiedTenantSession()
app\api\admin\organization-security\route.ts:38: session.role,
app\api\admin\organization-security\route.ts:39: session.operationalPermissions,
app\api\admin\organization-security\route.ts:51: role: session.role,
app\api\admin\organization-security\route.ts:80: role: session.role,
app\api\admin\organization-security\route.ts:92: await getVerifiedTenantSession()
app\api\admin\organization-security\route.ts:106: session.role,
app\api\admin\organization-security\route.ts:107: session.operationalPermissions,
app\api\admin\organization-security\route.ts:115: { status: 403 },
app\api\admin\organizations\current\ordering\route.ts:6: getVerifiedTenantSession,
app\api\admin\organizations\current\ordering\route.ts:8: import { canManageOrganizationSettings } from "@/lib/tenant-permissions"
app\api\admin\organizations\current\ordering\route.ts:14: await getVerifiedTenantSession()
app\api\admin\organizations\current\ordering\route.ts:26: if (!canManageOrganizationSettings(session.role)) {
app\api\admin\organizations\current\ordering\route.ts:32: { status: 403 },
app\api\admin\organizations\route.ts:17: import { listOrganizationMembershipsForUserId } from "@/lib/tenant-context"
app\api\admin\organizations\route.ts:18: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\organizations\route.ts:22: } from "@/lib/security/admin-sessions"
app\api\admin\organizations\route.ts:28: const session = await getVerifiedTenantSession()
app\api\admin\organizations\route.ts:33: const organizations = await listOrganizationMembershipsForUserId(session.userId)
app\api\admin\organizations\route.ts:41: const tenantSession = await getVerifiedTenantSession().catch(() => null)
app\api\admin\organizations\route.ts:49: if (tenantSession.role !== "owner") {
app\api\admin\organizations\route.ts:52: }, { status: 403 })
app\api\admin\organizations\route.ts:128: superadminAuthorized: false,
app\api\admin\organizations\route.ts:140: role: context.role,
app\api\admin\passkeys\register\options\route.ts:5: getVerifiedTenantSession,
app\api\admin\passkeys\register\options\route.ts:78: 403,
app\api\admin\passkeys\register\options\route.ts:83: await getVerifiedTenantSession()
app\api\admin\passkeys\register\verify\route.ts:8: getVerifiedTenantSession,
app\api\admin\passkeys\register\verify\route.ts:54: 403,
app\api\admin\passkeys\register\verify\route.ts:59: await getVerifiedTenantSession()
app\api\admin\passkeys\register\verify\route.ts:122: 403,
app\api\admin\passkeys\route.ts:5: getVerifiedTenantSession,
app\api\admin\passkeys\route.ts:65: await getVerifiedTenantSession()
app\api\admin\passkeys\route.ts:117: 403,
app\api\admin\passkeys\route.ts:122: await getVerifiedTenantSession()
app\api\admin\password\change\route.ts:8: getVerifiedTenantSession,
app\api\admin\password\change\route.ts:12: } from "@/lib/admin-user-db"
app\api\admin\password\change\route.ts:69: 403,
app\api\admin\password\change\route.ts:74: await getVerifiedTenantSession()
app\api\admin\pdv-customers\route.ts:9: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\pdv-customers\route.ts:10: import { canUsePdv } from "@/lib/admin-access"
app\api\admin\pdv-customers\route.ts:18: const session = await getVerifiedTenantSession()
app\api\admin\pdv-customers\route.ts:21: if (!canUsePdv(session.role, session.operationalPermissions)) {
app\api\admin\pdv-customers\route.ts:22: return NextResponse.json({ error: "Seu perfil não pode usar o PDV." }, { status: 403 })
app\api\admin\pdv-delivery-quote\route.ts:6: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\pdv-delivery-quote\route.ts:8: import { canUsePdv } from "@/lib/admin-access"
app\api\admin\pdv-delivery-quote\route.ts:21: const session = await getVerifiedTenantSession()
app\api\admin\pdv-delivery-quote\route.ts:29: if (!canUsePdv(session.role, session.operationalPermissions)) {
app\api\admin\pdv-delivery-quote\route.ts:30: return NextResponse.json({ error: "Seu perfil não pode usar o PDV." }, { status: 403 })
app\api\admin\pdv-order\route.ts:18: getVerifiedTenantSession,
app\api\admin\pdv-order\route.ts:23: import { canUsePdv } from "@/lib/admin-access"
app\api\admin\pdv-order\route.ts:132: await getVerifiedTenantSession()
app\api\admin\pdv-order\route.ts:135: if (!canUsePdv(session.role, session.operationalPermissions)) {
app\api\admin\pdv-order\route.ts:141: { status: 403 },
app\api\admin\phase25-6-health\route.ts:6: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\phase25-6-health\route.ts:12: const session = await getVerifiedTenantSession()
app\api\admin\phase25-6-health\route.ts:87: runtimeRole: bridge.role,
app\api\admin\print-agents\route.ts:3: getVerifiedTenantSession,
app\api\admin\print-agents\route.ts:7: } from "@/lib/admin-access"
app\api\admin\print-agents\route.ts:19: await getVerifiedTenantSession()
app\api\admin\print-agents\route.ts:23: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\print-agents\route.ts:27: { status: session ? 403 : 401 },
app\api\admin\print-agents\route.ts:43: await getVerifiedTenantSession()
app\api\admin\print-agents\route.ts:47: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\print-agents\route.ts:51: { status: session ? 403 : 401 },
app\api\admin\print-agents\route.ts:97: await getVerifiedTenantSession()
app\api\admin\print-agents\route.ts:101: !canManageSecurity(session.role, session.operationalPermissions)
app\api\admin\print-agents\route.ts:105: { status: session ? 403 : 401 },
app\api\admin\public-links-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\public-links-health\route.ts:17: const session = await getVerifiedTenantSession()
app\api\admin\rbac-health\route.ts:3: CUSTOM_PERMISSION_MARKER,
app\api\admin\rbac-health\route.ts:4: OPERATIONAL_PERMISSION_CATALOG,
app\api\admin\rbac-health\route.ts:5: ROLE_PERMISSION_PRESETS,
app\api\admin\rbac-health\route.ts:6: } from "@/lib/operational-permissions"
app\api\admin\rbac-health\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\rbac-health\route.ts:13: const session = await getVerifiedTenantSession()
app\api\admin\rbac-health\route.ts:31: COUNT(*) FILTER (WHERE permissions ? $2)::int AS custom_profiles
app\api\admin\rbac-health\route.ts:35: [session.organizationId, CUSTOM_PERMISSION_MARKER],
app\api\admin\rbac-health\route.ts:54: role: session.role,
app\api\admin\rbac-health\route.ts:55: mode: session.operationalPermissionMode,
app\api\admin\rbac-health\route.ts:57: permissionCount: session.operationalPermissions.length,
app\api\admin\rbac-health\route.ts:58: permissions: session.operationalPermissions,
app\api\admin\rbac-health\route.ts:66: permissions: OPERATIONAL_PERMISSION_CATALOG.length,
app\api\admin\rbac-health\route.ts:68: Object.entries(ROLE_PERMISSION_PRESETS).map(([role, permissions]) => [
app\api\admin\rbac-health\route.ts:69: role,
app\api\admin\rbac-health\route.ts:70: permissions.length,
app\api\admin\rbac-health\route.ts:77: backendAuthorization: true,
app\api\admin\rbac-health\route.ts:89: customPermissionsNeverBypassRls: true,
app\api\admin\reports\export\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\reports\export\route.ts:16: const session = await getVerifiedTenantSession()
app\api\admin\reports\export\route.ts:18: if (!canAccessManagementReports(session)) return new Response("Seu perfil não possui acesso aos relatórios gerenciais.", { status: 403 })
app\api\admin\reports\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\reports\route.ts:14: const session = await getVerifiedTenantSession()
app\api\admin\reports\route.ts:16: if (!canAccessManagementReports(session)) return NextResponse.json({ error: "Seu perfil não possui acesso aos relatórios gerenciais." }, { status: 403 })
app\api\admin\reports-health\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\reports-health\route.ts:9: const session = await getVerifiedTenantSession()
app\api\admin\reports-health\route.ts:11: if (!canAccessManagementReports(session)) return NextResponse.json({ error: "Seu perfil não possui acesso aos relatórios gerenciais." }, { status: 403 })
app\api\admin\rls-health\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\rls-health\route.ts:17: const session = await getVerifiedTenantSession()
app\api\admin\rls-health\route.ts:120: controlPlaneTablesRemainBackendAuthorized: true,
app\api\admin\security-health\route.ts:8: } from "@/lib/admin-user-db"
app\api\admin\security-health\route.ts:10: getVerifiedTenantSession,
app\api\admin\security-health\route.ts:25: } from "@/lib/tenant-permissions"
app\api\admin\security-health\route.ts:32: await getVerifiedTenantSession()
app\api\admin\security-health\route.ts:45: if (!canViewSecurity(session.role)) {
app\api\admin\security-health\route.ts:48: { status: 403 },
app\api\admin\security-health\route.ts:74: canViewTeam(session.role)
app\api\admin\security-health\route.ts:100: item.membershipStatus ===
app\api\admin\security-health\route.ts:106: item.membershipStatus ===
app\api\admin\security-health\route.ts:112: item.membershipStatus ===
app\api\admin\security-health\route.ts:176: role:
app\api\admin\security-health\route.ts:177: session.role,
app\api\admin\security-health\route.ts:204: ? "O enforcement tenant RLS está ativo. A validação de FORCE RLS e do papel restrito do runtime é feita em /api/admin/rls-health."
app\api\admin\sessions\route.ts:10: getVerifiedTenantSession,
app\api\admin\sessions\route.ts:16: } from "@/lib/security/admin-sessions"
app\api\admin\sessions\route.ts:68: await getVerifiedTenantSession()
app\api\admin\sessions\route.ts:105: 403,
app\api\admin\sessions\route.ts:110: await getVerifiedTenantSession()
app\api\admin\sessions\route.ts:169: 403,
app\api\admin\sessions\route.ts:174: await getVerifiedTenantSession()
app\api\admin\staff-profile-health\route.ts:7: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\staff-profile-health\route.ts:8: import { canViewTeam } from "@/lib/tenant-permissions"
app\api\admin\staff-profile-health\route.ts:13: const session = await getVerifiedTenantSession()
app\api\admin\staff-profile-health\route.ts:19: if (!canViewTeam(session.role)) {
app\api\admin\staff-profile-health\route.ts:22: { status: 403 },
app\api\admin\staff-profile-health\route.ts:45: role: session.role,
app\api\admin\staff-profile-health\route.ts:61: perEmployeePermissionsPreserved: true,
app\api\admin\storage\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\storage\route.ts:5: import { canManageSecurity } from "@/lib/admin-access"
app\api\admin\storage\route.ts:24: async function authorizedSession() {
app\api\admin\storage\route.ts:25: const session = await getVerifiedTenantSession().catch(() => null)
app\api\admin\storage\route.ts:27: if (!canManageSecurity(session.role, session.operationalPermissions)) return null
app\api\admin\storage\route.ts:44: const session = await authorizedSession()
app\api\admin\storage\route.ts:64: const session = await authorizedSession()
app\api\admin\switch-organization\route.ts:11: getVerifiedTenantSession,
app\api\admin\switch-organization\route.ts:15: } from "@/lib/security/admin-sessions"
app\api\admin\switch-organization\route.ts:21: await getVerifiedTenantSession()
app\api\admin\switch-organization\route.ts:63: { status: 403 },
app\api\admin\switch-organization\route.ts:98: role: context.role,
app\api\admin\team\access\route.ts:5: getVerifiedTenantSession,
app\api\admin\team\access\route.ts:11: } from "@/lib/tenant-permissions"
app\api\admin\team\access\route.ts:24: await getVerifiedTenantSession()
app\api\admin\team\access\route.ts:28: !canViewTeam(session.role)
app\api\admin\team\access\route.ts:32: { status: session ? 403 : 401 },
app\api\admin\team\access\route.ts:54: await getVerifiedTenantSession()
app\api\admin\team\access\route.ts:58: !canManageAccess(session.role)
app\api\admin\team\access\route.ts:62: { status: session ? 403 : 401 },
app\api\admin\team\access\route.ts:104: session.role,
app\api\admin\team\access\route.ts:139: if (!targetAccess || !["active", "invited"].includes(String(targetAccess.membershipStatus))) {
app\api\admin\team\access\route.ts:184: await getVerifiedTenantSession()
app\api\admin\team\access\route.ts:188: !canManageAccess(session.role)
app\api\admin\team\access\route.ts:192: { status: session ? 403 : 401 },
app\api\admin\tenant-context\route.ts:3: getAdminSession,
app\api\admin\tenant-context\route.ts:6: getVerifiedTenantSession,
app\api\admin\tenant-context\route.ts:9: listOrganizationMembershipsForUserId,
app\api\admin\tenant-context\route.ts:17: await getAdminSession()
app\api\admin\tenant-context\route.ts:45: await getVerifiedTenantSession()
app\api\admin\tenant-context\route.ts:52: "A membership ativa não existe mais.",
app\api\admin\tenant-context\route.ts:54: { status: 403 },
app\api\admin\tenant-context\route.ts:59: await listOrganizationMembershipsForUserId(
app\api\admin\tenant-context\route.ts:69: role: session.role,
app\api\admin\tenant-context\route.ts:79: activeMembership: true,
app\api\admin\tenant-context\route.ts:89: role:
app\api\admin\tenant-context\route.ts:90: organization.role,
app\api\admin\tenant-runtime-health\route.ts:13: getVerifiedTenantSession,
app\api\admin\tenant-runtime-health\route.ts:23: const session = await getVerifiedTenantSession()
app\api\admin\tenant-runtime-health\route.ts:101: role: session.role,
app\api\admin\workspaces-health\route.ts:6: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\admin\workspaces-health\route.ts:11: const session = await getVerifiedTenantSession()
app\api\admin\workspaces-health\route.ts:29: role: session.role,
app\api\admin\workspaces-health\route.ts:30: permissionMode: session.operationalPermissionMode,
app\api\admin\workspaces-health\route.ts:32: session.role,
app\api\admin\workspaces-health\route.ts:33: session.operationalPermissions,
app\api\admin\workspaces-health\route.ts:37: manager: {
app\api\admin\workspaces-health\route.ts:39: allowed: canAccessOperationalWorkspace("manager", session.role, session.operationalPermissions),
app\api\admin\workspaces-health\route.ts:43: allowed: canAccessOperationalWorkspace("pdv", session.role, session.operationalPermissions),
app\api\admin\workspaces-health\route.ts:45: kitchen: {
app\api\admin\workspaces-health\route.ts:47: allowed: canAccessOperationalWorkspace("kitchen", session.role, session.operationalPermissions),
app\api\admin\workspaces-health\route.ts:49: courier: {
app\api\admin\workspaces-health\route.ts:51: allowed: canAccessOperationalWorkspace("courier", session.role, session.operationalPermissions),
app\api\auth\2fa\verify\route.ts:33: } from "@/lib/security/admin-sessions"
app\api\auth\2fa\verify\route.ts:119: 403,
app\api\auth\2fa\verify\route.ts:277: 403,
app\api\auth\2fa\verify\route.ts:292: : "/admin"
app\api\auth\2fa\verify\route.ts:308: superadminAuthorized:
app\api\auth\google\route.ts:11: } from "@/lib/admin-user-db"
app\api\auth\google\route.ts:120: 403,
app\api\auth\google\route.ts:127: `admin-google:${requestIp(
app\api\auth\google\route.ts:188: 403,
app\api\auth\google\route.ts:205: 403,
app\api\auth\google\route.ts:258: "[SaborFlow Google Admin] Falha ao autenticar:",
app\api\auth\login\route.ts:11: } from "@/lib/admin-user-db"
app\api\auth\login\route.ts:145: 403,
app\api\auth\login\route.ts:310: 403,
app\api\auth\logout\route.ts:19: } from "@/lib/security/admin-sessions"
app\api\auth\passkey\options\route.ts:127: 403,
app\api\auth\passkey\verify\route.ts:33: } from "@/lib/security/admin-sessions"
app\api\auth\passkey\verify\route.ts:127: 403,
app\api\auth\passkey\verify\route.ts:254: 403,
app\api\auth\passkey\verify\route.ts:298: 403,
app\api\auth\passkey\verify\route.ts:313: : "/admin"
app\api\auth\passkey\verify\route.ts:329: superadminAuthorized:
app\api\auth\password-reset\request\route.ts:3: import { createSelfServicePasswordReset } from "@/lib/admin-user-db"
app\api\auth\password-reset\request\route.ts:82: authorization: `Bearer ${apiKey}`,
app\api\auth\password-reset\request\route.ts:124: 403,
app\api\billing\google\route.ts:34: return error("Origem da requisição não autorizada.", 403)
app\api\billing\google\route.ts:83: 403,
app\api\billing\sign-in\route.ts:2: import { authenticateAdminUser } from "@/lib/admin-user-db"
app\api\billing\sign-in\route.ts:32: if (!requestIsSameOrigin(request)) return jsonError("Origem da requisição não autorizada.", 403)
app\api\billing\sign-in\route.ts:64: return jsonError("Não foi possível entrar nesta conta.", 403)
app\api\cash\route.ts:9: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\cash\route.ts:10: import { canUseCashRegister } from "@/lib/tenant-permissions"
app\api\cash\route.ts:38: const session = await getVerifiedTenantSession()
app\api\cash\route.ts:50: if (!canUseCashRegister(session.role)) {
app\api\cash\route.ts:56: { status: 403 },
app\api\cash\route.ts:104: const session = await getVerifiedTenantSession()
app\api\cash\route.ts:116: if (!canUseCashRegister(session.role)) {
app\api\cash\route.ts:122: { status: 403 },
app\api\categories\[id]\route.ts:6: getVerifiedTenantSession,
app\api\categories\[id]\route.ts:13: const session = await getVerifiedTenantSession().catch(() => null)
app\api\categories\[id]\route.ts:36: if (!canManageCatalog(session.role)) {
app\api\categories\[id]\route.ts:39: { status: 403 },
app\api\categories\route.ts:9: getVerifiedTenantSession,
app\api\categories\route.ts:13: const session = await getVerifiedTenantSession().catch(() => null)
app\api\categories\route.ts:33: const session = await getVerifiedTenantSession().catch(() => null)
app\api\categories\route.ts:47: if (!canManageCatalog(session.role)) {
app\api\categories\route.ts:50: { status: 403 },
app\api\coupons\[id]\route.ts:15: getVerifiedTenantSession,
app\api\coupons\[id]\route.ts:19: } from "@/lib/tenant-permissions"
app\api\coupons\[id]\route.ts:46: const session = await getVerifiedTenantSession()
app\api\coupons\[id]\route.ts:67: if (!canManageMarketing(session.role)) {
app\api\coupons\[id]\route.ts:73: { status: 403 },
app\api\coupons\route.ts:10: getVerifiedTenantSession,
app\api\coupons\route.ts:14: } from "@/lib/tenant-permissions"
app\api\coupons\route.ts:89: const session = await getVerifiedTenantSession()
app\api\coupons\route.ts:143: const session = await getVerifiedTenantSession()
app\api\coupons\route.ts:162: if (!canManageMarketing(session.role)) {
app\api\coupons\route.ts:168: { status: 403 },
app\api\courier\location\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\courier\location\route.ts:9: const session = await getVerifiedTenantSession()
app\api\courier\location\route.ts:15: if (session.role !== "courier") {
app\api\courier\location\route.ts:18: { status: 403 },
app\api\couriers\[id]\route.ts:7: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\couriers\[id]\route.ts:8: import { canManageDeliveryOperation } from "@/lib/tenant-permissions"
app\api\couriers\[id]\route.ts:16: const session = await getVerifiedTenantSession()
app\api\couriers\[id]\route.ts:25: if (!canManageDeliveryOperation(session.role)) {
app\api\couriers\[id]\route.ts:28: { status: 403 },
app\api\couriers\[id]\route.ts:80: const courier = await updateTenantCourier(
app\api\couriers\[id]\route.ts:86: if (!courier) {
app\api\couriers\[id]\route.ts:93: return NextResponse.json({ courier })
app\api\couriers\[id]\route.ts:111: const session = await getVerifiedTenantSession()
app\api\couriers\[id]\route.ts:120: if (!canManageDeliveryOperation(session.role)) {
app\api\couriers\[id]\route.ts:123: { status: 403 },
app\api\couriers\[id]\route.ts:148: const courier = await deactivateTenantCourier(
app\api\couriers\[id]\route.ts:153: if (!courier) {
app\api\couriers\route.ts:7: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\couriers\route.ts:8: import { canManageDeliveryOperation } from "@/lib/tenant-permissions"
app\api\couriers\route.ts:13: const session = await getVerifiedTenantSession()
app\api\couriers\route.ts:22: if (!canManageDeliveryOperation(session.role)) {
app\api\couriers\route.ts:25: { status: 403 },
app\api\couriers\route.ts:49: const session = await getVerifiedTenantSession()
app\api\couriers\route.ts:58: if (!canManageDeliveryOperation(session.role)) {
app\api\couriers\route.ts:61: { status: 403 },
app\api\couriers\route.ts:96: const courier = await createTenantCourier(
app\api\couriers\route.ts:107: { courier },
app\api\dashboard\live\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\dashboard\live\route.ts:5: import { canReadOrders } from "@/lib/admin-access"
app\api\dashboard\live\route.ts:12: const session = await getVerifiedTenantSession()
app\api\dashboard\live\route.ts:19: role: session.role,
app\api\dashboard\live\route.ts:21: session.role,
app\api\dashboard\live\route.ts:22: session.operationalPermissions,
app\api\dashboard\live\route.ts:26: if (!canReadOrders(session.role, session.operationalPermissions)) {
app\api\dashboard\live\route.ts:37: if (session.role !== "courier") {
app\api\dashboard\live\route.ts:44: role: session.role,
app\api\dashboard\live\route.ts:45: permissions: session.operationalPermissions,
app\api\dashboard\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\dashboard\route.ts:4: import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
app\api\dashboard\route.ts:10: const session = await getVerifiedTenantSession()
app\api\dashboard\route.ts:21: session.operationalPermissions,
app\api\dashboard\route.ts:25: role: session.role,
app\api\dashboard\route.ts:27: session.role,
app\api\dashboard\route.ts:28: session.operationalPermissions,
app\api\dashboard\route.ts:32: if (session.role !== "courier") {
app\api\dashboard\route.ts:42: role: session.role,
app\api\dashboard\route.ts:43: permissions: session.operationalPermissions,
app\api\dashboard\route.ts:52: couriers: dispatch.courier ? [dispatch.courier] : [],
app\api\dashboard\route.ts:53: courierIdentity: dispatch.courier
app\api\dashboard\route.ts:55: id: dispatch.courier.id,
app\api\dashboard\route.ts:56: name: dispatch.courier.name,
app\api\dashboard\route.ts:57: staffMemberId: dispatch.courier.staffMemberId ?? null,
app\api\delivery-zones\[id]\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\delivery-zones\[id]\route.ts:9: import { canManageDeliveryOperation } from "@/lib/tenant-permissions"
app\api\delivery-zones\[id]\route.ts:43: const session = await getVerifiedTenantSession()
app\api\delivery-zones\[id]\route.ts:51: if (!canManageDeliveryOperation(session.role)) {
app\api\delivery-zones\[id]\route.ts:54: { status: 403 },
app\api\delivery-zones\[id]\route.ts:152: const session = await getVerifiedTenantSession()
app\api\delivery-zones\[id]\route.ts:160: if (!canManageDeliveryOperation(session.role)) {
app\api\delivery-zones\[id]\route.ts:163: { status: 403 },
app\api\delivery-zones\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\delivery-zones\route.ts:9: import { canManageDeliveryOperation } from "@/lib/tenant-permissions"
app\api\delivery-zones\route.ts:40: const session = await getVerifiedTenantSession()
app\api\delivery-zones\route.ts:48: if (!canManageDeliveryOperation(session.role)) {
app\api\delivery-zones\route.ts:51: { status: 403 },
app\api\delivery-zones\route.ts:98: const session = await getVerifiedTenantSession()
app\api\delivery-zones\route.ts:106: if (!canManageDeliveryOperation(session.role)) {
app\api\delivery-zones\route.ts:109: { status: 403 },
app\api\demo\public\start\route.ts:12: } from "@/lib/security/admin-sessions"
app\api\demo\public\start\route.ts:19: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\demo\public\start\route.ts:34: superadminAuthorized: false,
app\api\demo\public\start\route.ts:43: redirectTo: "/admin?demo=1",
app\api\demo\trial\start\route.ts:13: } from "@/lib/security/admin-sessions"
app\api\demo\trial\start\route.ts:20: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\demo\trial\start\route.ts:43: superadminAuthorized: false,
app\api\demo\trial\start\route.ts:53: redirectTo: "/admin?trial=1",
app\api\feedback\route.ts:12: getVerifiedTenantSession,
app\api\feedback\route.ts:16: } from "@/lib/tenant-permissions"
app\api\feedback\route.ts:27: const session = await getVerifiedTenantSession()
app\api\feedback\route.ts:45: if (!canViewFeedback(session.role)) {
app\api\feedback\route.ts:51: { status: 403 },
app\api\financial\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\financial\route.ts:12: } from "@/lib/tenant-permissions"
app\api\financial\route.ts:49: const session = await getVerifiedTenantSession()
app\api\financial\route.ts:61: if (!canViewFinance(session.role)) {
app\api\financial\route.ts:67: { status: 403 },
app\api\financial\route.ts:120: const session = await getVerifiedTenantSession()
app\api\financial\route.ts:132: if (!canManageFinance(session.role)) {
app\api\financial\route.ts:138: { status: 403 },
app\api\internal\integrations\process\route.ts:3: import { integrationWorkerRequestIsAuthorized } from "@/lib/integrations-request"
app\api\internal\integrations\process\route.ts:10: if (!integrationWorkerRequestIsAuthorized(request)) {
app\api\invitations\[token]\route.ts:59: role: preview.role,
app\api\legal\accept\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\legal\accept\route.ts:11: return NextResponse.json({ error: "Origem da requisição não autorizada." }, { status: 403 })
app\api\legal\accept\route.ts:14: const session = await getVerifiedTenantSession().catch(() => null)
app\api\legal\accept\route.ts:26: source: "admin-legal-gate",
app\api\orders\accept-pending\route.ts:2: import { canUpdateOrderStatus } from "@/lib/admin-access"
app\api\orders\accept-pending\route.ts:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\orders\accept-pending\route.ts:8: const session = await getVerifiedTenantSession()
app\api\orders\accept-pending\route.ts:15: session.role === "courier" ||
app\api\orders\accept-pending\route.ts:16: !canUpdateOrderStatus(session.role, session.operationalPermissions)
app\api\orders\accept-pending\route.ts:20: { status: 403 },
app\api\orders\[id]\ticket-pdf\route.ts:5: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\orders\[id]\ticket-pdf\route.ts:8: import { canReadOrders } from "@/lib/admin-access"
app\api\orders\[id]\ticket-pdf\route.ts:52: const session = await getVerifiedTenantSession()
app\api\orders\[id]\ticket-pdf\route.ts:55: !canReadOrders(session.role, session.operationalPermissions)
app\api\orders\[id]\ticket-pdf\route.ts:59: { status: 403 },
app\api\orders\[id]\ticket-pdf\route.ts:90: if (session?.role === "courier") {
app\api\orders\[id]\ticket-pdf\route.ts:95: role: session.role,
app\api\orders\[id]\ticket-pdf\route.ts:101: { status: 403 },
app\api\orders\[id]\ticket-pdf\route.ts:136: const mode = new URL(request.url).searchParams.get("mode") === "kitchen" ? "kitchen" : "customer"
app\api\orders\[id]\ticket-pdf\route.ts:155: lines.push({ text: `${item.quantity}x ${item.name}${mode === "customer" ? `  ${money(item.subtotal)}` : ""}`, bold: mode === "kitchen" })
app\api\orders\[id]\route.ts:7: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\orders\[id]\route.ts:15: } from "@/lib/admin-access"
app\api\orders\[id]\route.ts:38: const session = await getVerifiedTenantSession()
app\api\orders\[id]\route.ts:110: !canUpdateOrderStatus(session.role, session.operationalPermissions)
app\api\orders\[id]\route.ts:114: { status: 403 },
app\api\orders\[id]\route.ts:120: !canUpdatePaymentStatus(session.role, session.operationalPermissions)
app\api\orders\[id]\route.ts:124: { status: 403 },
app\api\orders\[id]\route.ts:130: !canAssignCourier(session.role, session.operationalPermissions)
app\api\orders\[id]\route.ts:134: { status: 403 },
app\api\orders\[id]\route.ts:138: if (session.role === "cashier" && body.status) {
app\api\orders\[id]\route.ts:154: { status: 403 },
app\api\orders\[id]\route.ts:159: if (session.role === "kitchen" && body.status) {
app\api\orders\[id]\route.ts:171: { status: 403 },
app\api\orders\[id]\route.ts:176: if (session.role === "courier" && body.status) {
app\api\orders\[id]\route.ts:181: "Sua sessão atual é de entregador. O cancelamento deve ser feito no Admin ou Caixa por uma conta autorizada. Pedidos agendados podem ser cancelados normalmente por uma sessão de gestão.",
app\api\orders\[id]\route.ts:185: { status: 403 },
app\api\orders\[id]\route.ts:193: role: session.role,
app\api\orders\[id]\route.ts:204: { status: 403 },
app\api\orders\[id]\route.ts:216: "Sua sessão atual é de entregador. No app do entregador, o pedido só pode avançar de pronto para em rota e depois para concluído. Para aceitar, preparar ou cancelar, use uma sessão de gestão no Admin/Caixa.",
app\api\orders\[id]\route.ts:220: { status: 403 },
app\api\orders\[id]\route.ts:258: const courier = await getTenantCourier(
app\api\orders\[id]\route.ts:263: if (!courier || !courier.active) {
app\api\orders\[id]\route.ts:271: courierId: courier.id,
app\api\orders\[id]\route.ts:272: courierName: courier.name,
app\api\orders\route.ts:16: getVerifiedTenantSession,
app\api\orders\route.ts:24: import { canReadOrders } from "@/lib/admin-access"
app\api\orders\route.ts:42: await getVerifiedTenantSession()
app\api\orders\route.ts:45: if (!canReadOrders(session.role, session.operationalPermissions)) {
app\api\orders\route.ts:51: { status: 403 },
app\api\orders\route.ts:61: if (session.role === "courier") {
app\api\orders\route.ts:65: role: session.role,
app\api\orders\route.ts:66: permissions: session.operationalPermissions,
app\api\password-reset\[token]\route.ts:5: } from "@/lib/admin-user-db"
app\api\print-queue\route.ts:100: { status: 403 },
app\api\print-queue\route.ts:162: { status: 403 },
app\api\products\[id]\composition\route.ts:11: getVerifiedTenantSession,
app\api\products\[id]\composition\route.ts:26: const session = await getVerifiedTenantSession()
app\api\products\[id]\composition\route.ts:63: const session = await getVerifiedTenantSession()
app\api\products\[id]\composition\route.ts:71: if (!canManageCatalog(session.role)) {
app\api\products\[id]\composition\route.ts:74: { status: 403 },
app\api\products\[id]\route.ts:9: getVerifiedTenantSession,
app\api\products\[id]\route.ts:16: const session = await getVerifiedTenantSession().catch(() => null)
app\api\products\[id]\route.ts:72: if (!canManageCatalog(session.role)) {
app\api\products\[id]\route.ts:75: { status: 403 },
app\api\products\[id]\route.ts:116: const session = await getVerifiedTenantSession().catch(() => null)
app\api\products\[id]\route.ts:135: if (!canManageCatalog(session.role)) {
app\api\products\[id]\route.ts:138: { status: 403 },
app\api\products\route.ts:9: getVerifiedTenantSession,
app\api\products\route.ts:14: const session = await getVerifiedTenantSession().catch(() => null)
app\api\products\route.ts:34: const session = await getVerifiedTenantSession().catch(() => null)
app\api\products\route.ts:79: if (!canManageCatalog(session.role)) {
app\api\products\route.ts:82: { status: 403 },
app\api\settings\route.ts:7: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\settings\route.ts:11: } from "@/lib/tenant-permissions"
app\api\settings\route.ts:27: const session = await getVerifiedTenantSession()
app\api\settings\route.ts:36: if (!canViewOrganizationSettings(session.role)) {
app\api\settings\route.ts:39: { status: 403 },
app\api\settings\route.ts:73: const session = await getVerifiedTenantSession()
app\api\settings\route.ts:82: if (!canManageOrganizationSettings(session.role)) {
app\api\settings\route.ts:85: { status: 403 },
app\api\staff\[id]\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\staff\[id]\route.ts:12: } from "@/lib/tenant-permissions"
app\api\staff\[id]\route.ts:21: const session = await getVerifiedTenantSession()
app\api\staff\[id]\route.ts:30: if (!canManageTeam(session.role)) {
app\api\staff\[id]\route.ts:33: { status: 403 },
app\api\staff\[id]\route.ts:52: (body.permissions !== undefined || body.role !== undefined) &&
app\api\staff\[id]\route.ts:53: !canManageAccess(session.role)
app\api\staff\[id]\route.ts:60: { status: 403 },
app\api\staff\[id]\route.ts:74: ...(body.role !== undefined
app\api\staff\[id]\route.ts:75: ? { role: String(body.role) as StaffRole }
app\api\staff\[id]\route.ts:80: ...(body.permissions !== undefined && Array.isArray(body.permissions)
app\api\staff\[id]\route.ts:81: ? { permissions: body.permissions.map(String) }
app\api\staff\[id]\route.ts:150: const session = await getVerifiedTenantSession()
app\api\staff\[id]\route.ts:159: if (!canManageAccess(session.role)) {
app\api\staff\[id]\route.ts:165: { status: 403 },
app\api\staff\route.ts:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\staff\route.ts:13: } from "@/lib/tenant-permissions"
app\api\staff\route.ts:19: const session = await getVerifiedTenantSession()
app\api\staff\route.ts:28: if (!canViewTeam(session.role)) {
app\api\staff\route.ts:31: { status: 403 },
app\api\staff\route.ts:63: const session = await getVerifiedTenantSession()
app\api\staff\route.ts:72: if (!canManageTeam(session.role)) {
app\api\staff\route.ts:75: { status: 403 },
app\api\staff\route.ts:94: role: String(body.role || "cashier") as StaffRole,
app\api\staff\route.ts:95: permissions: Array.isArray(body.permissions)
app\api\staff\route.ts:96: ? body.permissions.map(String)
app\api\staff\route.ts:104: (input.role === "admin" || input.permissions.length > 0) &&
app\api\staff\route.ts:105: !canManageAccess(session.role)
app\api\staff\route.ts:112: { status: 403 },
app\api\superadmin\actions\route.ts:11: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\superadmin\actions\route.ts:14: if (!access) return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\superadmin\actions\route.ts:21: if (!canReviewRegistrations(access.role)) return NextResponse.json({ error: "Sem permissão para validar cadastros." }, { status: 403 })
app\api\superadmin\actions\route.ts:26: if (!canManageCommercialState(access.role)) return NextResponse.json({ error: "Sem permissão para alterar cobrança." }, { status: 403 })
app\api\superadmin\actions\route.ts:31: if (!canManageCommercialState(access.role)) return NextResponse.json({ error: "Sem permissão para alterar planos." }, { status: 403 })
app\api\superadmin\actions\route.ts:34: if (!canManageCommercialState(access.role)) return NextResponse.json({ error: "Sem permissão para liberar recursos." }, { status: 403 })
app\api\superadmin\actions\route.ts:37: if (!canManageCommercialState(access.role)) return NextResponse.json({ error: "Sem permissão para gerenciar cupons." }, { status: 403 })
app\api\superadmin\actions\route.ts:48: if (!canManageSupport(access.role)) return NextResponse.json({ error: "Sem permissão para gerenciar suporte." }, { status: 403 })
app\api\superadmin\finance\route.ts:14: if (!access) return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\superadmin\finance\route.ts:21: return NextResponse.json({ error: "Origem da requisição não permitida." }, { status: 403 })
app\api\superadmin\finance\route.ts:24: if (!access) return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\superadmin\finance\route.ts:25: if (!canManagePlatformFinance(access.role)) {
app\api\superadmin\finance\route.ts:26: return NextResponse.json({ error: "Sem permissão para gerenciar o financeiro da plataforma." }, { status: 403 })
app\api\superadmin\health\route.ts:9: if (!access) return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\superadmin\health\route.ts:33: access: { email: access.email, role: access.role, platformAdminId: access.platformAdminId },
app\api\superadmin\overview\route.ts:9: if (!access) return NextResponse.json({ error: "Não autorizado." }, { status: 403 })
app\api\superadmin\overview\route.ts:10: return NextResponse.json({ ok: true, access: { email: access.email, role: access.role }, data: await getSuperadminSnapshot() })
app\api\uploads\product-image\route.ts:2: import { getVerifiedTenantSession, canManageCatalog } from "@/lib/tenant-access"
app\api\uploads\product-image\route.ts:7: const session = await getVerifiedTenantSession().catch(() => null)
app\api\uploads\product-image\route.ts:12: if (!canManageCatalog(session.role)) {
app\api\uploads\product-image\route.ts:15: { status: 403 },
app\api\uploads\store-image\route.ts:2: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\api\uploads\store-image\route.ts:3: import { canManageOrganizationSettings } from "@/lib/tenant-permissions"
app\api\uploads\store-image\route.ts:8: const session = await getVerifiedTenantSession().catch(() => null)
app\api\uploads\store-image\route.ts:13: if (!canManageOrganizationSettings(session.role)) {
app\api\uploads\store-image\route.ts:16: { status: 403 },
app\cozinha\page.tsx:2: import { KitchenWorkspace } from "@/components/operational/kitchen-workspace"
app\cozinha\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\cozinha\page.tsx:5: import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
app\cozinha\page.tsx:10: const session = await getVerifiedTenantSession()
app\cozinha\page.tsx:13: if (!canAccessOperationalWorkspace("kitchen", session.role, session.operationalPermissions)) {
app\cozinha\page.tsx:14: redirect(getDefaultOperationalPath(session.role, session.operationalPermissions))
app\cozinha\page.tsx:17: const data = await getTenantAwareAdminData(session, session.operationalPermissions)
app\entregador\page.tsx:2: import { CourierWorkspace } from "@/components/operational/courier-workspace"
app\entregador\page.tsx:8: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\entregador\page.tsx:14: const session = await getVerifiedTenantSession()
app\entregador\page.tsx:19: "courier",
app\entregador\page.tsx:20: session.role,
app\entregador\page.tsx:21: session.operationalPermissions,
app\entregador\page.tsx:26: session.role,
app\entregador\page.tsx:27: session.operationalPermissions,
app\entregador\page.tsx:39: role: session.role,
app\entregador\page.tsx:40: permissions: session.operationalPermissions,
app\entregador\page.tsx:50: courier={dispatch.courier}
app\gerente\page.tsx:2: import { AdminDashboard } from "@/components/admin/admin-dashboard"
app\gerente\page.tsx:6: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\gerente\page.tsx:7: import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
app\gerente\page.tsx:12: const session = await getVerifiedTenantSession()
app\gerente\page.tsx:15: if (!canAccessOperationalWorkspace("manager", session.role, session.operationalPermissions)) {
app\gerente\page.tsx:16: redirect(getDefaultOperationalPath(session.role, session.operationalPermissions))
app\gerente\page.tsx:24: adminRole={session.role}
app\gerente\page.tsx:25: operationalPermissions={session.operationalPermissions}
app\gerente\page.tsx:31: initialData={await getTenantAwareAdminData(session, session.operationalPermissions)}
app\legal\aceite\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\legal\aceite\page.tsx:9: const session = await getVerifiedTenantSession()
app\legal\aceite\page.tsx:11: if (await hasCurrentLegalAcceptance(session.userId, session.organizationId)) redirect("/admin")
app\login\page.tsx:2: import { LoginForm } from "@/components/admin/login-form"
app\login\page.tsx:8: if (await isAdminAuthenticated()) redirect("/admin")
app\minha-loja\route.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\minha-loja\route.ts:24: const session = await getVerifiedTenantSession()
app\onboarding\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\onboarding\page.tsx:9: const session = await getVerifiedTenantSession()
app\onboarding\page.tsx:11: if (session.role !== "owner") redirect("/admin")
app\onboarding\page.tsx:28: if (onboarding.state.completed) redirect("/admin")
app\pdv\page.tsx:4: import { getVerifiedTenantSession } from "@/lib/tenant-access"
app\pdv\page.tsx:5: import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
app\pdv\page.tsx:10: const session = await getVerifiedTenantSession()
app\pdv\page.tsx:13: if (!canAccessOperationalWorkspace("pdv", session.role, session.operationalPermissions)) {
app\pdv\page.tsx:14: redirect(getDefaultOperationalPath(session.role, session.operationalPermissions))
app\pdv\page.tsx:17: const data = await getTenantAwareAdminData(session, session.operationalPermissions)
app\superadmin\page.tsx:3: import { getAdminSession } from "@/lib/auth"
app\superadmin\page.tsx:10: const session = await getAdminSession()
app\superadmin\page.tsx:26: return <SuperadminDashboard access={{ email: access.email, role: access.role }} initialData={await getSuperadminSnapshot()} />
app\totem\page.tsx:38: Ative o modo totem em Admin → Configurações para usar esta tela.
lib\billing-providers\mercado-pago.ts:20: Authorization: `Bearer ${accessToken()}`,
lib\security\admin-sessions.ts:28: superadmin_authorized: boolean
lib\security\admin-sessions.ts:40: superadminAuthorized: boolean
lib\security\admin-sessions.ts:58: `saborflow:admin-session:${namespace}:v1:${value}`,
lib\security\admin-sessions.ts:112: superadminAuthorized: boolean
lib\security\admin-sessions.ts:150: superadmin_authorized,
lib\security\admin-sessions.ts:181: input.superadminAuthorized,
lib\security\admin-sessions.ts:293: s.superadmin_authorized,
lib\security\admin-sessions.ts:331: superadminAuthorized:
lib\security\admin-sessions.ts:333: row.superadmin_authorized,
lib\security\two-factor-challenge.ts:20: purpose: "admin-2fa"
lib\security\two-factor-challenge.ts:81: purpose: "admin-2fa",
lib\security\two-factor-challenge.ts:97: `admin-2fa:${encoded}`,
lib\security\two-factor-challenge.ts:126: `admin-2fa:${encoded}`,
lib\security\two-factor-challenge.ts:148: payload.purpose !== "admin-2fa" ||
lib\security\two-factor-challenge.ts:167: purpose: "admin-2fa",
lib\storage\r2.ts:155: "authorization",
lib\admin-access.ts:3: getRolePermissionPreset,
lib\admin-access.ts:4: permissionListHas,
lib\admin-access.ts:5: type OperationalPermission,
lib\admin-access.ts:6: } from "@/lib/operational-permissions"
lib\admin-access.ts:14: | "kitchen"
lib\admin-access.ts:28: const sectionPermission: Record<AdminSection, OperationalPermission> = {
lib\admin-access.ts:34: kitchen: "kitchen.use",
lib\admin-access.ts:49: const allSections = Object.keys(sectionPermission) as AdminSection[]
lib\admin-access.ts:52: role: OrganizationRole,
lib\admin-access.ts:53: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:55: return permissions ?? getRolePermissionPreset(role)
lib\admin-access.ts:59: role: OrganizationRole,
lib\admin-access.ts:60: permission: OperationalPermission,
lib\admin-access.ts:61: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:63: return permissionListHas(accessList(role, permissions), permission)
lib\admin-access.ts:67: role: OrganizationRole,
lib\admin-access.ts:68: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:71: has(role, sectionPermission[section], permissions),
lib\admin-access.ts:76: role: OrganizationRole,
lib\admin-access.ts:78: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:80: return has(role, sectionPermission[section], permissions)
lib\admin-access.ts:84: role: OrganizationRole,
lib\admin-access.ts:85: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:87: return has(role, "orders.view", permissions)
lib\admin-access.ts:91: role: OrganizationRole,
lib\admin-access.ts:92: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:94: return has(role, "customers.view", permissions)
lib\admin-access.ts:98: role: OrganizationRole,
lib\admin-access.ts:99: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:101: return has(role, "finance.view", permissions)
lib\admin-access.ts:105: role: OrganizationRole,
lib\admin-access.ts:106: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:108: return has(role, "catalog.view", permissions)
lib\admin-access.ts:112: role: OrganizationRole,
lib\admin-access.ts:113: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:115: return has(role, "marketing.view", permissions)
lib\admin-access.ts:119: role: OrganizationRole,
lib\admin-access.ts:120: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:122: return has(role, "team.view", permissions)
lib\admin-access.ts:126: role: OrganizationRole,
lib\admin-access.ts:127: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:129: return has(role, "security.manage", permissions)
lib\admin-access.ts:133: role: OrganizationRole,
lib\admin-access.ts:134: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:136: return has(role, "pdv.use", permissions)
lib\admin-access.ts:140: role: OrganizationRole,
lib\admin-access.ts:141: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:143: return has(role, "orders.status.update", permissions)
lib\admin-access.ts:147: role: OrganizationRole,
lib\admin-access.ts:148: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:150: return has(role, "orders.payment.update", permissions)
lib\admin-access.ts:154: role: OrganizationRole,
lib\admin-access.ts:155: permissions?: readonly OperationalPermission[],
lib\admin-access.ts:157: return has(role, "delivery.manage", permissions)
lib\admin-help.ts:118: "orders.courier": {
lib\admin-help.ts:170: "team.role": {
lib\admin-help.ts:172: text: "A função determina o conjunto padrão de permissões e quais áreas do Admin o colaborador pode usar.",
lib\admin-help.ts:202: "kitchen.flow": {
lib\auth.ts:41: role: OrganizationRole
lib\auth.ts:48: // getAdminSession() nunca produz esta variante após a Fase 25.
lib\auth.ts:89: | "role"
lib\auth.ts:102: role: session.role,
lib\auth.ts:132: role: context.role,
lib\auth.ts:192: role?: OrganizationRole
lib\auth.ts:206: !payload.role ||
lib\auth.ts:223: role: payload.role,
lib\auth.ts:288: role: session.role,
lib\auth.ts:322: export async function getAdminSession(): Promise<AdminSession | null> {
lib\auth.ts:343: return Boolean(await getAdminSession())
lib\billing-contracting.ts:3: import { hashAdminPassword } from "@/lib/admin-user-db"
lib\billing-contracting.ts:29: kitchen: false,
lib\billing-contracting.ts:58: kitchen: raw.kitchen === true,
lib\billing-contracting.ts:476: if (normalized === "authorized") return "active"
lib\billing-contracting.ts:671: FROM sf_memberships
lib\billing-db.ts:17: kitchen: false,
lib\billing-db.ts:30: constructor(message: string, code: string, status = 403) {
lib\billing-db.ts:86: kitchen: boolValue(raw.kitchen),
lib\billing-db.ts:235: FROM sf_memberships m
lib\billing-db.ts:462: throw new BillingAccessError("Este recurso não está incluído no plano atual.", `entitlement_${key}_required`, 403)
lib\billing-identity.ts:3: import { getVerifiedTenantSession } from "@/lib/tenant-access"
lib\billing-identity.ts:14: const tenant = await getVerifiedTenantSession().catch(() => null)
lib\billing-identity.ts:15: if (tenant?.role === "owner") {
lib\billing-types.ts:7: "kitchen",
lib\billing-types.ts:35: kitchen: boolean
lib\cloudflare-saas.ts:93: Authorization: `Bearer ${apiToken}`,
lib\corporate-db.ts:7: export type CorporateGroupRole = "owner" | "admin" | "analyst"
lib\corporate-db.ts:34: role: CorporateGroupRole
lib\corporate-db.ts:50: role: string
lib\corporate-db.ts:56: role: CorporateGroupRole | null
lib\corporate-db.ts:59: permissions: {
lib\corporate-db.ts:79: operationalAccessRequiresTenantMembership: true
lib\corporate-db.ts:120: role: CorporateGroupRole
lib\corporate-db.ts:133: function canManage(role: CorporateGroupRole | null) {
lib\corporate-db.ts:134: return role === "owner" || role === "admin"
lib\corporate-db.ts:180: gm.role AS member_role
lib\corporate-db.ts:238: gm.role AS member_role
lib\corporate-db.ts:258: const role: CorporateGroupRole | null =
lib\corporate-db.ts:259: row.member_role || (row.owner_user_id === session.userId ? "owner" : null)
lib\corporate-db.ts:261: if (!canManage(role)) {
lib\corporate-db.ts:265: return { ...row, role }
lib\corporate-db.ts:278: role: session.role,
lib\corporate-db.ts:281: permissions: {
lib\corporate-db.ts:301: operationalAccessRequiresTenantMembership: true,
lib\corporate-db.ts:315: role: session.role,
lib\corporate-db.ts:318: permissions: {
lib\corporate-db.ts:338: operationalAccessRequiresTenantMembership: true,
lib\corporate-db.ts:347: group?.member_role || (group && isBillingOwner ? "owner" : null)
lib\corporate-db.ts:377: member.role AS tenant_role,
lib\corporate-db.ts:388: SELECT m.role
lib\corporate-db.ts:389: FROM sf_memberships m
lib\corporate-db.ts:394: ) member ON true
lib\corporate-db.ts:467: SELECT gm.id, gm.user_id, u.name, u.email, gm.role, gm.status, gm.created_at
lib\corporate-db.ts:472: CASE gm.role WHEN 'owner' THEN 1 WHEN 'admin' THEN 2 ELSE 3 END,
lib\corporate-db.ts:483: role: row.role,
lib\corporate-db.ts:507: role: session.role,
lib\corporate-db.ts:514: role: effectiveRole,
lib\corporate-db.ts:518: permissions: {
lib\corporate-db.ts:519: canCreateGroup: !group && isBillingOwner && session.role === "owner",
lib\corporate-db.ts:544: operationalAccessRequiresTenantMembership: true,
lib\corporate-db.ts:564: if (account.owner_user_id !== session.userId || session.role !== "owner") {
lib\corporate-db.ts:632: id, group_id, user_id, role, status, created_by_user_id
lib\corporate-db.ts:633: ) VALUES ($1, $2, $3, 'owner', 'active', $3)
lib\corporate-db.ts:824: input: { email: string; role: CorporateGroupRole },
lib\corporate-db.ts:829: if (!["owner", "admin", "analyst"].includes(input.role)) throw new Error("Papel corporativo inválido.")
lib\corporate-db.ts:835: if (input.role === "owner" && access.role !== "owner") {
lib\corporate-db.ts:836: throw new Error("Somente um proprietário corporativo pode conceder o papel owner.")
lib\corporate-db.ts:847: FROM sf_memberships m
lib\corporate-db.ts:865: id, group_id, user_id, role, status, created_by_user_id
lib\corporate-db.ts:868: DO UPDATE SET role = EXCLUDED.role, status = 'active', updated_at = now()
lib\corporate-db.ts:870: [randomUUID(), groupId, user.rows[0].id, input.role, session.userId],
lib\corporate-db.ts:873: await recordAudit(client, groupId, session.userId, "member.upserted", "user", user.rows[0].id, { email, role: input.role }, ipAddress)
lib\corporate-db.ts:896: role: CorporateGroupRole
lib\corporate-db.ts:899: `SELECT user_id, role, status FROM sf_corporate_group_members WHERE id = $1 AND group_id = $2 LIMIT 1`,
lib\corporate-db.ts:905: if (row.role === "owner" && access.role !== "owner") {
lib\corporate-db.ts:906: throw new Error("Somente um proprietário corporativo pode alterar outro owner.")
lib\corporate-db.ts:909: if (status === "disabled" && row.role === "owner") {
lib\corporate-db.ts:911: `SELECT COUNT(*)::text AS count FROM sf_corporate_group_members WHERE group_id = $1 AND role = 'owner' AND status = 'active' AND id <> $2`,
lib\corporate-db.ts:923: await recordAudit(client, groupId, session.userId, `member.${status}`, "user", row.user_id, { memberId, role: row.role }, ipAddress)
lib\corporate-db.ts:964: (currentGroup && account.owner_user_id === session.userId ? "owner" : null)
lib\corporate-db.ts:987: ? { id: accessibleGroup.id, name: accessibleGroup.name, role: effectiveRole }
lib\crm-db.ts:6: import { permissionListHas } from "@/lib/operational-permissions"
lib\crm-db.ts:85: return permissionListHas(session.operationalPermissions, "crm.manage")
lib\crm-db.ts:403: `admin:${session.userId}`,
lib\db.ts:5: Courier,
lib\db.ts:55: export function getCouriers(..._args: any[]): Promise<Courier[]> { return disabled("consulta de entregadores") }
lib\db.ts:56: export function createCourier(..._args: any[]): Promise<Courier> { return disabled("criação de entregador") }
lib\db.ts:57: export function updateCourier(..._args: any[]): Promise<Courier | null> { return disabled("atualização de entregador") }
lib\db.ts:58: export function deleteCourier(..._args: any[]): Promise<Courier | null> { return disabled("exclusão de entregador") }
lib\db.ts:99: couriers: Courier[]
lib\delivery-dispatch-db.ts:7: import type { OperationalPermission } from "@/lib/operational-permissions"
lib\delivery-dispatch-db.ts:8: import { permissionListHas } from "@/lib/operational-permissions"
lib\delivery-dispatch-db.ts:14: courier: Awaited<ReturnType<typeof getTenantCourierForUser>>
lib\delivery-dispatch-db.ts:21: role: OrganizationRole
lib\delivery-dispatch-db.ts:22: permissions: readonly OperationalPermission[]
lib\delivery-dispatch-db.ts:28: const selfMode = input.role === "courier"
lib\delivery-dispatch-db.ts:29: const courier = selfMode
lib\delivery-dispatch-db.ts:39: courier: null,
lib\delivery-dispatch-db.ts:40: orders: permissionListHas(input.permissions, "delivery.manage")
lib\delivery-dispatch-db.ts:46: if (!courier) {
lib\delivery-dispatch-db.ts:49: courier: null,
lib\delivery-dispatch-db.ts:56: courier,
lib\delivery-dispatch-db.ts:57: orders: deliveryOrders.filter((order) => order.courierId === courier.id),
lib\delivery-dispatch-db.ts:67: role: OrganizationRole
lib\delivery-dispatch-db.ts:70: if (input.role !== "courier") return null
lib\delivery-dispatch-db.ts:76: const courier = await getTenantCourierForUser(
lib\delivery-dispatch-db.ts:81: if (!courier) {
lib\delivery-dispatch-db.ts:91: if (!input.order.courierId || input.order.courierId !== courier.id) {
lib\delivery-dispatch-db.ts:97: return courier
lib\delivery-dispatch-db.ts:106: role: OrganizationRole
lib\delivery-dispatch-db.ts:164: JOIN sf_memberships m
lib\delivery-dispatch-db.ts:171: AND s.role = 'courier') AS linked_logins,
lib\delivery-dispatch-db.ts:189: const currentCourier = input.role === "courier"
lib\delivery-tracking-db.ts:9: | "waiting-courier"
lib\delivery-tracking-db.ts:134: const courier = await getTenantCourierForUser(
lib\delivery-tracking-db.ts:139: if (!courier || !courier.active) {
lib\delivery-tracking-db.ts:176: courier.id,
lib\delivery-tracking-db.ts:189: courierId: courier.id,
lib\delivery-tracking-db.ts:236: state: input.order.status === "ready" ? "waiting-courier" : "preparing",
lib\delivery-tracking-db.ts:281: const courier = result.rows[0]
lib\delivery-tracking-db.ts:282: if (!courier || !courier.active) {
lib\delivery-tracking-db.ts:285: state: "waiting-courier",
lib\delivery-tracking-db.ts:290: const activeOrderId = courier.active_order_id == null
lib\delivery-tracking-db.ts:292: : Number(courier.active_order_id)
lib\delivery-tracking-db.ts:298: courierName: courier.name,
lib\delivery-tracking-db.ts:308: courierName: courier.name,
lib\delivery-tracking-db.ts:317: courierName: courier.name,
lib\delivery-tracking-db.ts:322: const latitude = Number(courier.current_latitude)
lib\delivery-tracking-db.ts:323: const longitude = Number(courier.current_longitude)
lib\delivery-tracking-db.ts:324: const updatedAt = courier.location_updated_at
lib\delivery-tracking-db.ts:325: ? new Date(courier.location_updated_at)
lib\delivery-tracking-db.ts:341: courierName: courier.name,
lib\delivery-tracking-db.ts:349: courierName: courier.name,
lib\delivery-tracking-db.ts:354: accuracyMeters: courier.location_accuracy_meters == null
lib\delivery-tracking-db.ts:356: : Number(courier.location_accuracy_meters),
lib\demo-db.ts:223: organization_id, id, name, email, phone, role, active, permissions, created_at, updated_at
lib\demo-db.ts:225: ($1, 1, 'Lucas Cozinha', 'cozinha@example.invalid', '', 'kitchen', true, '[]'::jsonb, $2, $2),
lib\demo-db.ts:226: ($1, 2, 'Fernanda Caixa', 'caixa@example.invalid', '', 'cashier', true, '[]'::jsonb, $2, $2)
lib\demo-db.ts:253: { id: 1, status: 'pending', type: 'delivery', customer: { name: 'Mariana Demo', phone: '(11) 90000-0001', address: 'Rua das Flores, 10', district: 'Centro', city: 'São Paulo', state: 'SP', zipCode: '01000-001' }, courier: null, age: 5 },
lib\demo-db.ts:254: { id: 2, status: 'accepted', type: 'pickup', customer: { name: 'Carlos Demo', phone: '(11) 90000-0002' }, courier: null, age: 12 },
lib\demo-db.ts:255: { id: 3, status: 'preparing', type: 'delivery', customer: { name: 'Ana Demo', phone: '(11) 90000-0003', address: 'Rua Augusta, 250', district: 'Consolação', city: 'São Paulo', state: 'SP', zipCode: '01305-000' }, courier: null, age: 20 },
lib\demo-db.ts:256: { id: 4, status: 'ready', type: 'pickup', customer: { name: 'Mariana Demo', phone: '(11) 90000-0001' }, courier: null, age: 35 },
lib\demo-db.ts:257: { id: 5, status: 'in-route', type: 'delivery', customer: { name: 'Carlos Demo', phone: '(11) 90000-0002', address: 'Av. Paulista, 1000', district: 'Bela Vista', city: 'São Paulo', state: 'SP', zipCode: '01310-100' }, courier: { id: 1, name: 'João Entregador' }, age: 45 },
lib\demo-db.ts:258: { id: 6, status: 'completed', type: 'delivery', customer: { name: 'Ana Demo', phone: '(11) 90000-0003', address: 'Rua Augusta, 250', district: 'Consolação', city: 'São Paulo', state: 'SP', zipCode: '01305-000' }, courier: { id: 2, name: 'Paula Entregadora' }, age: 180 },
lib\demo-db.ts:293: order.courier?.id || null,
lib\demo-db.ts:294: order.courier?.name || null,
lib\demo-db.ts:410: role: "owner" as const,
lib\demo-db.ts:496: const membershipId = randomUUID()
lib\demo-db.ts:542: INSERT INTO sf_memberships (
lib\demo-db.ts:543: id, organization_id, user_id, role, status, accepted_at, created_at, updated_at
lib\demo-db.ts:544: ) VALUES ($1, $2, $3, 'owner', 'active', now(), now(), now())
lib\demo-db.ts:545: `, [membershipId, organizationId, adminUserId])
lib\demo-db.ts:610: role: "owner",
lib\demo-policy.ts:20: constructor(message: string, code = "demo_action_blocked", status = 403) {
lib\food-operations-db.ts:5: import { permissionListHas } from "@/lib/operational-permissions"
lib\food-operations-db.ts:82: return permissionListHas(session.operationalPermissions, "food_operations.manage")
lib\integration-providers.ts:74: authorization: `Bearer ${apiKey}`,
lib\integration-providers.ts:105: authorization: `Basic ${Buffer.from(`${accountSid}:${authToken}`).toString("base64")}`,
lib\integration-providers.ts:130: authorization: `Bearer ${accessToken}`,
lib\integrations-db.ts:17: import { permissionListHas } from "@/lib/operational-permissions"
lib\integrations-db.ts:57: return permissionListHas(session.operationalPermissions, "integrations.manage")
lib\integrations-request.ts:58: export function integrationWorkerRequestIsAuthorized(request: Request) {
lib\integrations-request.ts:61: const authorization = request.headers.get("authorization") || ""
lib\integrations-request.ts:62: const bearer = authorization.toLowerCase().startsWith("bearer ")
lib\integrations-request.ts:63: ? authorization.slice(7).trim()
lib\legal-documents.ts:13: | "admin-legal-gate"
lib\marketing-content.ts:29: icon: "kitchen",
lib\operational-home.ts:3: permissionListHas,
lib\operational-home.ts:4: type OperationalPermission,
lib\operational-home.ts:5: } from "@/lib/operational-permissions"
lib\operational-home.ts:8: | "manager"
lib\operational-home.ts:10: | "kitchen"
lib\operational-home.ts:11: | "courier"
lib\operational-home.ts:14: | "/admin"
lib\operational-home.ts:21: permissions: readonly OperationalPermission[],
lib\operational-home.ts:22: permission: OperationalPermission,
lib\operational-home.ts:24: return permissionListHas(permissions, permission)
lib\operational-home.ts:28: role: OrganizationRole,
lib\operational-home.ts:29: permissions: readonly OperationalPermission[],
lib\operational-home.ts:31: if (role === "owner" || role === "admin") return "/admin"
lib\operational-home.ts:33: if (role === "manager") return "/gerente"
lib\operational-home.ts:35: if (role === "cashier" && has(permissions, "pdv.use")) {
lib\operational-home.ts:39: if (role === "kitchen" && has(permissions, "kitchen.use")) {
lib\operational-home.ts:44: role === "courier" &&
lib\operational-home.ts:45: has(permissions, "orders.view") &&
lib\operational-home.ts:46: has(permissions, "orders.status.update")
lib\operational-home.ts:51: return "/admin"
lib\operational-home.ts:56: role: OrganizationRole,
lib\operational-home.ts:57: permissions: readonly OperationalPermission[],
lib\operational-home.ts:59: if (workspace === "manager") {
lib\operational-home.ts:60: return role === "owner" || role === "admin" || role === "manager"
lib\operational-home.ts:64: return has(permissions, "pdv.use")
lib\operational-home.ts:67: if (workspace === "kitchen") {
lib\operational-home.ts:68: return has(permissions, "kitchen.use")
lib\operational-home.ts:72: has(permissions, "delivery.manage") ||
lib\operational-home.ts:73: (role === "courier" &&
lib\operational-home.ts:74: has(permissions, "orders.view") &&
lib\operational-home.ts:75: has(permissions, "orders.status.update"))
lib\operational-permissions.ts:3: export const CUSTOM_PERMISSION_MARKER = "__saborflow_custom_permissions_v1__"
lib\operational-permissions.ts:5: export const OPERATIONAL_PERMISSION_CATALOG = [
lib\operational-permissions.ts:12: { id: "kitchen.use", group: "Cozinha", label: "Usar painel da cozinha" },
lib\operational-permissions.ts:36: export type OperationalPermission =
lib\operational-permissions.ts:37: (typeof OPERATIONAL_PERMISSION_CATALOG)[number]["id"]
lib\operational-permissions.ts:39: const ALL_PERMISSIONS = OPERATIONAL_PERMISSION_CATALOG.map(
lib\operational-permissions.ts:41: ) as OperationalPermission[]
lib\operational-permissions.ts:43: const managerDenied = new Set<OperationalPermission>([
lib\operational-permissions.ts:49: export const ROLE_PERMISSION_PRESETS: Record<
lib\operational-permissions.ts:51: OperationalPermission[]
lib\operational-permissions.ts:53: owner: [...ALL_PERMISSIONS],
lib\operational-permissions.ts:54: admin: ALL_PERMISSIONS.filter((permission) => permission !== "billing.view"),
lib\operational-permissions.ts:55: manager: ALL_PERMISSIONS.filter(
lib\operational-permissions.ts:56: (permission) => !managerDenied.has(permission),
lib\operational-permissions.ts:58: cashier: [
lib\operational-permissions.ts:69: kitchen: [
lib\operational-permissions.ts:72: "kitchen.use",
lib\operational-permissions.ts:75: courier: [
lib\operational-permissions.ts:80: member: [
lib\operational-permissions.ts:86: const validPermissionIds = new Set<string>(ALL_PERMISSIONS)
lib\operational-permissions.ts:88: export function sanitizeOperationalPermissions(
lib\operational-permissions.ts:90: ): OperationalPermission[] {
lib\operational-permissions.ts:96: .filter((value) => validPermissionIds.has(value)),
lib\operational-permissions.ts:97: )] as OperationalPermission[]
lib\operational-permissions.ts:100: export function getRolePermissionPreset(
lib\operational-permissions.ts:101: role: OrganizationRole,
lib\operational-permissions.ts:102: ): OperationalPermission[] {
lib\operational-permissions.ts:103: return [...ROLE_PERMISSION_PRESETS[role]]
lib\operational-permissions.ts:106: export function roleHasOperationalPermission(
lib\operational-permissions.ts:107: role: OrganizationRole,
lib\operational-permissions.ts:108: permission: OperationalPermission,
lib\operational-permissions.ts:110: return ROLE_PERMISSION_PRESETS[role].includes(permission)
lib\operational-permissions.ts:113: export function permissionListHas(
lib\operational-permissions.ts:114: permissions: readonly string[] | null | undefined,
lib\operational-permissions.ts:115: permission: OperationalPermission,
lib\operational-permissions.ts:117: return Boolean(permissions?.includes(permission))
lib\operational-permissions.ts:120: export function storedPermissionsAreCustom(
lib\operational-permissions.ts:123: return Boolean(stored?.includes(CUSTOM_PERMISSION_MARKER))
lib\operational-permissions.ts:126: export function getCustomPermissionsFromStorage(
lib\operational-permissions.ts:129: if (!storedPermissionsAreCustom(stored)) return []
lib\operational-permissions.ts:130: return sanitizeOperationalPermissions(stored)
lib\operational-permissions.ts:133: export function toCustomPermissionStorage(
lib\operational-permissions.ts:134: permissions: readonly string[],
lib\operational-permissions.ts:137: CUSTOM_PERMISSION_MARKER,
lib\operational-permissions.ts:138: ...sanitizeOperationalPermissions(permissions),
lib\operational-rbac.ts:6: getCustomPermissionsFromStorage,
lib\operational-rbac.ts:7: getRolePermissionPreset,
lib\operational-rbac.ts:8: permissionListHas,
lib\operational-rbac.ts:9: storedPermissionsAreCustom,
lib\operational-rbac.ts:10: type OperationalPermission,
lib\operational-rbac.ts:11: } from "@/lib/operational-permissions"
lib\operational-rbac.ts:13: export type OperationalPermissionMode = "role" | "custom"
lib\operational-rbac.ts:18: role: OrganizationRole
lib\operational-rbac.ts:19: mode: OperationalPermissionMode
lib\operational-rbac.ts:21: permissions: OperationalPermission[]
lib\operational-rbac.ts:24: type StaffPermissionRow = {
lib\operational-rbac.ts:26: permissions: string[]
lib\operational-rbac.ts:56: export function currentOrRoleHasOperationalPermission(
lib\operational-rbac.ts:57: role: OrganizationRole,
lib\operational-rbac.ts:58: permission: OperationalPermission,
lib\operational-rbac.ts:63: return permissionListHas(current.permissions, permission)
lib\operational-rbac.ts:66: return getRolePermissionPreset(role).includes(permission)
lib\operational-rbac.ts:72: role: OrganizationRole
lib\operational-rbac.ts:74: if (input.role === "owner") {
lib\operational-rbac.ts:78: role: input.role,
lib\operational-rbac.ts:79: mode: "role",
lib\operational-rbac.ts:81: permissions: getRolePermissionPreset(input.role),
lib\operational-rbac.ts:89: getPostgresPool().query<StaffPermissionRow>(
lib\operational-rbac.ts:91: SELECT id, permissions, active
lib\operational-rbac.ts:105: storedPermissionsAreCustom(staff?.permissions)
lib\operational-rbac.ts:110: role: input.role,
lib\operational-rbac.ts:111: mode: custom ? "custom" : "role",
lib\operational-rbac.ts:113: permissions: custom
lib\operational-rbac.ts:114: ? getCustomPermissionsFromStorage(staff.permissions)
lib\operational-rbac.ts:115: : getRolePermissionPreset(input.role),
lib\operational-rbac.ts:120: access: Pick<OperationalAccessContext, "permissions">,
lib\operational-rbac.ts:121: permission: OperationalPermission,
lib\operational-rbac.ts:123: return permissionListHas(access.permissions, permission)
lib\operations-db.ts:17: Courier,
lib\operations-db.ts:195: function mapCourier(row: CourierRow): Courier {
lib\operations-db.ts:1296: LEFT JOIN sf_memberships m
lib\operations-db.ts:1331: LEFT JOIN sf_memberships m
lib\operations-db.ts:1371: AND s.role = 'courier'
lib\operations-db.ts:1399: AND role = 'courier'
lib\operations-db.ts:1415: input: Pick<Courier, "name" | "phone" | "vehicle"> & {
lib\operations-db.ts:1497: Pick<Courier, "name" | "phone" | "vehicle" | "active">
lib\order-db.ts:545: if (["tenant-checkout", "tenant-pdv", "postgres-admin"].includes(previous.rows[0].source)) {
lib\order-db.ts:653: return upsertTenantOrder(organizationId, next, "postgres-admin")
lib\organization-db.ts:25: role: StaffRole
lib\organization-db.ts:27: permissions: string[]
lib\organization-db.ts:47: role: row.role,
lib\organization-db.ts:49: permissions: Array.isArray(row.permissions)
lib\organization-db.ts:50: ? row.permissions.map(String)
lib\organization-db.ts:454: role,
lib\organization-db.ts:456: permissions,
lib\organization-db.ts:493: "name" | "email" | "phone" | "role" | "permissions"
lib\organization-db.ts:545: role,
lib\organization-db.ts:547: permissions,
lib\organization-db.ts:563: role,
lib\organization-db.ts:565: permissions,
lib\organization-db.ts:578: input.role,
lib\organization-db.ts:579: JSON.stringify(input.permissions || []),
lib\organization-db.ts:615: | "role"
lib\organization-db.ts:617: | "permissions"
lib\organization-db.ts:658: permissions:
lib\organization-db.ts:659: patch.permissions !== undefined
lib\organization-db.ts:660: ? patch.permissions.map(String)
lib\organization-db.ts:661: : current.permissions,
lib\organization-db.ts:686: role = $6,
lib\organization-db.ts:688: permissions = $8::jsonb,
lib\organization-db.ts:700: role,
lib\organization-db.ts:702: permissions,
lib\organization-db.ts:715: next.role,
lib\organization-db.ts:717: JSON.stringify(next.permissions),
lib\organization-db.ts:726: patch.role !== undefined
lib\organization-db.ts:730: UPDATE sf_memberships
lib\organization-db.ts:732: role = $3,
lib\organization-db.ts:743: AND role <> 'owner'
lib\organization-db.ts:745: [organizationId, id, next.role],
lib\organization-db.ts:755: UPDATE sf_memberships
lib\organization-db.ts:768: AND role <> 'owner'
lib\organization-db.ts:814: const membership = await client.query<{
lib\organization-db.ts:815: role: string
lib\organization-db.ts:818: SELECT role
lib\organization-db.ts:819: FROM sf_memberships
lib\organization-db.ts:827: if (membership.rows[0]?.role === "owner") {
lib\organization-db.ts:835: UPDATE sf_memberships
lib\organization-db.ts:841: AND role <> 'owner'
lib\organization-db.ts:1052: "www,painel,media,api,admin")
lib\organization-onboarding.ts:350: INSERT INTO sf_memberships (
lib\organization-onboarding.ts:354: role,
lib\organization-onboarding.ts:358: $1, $2, $3, 'owner', 'active'
lib\organization-onboarding.ts:581: role: "owner",
lib\postgres.ts:96: ? `SET LOCAL ROLE ${RLS_RUNTIME_ROLE}`
lib\postgres.ts:97: : `SET ROLE ${RLS_RUNTIME_ROLE}`,
lib\postgres.ts:129: await client.query("RESET ROLE").catch(() => undefined)
lib\postgres.ts:234: role: RLS_RUNTIME_ROLE,
lib\print-order.ts:7: export function orderTicketHtml(order: Order, settings?: Pick<StoreSettings, "storeName" | "address">, mode: "kitchen" | "customer" = "customer") {
lib\print-order.ts:16: export function printOrder(order: Order, settings?: Pick<StoreSettings, "storeName" | "address">, mode: "kitchen" | "customer" = "customer") {
lib\reports-db.ts:6: import { permissionListHas } from "@/lib/operational-permissions"
lib\reports-db.ts:21: return permissionListHas(session.operationalPermissions, "reports.view")
lib\reports-db.ts:140: const groupAvailable = Boolean(corporate.group && corporate.group.role && corporate.organizations.some((item) => item.inGroup))
lib\reports-db.ts:522: groupAvailable: Boolean(corporate?.group && corporate.group.role && corporate.organizations.some((item) => item.inGroup)),
lib\superadmin-auth.ts:2: getAdminSession,
lib\superadmin-auth.ts:6: import { validateAndTouchAdminSession } from "@/lib/security/admin-sessions"
lib\superadmin-auth.ts:9: | "owner"
lib\superadmin-auth.ts:18: role: PlatformAdminRole
lib\superadmin-auth.ts:75: await getAdminSession()
lib\superadmin-auth.ts:125: role: PlatformAdminRole
lib\superadmin-auth.ts:132: pa.role
lib\superadmin-auth.ts:152: role: row.role,
lib\superadmin-auth.ts:167: role: PlatformAdminRole,
lib\superadmin-auth.ts:170: role === "owner" ||
lib\superadmin-auth.ts:171: role === "operator" ||
lib\superadmin-auth.ts:172: role === "finance"
lib\superadmin-auth.ts:177: role: PlatformAdminRole,
lib\superadmin-auth.ts:180: role === "owner" ||
lib\superadmin-auth.ts:181: role === "operator" ||
lib\superadmin-auth.ts:182: role === "support"
lib\superadmin-auth.ts:187: role: PlatformAdminRole,
lib\superadmin-auth.ts:189: return role === "owner"
lib\superadmin-auth.ts:193: role: PlatformAdminRole,
lib\superadmin-auth.ts:196: role === "owner" ||
lib\superadmin-auth.ts:197: role === "operator"
lib\superadmin-auth.ts:202: role: PlatformAdminRole,
lib\superadmin-auth.ts:205: role === "owner" ||
lib\superadmin-auth.ts:206: role === "operator" ||
lib\superadmin-auth.ts:207: role === "finance"
lib\team-access-db.ts:5: } from "@/lib/admin-user-db"
lib\team-access-db.ts:24: role: StaffRole
lib\team-access-db.ts:32: membershipStatus:
lib\team-access-db.ts:44: role: StaffRole
lib\team-access-db.ts:52: membership_status:
lib\team-access-db.ts:68: role: row.role,
lib\team-access-db.ts:73: membershipStatus:
lib\team-access-db.ts:74: row.membership_status,
lib\team-access-db.ts:90: s.role,
lib\team-access-db.ts:94: m.status AS membership_status,
lib\team-access-db.ts:99: LEFT JOIN sf_memberships m
lib\team-access-db.ts:142: role: StaffRole
lib\team-access-db.ts:154: s.role,
lib\team-access-db.ts:282: const membership =
lib\team-access-db.ts:292: FROM sf_memberships
lib\team-access-db.ts:304: membership.rows[0]?.status ===
lib\team-access-db.ts:308: if (membership.rows[0]) {
lib\team-access-db.ts:311: UPDATE sf_memberships
lib\team-access-db.ts:313: role = $3,
lib\team-access-db.ts:325: staff.role,
lib\team-access-db.ts:332: INSERT INTO sf_memberships (
lib\team-access-db.ts:336: role,
lib\team-access-db.ts:359: staff.role,
lib\team-access-db.ts:461: membership_status:
lib\team-access-db.ts:465: role: OrganizationRole
lib\team-access-db.ts:475: m.status AS membership_status,
lib\team-access-db.ts:476: m.role,
lib\team-access-db.ts:480: INNER JOIN sf_memberships m
lib\team-access-db.ts:502: row.membership_status !==
lib\team-access-db.ts:515: membershipStatus:
lib\team-access-db.ts:516: row.membership_status,
lib\team-access-db.ts:517: role: row.role,
lib\team-access-db.ts:536: role: OrganizationRole
lib\team-access-db.ts:541: if (input.role !== "courier") return
lib\team-access-db.ts:624: [`saborflow-courier-id:${input.organizationId}`],
lib\team-access-db.ts:754: const membership =
lib\team-access-db.ts:757: UPDATE sf_memberships
lib\team-access-db.ts:773: if (!membership.rowCount) {
lib\team-access-db.ts:789: RETURNING id, role, name, phone
lib\team-access-db.ts:805: (row) => row.role === preview.role,
lib\team-access-db.ts:811: role: preview.role,
lib\team-access-db.ts:844: const membership =
lib\team-access-db.ts:846: role: OrganizationRole
lib\team-access-db.ts:849: SELECT role
lib\team-access-db.ts:850: FROM sf_memberships
lib\team-access-db.ts:858: if (!membership.rows[0]) {
lib\team-access-db.ts:865: membership.rows[0].role ===
lib\team-access-db.ts:866: "owner"
lib\team-access-db.ts:875: UPDATE sf_memberships
lib\team-access-db.ts:928: role: OrganizationRole
lib\team-access-db.ts:937: m.role,
lib\team-access-db.ts:939: FROM sf_memberships m
lib\team-access-db.ts:964: target.role === "owner" &&
lib\team-access-db.ts:965: input.actorRole !== "owner"
lib\tenant-access.ts:2: import { getAdminSession } from "@/lib/auth"
lib\tenant-access.ts:5: currentOrRoleHasOperationalPermission,
lib\tenant-access.ts:8: type OperationalPermissionMode,
lib\tenant-access.ts:10: import type { OperationalPermission } from "@/lib/operational-permissions"
lib\tenant-access.ts:16: import { validateAndTouchAdminSession } from "@/lib/security/admin-sessions"
lib\tenant-access.ts:22: operationalPermissions: OperationalPermission[]
lib\tenant-access.ts:23: operationalPermissionMode: OperationalPermissionMode
lib\tenant-access.ts:27: export async function getVerifiedTenantSession():
lib\tenant-access.ts:29: const session = await getAdminSession()
lib\tenant-access.ts:104: operationalPermissions: access.permissions,
lib\tenant-access.ts:105: operationalPermissionMode: access.mode,
lib\tenant-access.ts:111: role: OrganizationRole,
lib\tenant-access.ts:113: return currentOrRoleHasOperationalPermission(role, "catalog.manage")
lib\tenant-admin-data.ts:10: getRolePermissionPreset,
lib\tenant-admin-data.ts:11: permissionListHas,
lib\tenant-admin-data.ts:12: type OperationalPermission,
lib\tenant-admin-data.ts:13: } from "@/lib/operational-permissions"
lib\tenant-admin-data.ts:22: } from "@/lib/admin-access"
lib\tenant-admin-data.ts:28: import { membershipExists } from "@/lib/tenant-context"
lib\tenant-admin-data.ts:130: role: OrganizationRole,
lib\tenant-admin-data.ts:131: permissions: readonly OperationalPermission[],
lib\tenant-admin-data.ts:133: const canOrders = canReadOrders(role, permissions)
lib\tenant-admin-data.ts:134: const canCatalog = canReadCatalog(role, permissions)
lib\tenant-admin-data.ts:135: const canCustomers = canReadCustomers(role, permissions)
lib\tenant-admin-data.ts:136: const canFinance = canReadFinance(role, permissions)
lib\tenant-admin-data.ts:137: const canMarketing = canReadMarketing(role, permissions)
lib\tenant-admin-data.ts:138: const canTeam = canReadTeam(role, permissions)
lib\tenant-admin-data.ts:139: const canPdv = permissionListHas(permissions, "pdv.use")
lib\tenant-admin-data.ts:140: const canCash = permissionListHas(permissions, "cash.manage")
lib\tenant-admin-data.ts:163: operationalPermissions?: readonly OperationalPermission[],
lib\tenant-admin-data.ts:175: const activeMembership = await membershipExists(
lib\tenant-admin-data.ts:180: if (!activeMembership) {
lib\tenant-admin-data.ts:186: session.role,
lib\tenant-admin-data.ts:187: operationalPermissions ?? getRolePermissionPreset(session.role),
lib\tenant-context.ts:5: | "owner"
lib\tenant-context.ts:6: | "admin"
lib\tenant-context.ts:7: | "manager"
lib\tenant-context.ts:8: | "cashier"
lib\tenant-context.ts:9: | "kitchen"
lib\tenant-context.ts:10: | "courier"
lib\tenant-context.ts:11: | "member"
lib\tenant-context.ts:19: role: OrganizationRole
lib\tenant-context.ts:23: export interface OrganizationMembershipSummary
lib\tenant-context.ts:39: owner: 1,
lib\tenant-context.ts:40: admin: 2,
lib\tenant-context.ts:41: manager: 3,
lib\tenant-context.ts:42: cashier: 4,
lib\tenant-context.ts:43: kitchen: 5,
lib\tenant-context.ts:44: courier: 6,
lib\tenant-context.ts:45: member: 7,
lib\tenant-context.ts:48: type OrganizationMembershipRow = {
lib\tenant-context.ts:64: role: OrganizationRole
lib\tenant-context.ts:69: row: OrganizationMembershipRow,
lib\tenant-context.ts:70: ): OrganizationMembershipSummary {
lib\tenant-context.ts:90: role: row.role,
lib\tenant-context.ts:109: m.role,
lib\tenant-context.ts:112: INNER JOIN sf_memberships m
lib\tenant-context.ts:125: values: OrganizationMembershipSummary[],
lib\tenant-context.ts:129: rolePriority[a.role] -
lib\tenant-context.ts:130: rolePriority[b.role] ||
lib\tenant-context.ts:138: export async function listOrganizationMembershipsForUser(
lib\tenant-context.ts:140: ): Promise<OrganizationMembershipSummary[]> {
lib\tenant-context.ts:143: await getPostgresPool().query<OrganizationMembershipRow>(
lib\tenant-context.ts:156: export async function listOrganizationMembershipsForUserId(
lib\tenant-context.ts:158: ): Promise<OrganizationMembershipSummary[]> {
lib\tenant-context.ts:161: await getPostgresPool().query<OrganizationMembershipRow>(
lib\tenant-context.ts:176: await listOrganizationMembershipsForUser(
lib\tenant-context.ts:195: await listOrganizationMembershipsForUserId(
lib\tenant-context.ts:236: await getPostgresPool().query<OrganizationMembershipRow>(
lib\tenant-context.ts:260: export async function membershipExists(
lib\tenant-permissions.ts:3: currentOrRoleHasOperationalPermission,
lib\tenant-permissions.ts:6: export function canManageMarketing(role: OrganizationRole) {
lib\tenant-permissions.ts:7: return currentOrRoleHasOperationalPermission(role, "marketing.manage")
lib\tenant-permissions.ts:10: export function canViewFeedback(role: OrganizationRole) {
lib\tenant-permissions.ts:11: return currentOrRoleHasOperationalPermission(role, "marketing.view")
lib\tenant-permissions.ts:14: export function canUseCashRegister(role: OrganizationRole) {
lib\tenant-permissions.ts:15: return currentOrRoleHasOperationalPermission(role, "cash.manage")
lib\tenant-permissions.ts:18: export function canViewFinance(role: OrganizationRole) {
lib\tenant-permissions.ts:19: return currentOrRoleHasOperationalPermission(role, "finance.view")
lib\tenant-permissions.ts:22: export function canManageFinance(role: OrganizationRole) {
lib\tenant-permissions.ts:23: return currentOrRoleHasOperationalPermission(role, "finance.manage")
lib\tenant-permissions.ts:26: export function canManageDeliveryOperation(role: OrganizationRole) {
lib\tenant-permissions.ts:27: return currentOrRoleHasOperationalPermission(role, "delivery.manage")
lib\tenant-permissions.ts:30: export function canViewOrganizationSettings(role: OrganizationRole) {
lib\tenant-permissions.ts:31: return currentOrRoleHasOperationalPermission(role, "settings.view")
lib\tenant-permissions.ts:34: export function canManageOrganizationSettings(role: OrganizationRole) {
lib\tenant-permissions.ts:35: return currentOrRoleHasOperationalPermission(role, "settings.manage")
lib\tenant-permissions.ts:38: export function canViewTeam(role: OrganizationRole) {
lib\tenant-permissions.ts:39: return currentOrRoleHasOperationalPermission(role, "team.view")
lib\tenant-permissions.ts:42: export function canManageTeam(role: OrganizationRole) {
lib\tenant-permissions.ts:43: return currentOrRoleHasOperationalPermission(role, "team.manage")
lib\tenant-permissions.ts:46: export function canManageAccess(role: OrganizationRole) {
lib\tenant-permissions.ts:47: return currentOrRoleHasOperationalPermission(role, "access.manage")
lib\tenant-permissions.ts:50: export function canViewCustomers(role: OrganizationRole) {
lib\tenant-permissions.ts:51: return currentOrRoleHasOperationalPermission(role, "customers.view")
lib\tenant-permissions.ts:54: export function canManageCustomers(role: OrganizationRole) {
lib\tenant-permissions.ts:55: return currentOrRoleHasOperationalPermission(role, "customers.manage")
lib\tenant-permissions.ts:58: export function canViewSecurity(role: OrganizationRole) {
lib\tenant-permissions.ts:59: return currentOrRoleHasOperationalPermission(role, "security.view")
lib\types.ts:15: export type StaffRole = "admin" | "manager" | "cashier" | "kitchen" | "courier"
lib\types.ts:171: export interface Courier {
lib\types.ts:300: role: StaffRole
lib\types.ts:302: permissions: string[]
lib\types.ts:413: couriers: Courier[]
lib\types.ts:426: courier: number

==========================================================================================
ROTAS API
==========================================================================================
app\api\address\cep\route.ts
app\api\admin\2fa\reconfigure\route.ts
app\api\admin\2fa\reconfigure\qr\route.ts
app\api\admin\2fa\recovery-codes\route.ts
app\api\admin\2fa\security\route.ts
app\api\admin\after-hours-scheduling-health\route.ts
app\api\admin\billing\route.ts
app\api\admin\billing-health\route.ts
app\api\admin\catalog-health\route.ts
app\api\admin\commercial-onboarding\route.ts
app\api\admin\corporate-group\route.ts
app\api\admin\corporate-health\route.ts
app\api\admin\crm\route.ts
app\api\admin\crm\actions\route.ts
app\api\admin\crm-health\route.ts
app\api\admin\customers\route.ts
app\api\admin\customers-health\route.ts
app\api\admin\database-health\route.ts
app\api\admin\database-structure\route.ts
app\api\admin\delivery-dispatch-health\route.ts
app\api\admin\delivery-tracking-health\route.ts
app\api\admin\delivery-zones-health\route.ts
app\api\admin\demo-health\route.ts
app\api\admin\domains\route.ts
app\api\admin\domains\verify\route.ts
app\api\admin\dre\route.ts
app\api\admin\dre-health\route.ts
app\api\admin\food-composition-health\route.ts
app\api\admin\food-operations\route.ts
app\api\admin\food-operations\actions\route.ts
app\api\admin\food-operations-health\route.ts
app\api\admin\google-link\route.ts
app\api\admin\ingredients\route.ts
app\api\admin\ingredients\[id]\route.ts
app\api\admin\ingredients\[id]\movement\route.ts
app\api\admin\integrations\route.ts
app\api\admin\integrations\actions\route.ts
app\api\admin\integrations-health\route.ts
app\api\admin\inventory-movements\route.ts
app\api\admin\legacy-health\route.ts
app\api\admin\multiempresa-health\route.ts
app\api\admin\onboarding-health\route.ts
app\api\admin\operations-health\route.ts
app\api\admin\order-flow-health\route.ts
app\api\admin\orders-health\route.ts
app\api\admin\organization-security\route.ts
app\api\admin\organizations\route.ts
app\api\admin\organizations\current\ordering\route.ts
app\api\admin\passkeys\route.ts
app\api\admin\passkeys\register\options\route.ts
app\api\admin\passkeys\register\verify\route.ts
app\api\admin\password\change\route.ts
app\api\admin\pdv-customers\route.ts
app\api\admin\pdv-delivery-quote\route.ts
app\api\admin\pdv-order\route.ts
app\api\admin\phase25-6-health\route.ts
app\api\admin\print-agents\route.ts
app\api\admin\public-links-health\route.ts
app\api\admin\rbac-health\route.ts
app\api\admin\reports\route.ts
app\api\admin\reports\export\route.ts
app\api\admin\reports-health\route.ts
app\api\admin\rls-health\route.ts
app\api\admin\security-health\route.ts
app\api\admin\sessions\route.ts
app\api\admin\staff-profile-health\route.ts
app\api\admin\storage\route.ts
app\api\admin\switch-organization\route.ts
app\api\admin\team\access\route.ts
app\api\admin\tenant-context\route.ts
app\api\admin\tenant-runtime-health\route.ts
app\api\admin\workspaces-health\route.ts
app\api\auth\2fa\qr\route.ts
app\api\auth\2fa\setup\route.ts
app\api\auth\2fa\verify\route.ts
app\api\auth\google\route.ts
app\api\auth\login\route.ts
app\api\auth\logout\route.ts
app\api\auth\passkey\options\route.ts
app\api\auth\passkey\verify\route.ts
app\api\auth\password-reset\request\route.ts
app\api\billing\checkout\route.ts
app\api\billing\google\route.ts
app\api\billing\plans\route.ts
app\api\billing\sign-in\route.ts
app\api\billing\signup\route.ts
app\api\billing\status\route.ts
app\api\billing\webhooks\mercado-pago\route.ts
app\api\cash\route.ts
app\api\categories\route.ts
app\api\categories\[id]\route.ts
app\api\client\login\route.ts
app\api\client\logout\route.ts
app\api\client\me\route.ts
app\api\client\orders\route.ts
app\api\client\profile\route.ts
app\api\client\register\route.ts
app\api\coupons\route.ts
app\api\coupons\[id]\route.ts
app\api\courier\location\route.ts
app\api\couriers\route.ts
app\api\couriers\[id]\route.ts
app\api\dashboard\route.ts
app\api\dashboard\live\route.ts
app\api\delivery\quote\route.ts
app\api\delivery-zones\route.ts
app\api\delivery-zones\[id]\route.ts
app\api\demo\public\start\route.ts
app\api\demo\trial\start\route.ts
app\api\feedback\route.ts
app\api\financial\route.ts
app\api\health\route.ts
app\api\integrations\webhooks\[connectionId]\route.ts
app\api\internal\integrations\process\route.ts
app\api\invitations\[token]\route.ts
app\api\legal\accept\route.ts
app\api\media\[name]\route.ts
app\api\order-status\[reference]\route.ts
app\api\orders\route.ts
app\api\orders\accept-pending\route.ts
app\api\orders\[id]\route.ts
app\api\orders\[id]\ticket-pdf\route.ts
app\api\password-reset\[token]\route.ts
app\api\print-queue\route.ts
app\api\products\route.ts
app\api\products\[id]\route.ts
app\api\products\[id]\composition\route.ts
app\api\public\tenant\route.ts
app\api\ready\route.ts
app\api\settings\route.ts
app\api\staff\route.ts
app\api\staff\[id]\route.ts
app\api\store\route.ts
app\api\superadmin\actions\route.ts
app\api\superadmin\finance\route.ts
app\api\superadmin\health\route.ts
app\api\superadmin\overview\route.ts
app\api\uploads\product-image\route.ts
app\api\uploads\store-image\route.ts

==========================================================================================
ARQUIVO: lib\tenant-access.ts
==========================================================================================
import type { AdminSession } from "@/lib/auth"
import { getAdminSession } from "@/lib/auth"
import { demoOrganizationIsUsable, touchDemoEnvironment } from "@/lib/demo-policy"
import {
  currentOrRoleHasOperationalPermission,
  enterOperationalAccessContext,
  getOperationalAccessForSession,
  type OperationalPermissionMode,
} from "@/lib/operational-rbac"
import type { OperationalPermission } from "@/lib/operational-permissions"
import {
  getOrganizationContextForUser,
  type OrganizationRole,
} from "@/lib/tenant-context"
import { enterTenantRlsContext } from "@/lib/rls-context"
import { validateAndTouchAdminSession } from "@/lib/security/admin-sessions"

export type TenantAdminSession = Extract<
  AdminSession,
  { mode: "tenant" }
> & {
  operationalPermissions: OperationalPermission[]
  operationalPermissionMode: OperationalPermissionMode
  staffMemberId: number | null
}

export async function getVerifiedTenantSession():
  Promise<TenantAdminSession | null> {
  const session = await getAdminSession()

  if (
    !session ||
    session.mode !== "tenant"
  ) {
    return null
  }

  if (!(await demoOrganizationIsUsable(session.organizationId))) {
    return null
  }

  const current =
    await getOrganizationContextForUser(
      session.userId,
      session.organizationId,
    )

  if (!current) return null

  if (
    current.sessionVersion !==
    session.sessionVersion
  ) {
    return null
  }

  const persistentSessionValid =
    await validateAndTouchAdminSession({
      sessionId:
        session.sessionId,
      userId:
        session.userId,
      organizationId:
        session.organizationId,
      sessionVersion:
        session.sessionVersion,
    })

  if (!persistentSessionValid) {
    return null
  }

  await touchDemoEnvironment(session.organizationId)

  // Reafirma o tenant apÃ³s verificaÃ§Ãµes que usam AsyncLocalStorage.run().
  // A Fase 25-R1 depende deste limite para manter o RLS efetivo nas consultas
  // administrativas e a Fase 25.1 reutiliza o mesmo escopo para resolver RBAC.
  enterTenantRlsContext(
    session.organizationId,
    session.userId,
    "tenant-session",
  )

  const access = await getOperationalAccessForSession(current)
  enterOperationalAccessContext(access)

  // A resoluÃ§Ã£o do RBAC tambÃ©m abre um escopo RLS explÃ­cito; reafirmamos o
  // tenant no retorno para que as consultas seguintes continuem fail-closed
  // para qualquer outra organizaÃ§Ã£o.
  enterTenantRlsContext(
    session.organizationId,
    session.userId,
    "tenant-session",
  )

  return {
    mode: "tenant",
    ...current,
    sessionId:
      session.sessionId,
    issuedAt:
      session.issuedAt,
    expiresAt: session.expiresAt,
    operationalPermissions: access.permissions,
    operationalPermissionMode: access.mode,
    staffMemberId: access.staffMemberId,
  }
}

export function canManageCatalog(
  role: OrganizationRole,
) {
  return currentOrRoleHasOperationalPermission(role, "catalog.manage")
}

==========================================================================================
ARQUIVO: lib\team-access-db.ts
==========================================================================================
import { randomUUID } from "node:crypto"
import type { PoolClient } from "pg"
import {
  hashAdminPassword,
} from "@/lib/admin-user-db"
import { getPostgresPool } from "@/lib/postgres"
import {
  createAuthToken,
  getValidAuthToken,
  revokeOutstandingAuthTokens,
} from "@/lib/security-tokens"
import type {
  OrganizationRole,
} from "@/lib/tenant-context"
import type {
  StaffRole,
} from "@/lib/types"
import { runWithRlsBypass } from "@/lib/rls-context"

export type TeamAccessStatus = {
  staffMemberId: number
  name: string
  email: string
  role: StaffRole
  staffActive: boolean
  userId: string | null
  userStatus:
    | "active"
    | "blocked"
    | "pending"
    | null
  membershipStatus:
    | "active"
    | "invited"
    | "disabled"
    | null
  passwordReady: boolean
}

type TeamAccessRow = {
  staff_member_id: number
  name: string
  email: string
  role: StaffRole
  staff_active: boolean
  user_id: string | null
  user_status:
    | "active"
    | "blocked"
    | "pending"
    | null
  membership_status:
    | "active"
    | "invited"
    | "disabled"
    | null
  password_ready: boolean
}

function mapAccess(
  row: TeamAccessRow,
): TeamAccessStatus {
  return {
    staffMemberId:
      Number(row.staff_member_id),
    name: row.name,
    email: row.email || "",
    role: row.role,
    staffActive:
      Boolean(row.staff_active),
    userId: row.user_id,
    userStatus: row.user_status,
    membershipStatus:
      row.membership_status,
    passwordReady:
      Boolean(row.password_ready),
  }
}

export async function listTeamAccess(
  organizationId: string,
) {
  const result =
    await getPostgresPool().query<TeamAccessRow>(
      `
        SELECT
          s.id AS staff_member_id,
          s.name,
          s.email,
          s.role,
          s.active AS staff_active,
          u.id AS user_id,
          u.status AS user_status,
          m.status AS membership_status,
          (u.password_hash IS NOT NULL) AS password_ready
        FROM sf_staff_members s
        LEFT JOIN sf_users u
          ON u.id = s.user_id
        LEFT JOIN sf_memberships m
          ON m.organization_id = s.organization_id
         AND m.user_id = u.id
        WHERE s.organization_id = $1
        ORDER BY s.name ASC, s.id ASC
      `,
      [organizationId],
    )

  return result.rows.map(mapAccess)
}

export async function createTeamInvitation(
  input: {
    organizationId: string
    staffMemberId: number
    invitedByUserId: string
  },
) {
  const pool = getPostgresPool()
  const client = await pool.connect()

  let userId = ""
  let email = ""
  let organizationName = ""
  let organizationSlug = ""
  let alreadyActive = false

  try {
    await client.query("BEGIN")
    await client.query(
      "SELECT pg_advisory_xact_lock(hashtextextended($1, 0))",
      [
        `saborflow-team-invite:${input.organizationId}:${input.staffMemberId}`,
      ],
    )

    const staffResult =
      await client.query<{
        id: number
        name: string
        email: string
        phone: string
        role: StaffRole
        active: boolean
        user_id: string | null
        organization_name: string
        organization_slug: string
      }>(
        `
          SELECT
            s.id,
            s.name,
            s.email,
            s.phone,
            s.role,
            s.active,
            s.user_id,
            o.trade_name AS organization_name,
            o.slug AS organization_slug
          FROM sf_staff_members s
          INNER JOIN sf_organizations o
            ON o.id = s.organization_id
          WHERE s.organization_id = $1
            AND s.id = $2
          LIMIT 1
        `,
        [
          input.organizationId,
          input.staffMemberId,
        ],
      )

    const staff = staffResult.rows[0]

    if (!staff) {
      throw new Error(
        "Colaborador nÃ£o encontrado.",
      )
    }

    email = staff.email
      .trim()
      .toLowerCase()

    if (!email) {
      throw new Error(
        "Cadastre um e-mail no colaborador antes de criar o acesso.",
      )
    }

    if (!staff.active) {
      throw new Error(
        "Ative o colaborador antes de criar o acesso.",
      )
    }

    organizationName =
      staff.organization_name
    organizationSlug =
      staff.organization_slug

    const existingUser =
      await client.query<{
        id: string
        status:
          | "active"
          | "blocked"
          | "pending"
      }>(
        `
          SELECT id, status
          FROM sf_users
          WHERE lower(email) = lower($1)
          LIMIT 1
        `,
        [email],
      )

    if (existingUser.rows[0]) {
      if (
        existingUser.rows[0].status ===
        "blocked"
      ) {
        throw new Error(
          "Este usuÃ¡rio estÃ¡ bloqueado na plataforma.",
        )
      }

      userId =
        existingUser.rows[0].id

      await client.query(
        `
          UPDATE sf_users
          SET
            name = CASE
              WHEN trim(name) = '' THEN $2
              ELSE name
            END,
            phone = COALESCE(NULLIF(phone, ''), NULLIF($3, '')),
            updated_at = now()
          WHERE id = $1
        `,
        [
          userId,
          staff.name.trim(),
          staff.phone.trim(),
        ],
      )
    } else {
      userId = randomUUID()

      await client.query(
        `
          INSERT INTO sf_users (
            id,
            name,
            email,
            phone,
            status,
            created_at,
            updated_at
          )
          VALUES (
            $1,
            $2,
            $3,
            $4,
            'pending',
            now(),
            now()
          )
        `,
        [
          userId,
          staff.name.trim(),
          email,
          staff.phone.trim() || null,
        ],
      )
    }

    const membership =
      await client.query<{
        id: string
        status:
          | "active"
          | "invited"
          | "disabled"
      }>(
        `
          SELECT id, status
          FROM sf_memberships
          WHERE organization_id = $1
            AND user_id = $2
          LIMIT 1
        `,
        [
          input.organizationId,
          userId,
        ],
      )

    alreadyActive =
      membership.rows[0]?.status ===
      "active"

    if (!alreadyActive) {
      if (membership.rows[0]) {
        await client.query(
          `
            UPDATE sf_memberships
            SET
              role = $3,
              status = 'invited',
              invited_by_user_id = $4,
              invited_at = now(),
              accepted_at = NULL,
              updated_at = now()
            WHERE organization_id = $1
              AND user_id = $2
          `,
          [
            input.organizationId,
            userId,
            staff.role,
            input.invitedByUserId,
          ],
        )
      } else {
        await client.query(
          `
            INSERT INTO sf_memberships (
              id,
              organization_id,
              user_id,
              role,
              status,
              invited_by_user_id,
              invited_at,
              created_at,
              updated_at
            )
            VALUES (
              $1,
              $2,
              $3,
              $4,
              'invited',
              $5,
              now(),
              now(),
              now()
            )
          `,
          [
            randomUUID(),
            input.organizationId,
            userId,
            staff.role,
            input.invitedByUserId,
          ],
        )
      }
    }

    await client.query(
      `
        UPDATE sf_staff_members
        SET
          user_id = $3,
          updated_at = now()
        WHERE organization_id = $1
          AND id = $2
      `,
      [
        input.organizationId,
        input.staffMemberId,
        userId,
      ],
    )

    await client.query("COMMIT")
  } catch (error) {
    await client.query("ROLLBACK")
    throw error
  } finally {
    client.release()
  }

  if (alreadyActive) {
    return {
      alreadyActive: true,
      userId,
      email,
      organizationName,
      organizationSlug,
      token: null,
      expiresAt: null,
    }
  }

  await revokeOutstandingAuthTokens(
    userId,
    "invite",
    input.organizationId,
  )

  const created = await createAuthToken({
    userId,
    organizationId:
      input.organizationId,
    purpose: "invite",
    createdByUserId:
      input.invitedByUserId,
    expiresInMinutes: 7 * 24 * 60,
    metadata: {
      organizationName,
      organizationSlug,
      email,
    },
  })

  return {
    alreadyActive: false,
    userId,
    email,
    organizationName,
    organizationSlug,
    token: created.token,
    expiresAt:
      created.expiresAt,
  }
}

export async function getInvitationPreview(
  token: string,
) {
  const valid =
    await getValidAuthToken(
      token,
      "invite",
    )

  if (
    !valid ||
    !valid.organizationId
  ) {
    return null
  }

  // A leitura pÃºblica do convite Ã© um fluxo de bootstrap autenticado pelo
  // prÃ³prio token opaco. Ela nÃ£o pode depender de um contexto RLS herdado de
  // uma sessÃ£o administrativa inexistente no navegador do convidado.
  const result =
    await runWithRlsBypass(() =>
      getPostgresPool().query<{
        user_id: string
        name: string
        email: string
        password_ready: boolean
        membership_status:
          | "active"
          | "invited"
          | "disabled"
        role: OrganizationRole
        organization_name: string
        organization_slug: string
      }>(
        `
          SELECT
            u.id AS user_id,
            u.name,
            u.email,
            (u.password_hash IS NOT NULL) AS password_ready,
            m.status AS membership_status,
            m.role,
            o.trade_name AS organization_name,
            o.slug AS organization_slug
          FROM sf_users u
          INNER JOIN sf_memberships m
            ON m.user_id = u.id
           AND m.organization_id = $2
          INNER JOIN sf_organizations o
            ON o.id = m.organization_id
          WHERE u.id = $1
            AND u.status <> 'blocked'
          LIMIT 1
        `,
        [
          valid.userId,
          valid.organizationId,
        ],
      ),
    )

  const row = result.rows[0]
  if (!row) return null

  // Um token de convite sÃ³ deve ativar uma associaÃ§Ã£o ainda pendente.
  // Acesso jÃ¡ ativo/desativado exige um novo fluxo administrativo.
  if (
    row.membership_status !==
    "invited"
  ) {
    return null
  }

  return {
    tokenId: valid.id,
    userId: row.user_id,
    name: row.name,
    email: row.email,
    passwordReady:
      Boolean(row.password_ready),
    membershipStatus:
      row.membership_status,
    role: row.role,
    organizationId:
      valid.organizationId,
    organizationName:
      row.organization_name,
    organizationSlug:
      row.organization_slug,
    expiresAt:
      valid.expiresAt,
  }
}



async function reconcileCourierProfileAfterInvitation(
  client: PoolClient,
  input: {
    organizationId: string
    staffMemberId: number
    role: OrganizationRole
    name: string
    phone: string
  },
) {
  if (input.role !== "courier") return

  const schema = await client.query(
    `
      SELECT EXISTS (
        SELECT 1
        FROM information_schema.columns
        WHERE table_schema = 'public'
          AND table_name = 'sf_couriers'
          AND column_name = 'staff_member_id'
      ) AS ready
    `,
  )

  if (!schema.rows[0]?.ready) return

  const linked = await client.query(
    `
      SELECT id
      FROM sf_couriers
      WHERE organization_id = $1
        AND staff_member_id = $2
      LIMIT 1
      FOR UPDATE
    `,
    [input.organizationId, input.staffMemberId],
  )

  if (linked.rows[0]) {
    await client.query(
      `
        UPDATE sf_couriers
        SET active = true, updated_at = now()
        WHERE organization_id = $1
          AND id = $2
      `,
      [input.organizationId, Number(linked.rows[0].id)],
    )
    return
  }

  const digits = input.phone.replace(/\D/g, "")
  const candidates = await client.query(
    `
      SELECT id
      FROM sf_couriers
      WHERE organization_id = $1
        AND staff_member_id IS NULL
        AND (
          ($2 <> '' AND regexp_replace(phone, '[^0-9]', '', 'g') = $2)
          OR lower(trim(name)) = lower(trim($3))
        )
      ORDER BY id ASC
      FOR UPDATE
    `,
    [input.organizationId, digits, input.name],
  )

  if (candidates.rows.length === 1) {
    await client.query(
      `
        UPDATE sf_couriers
        SET
          staff_member_id = $3,
          active = true,
          updated_at = now()
        WHERE organization_id = $1
          AND id = $2
          AND staff_member_id IS NULL
      `,
      [
        input.organizationId,
        Number(candidates.rows[0].id),
        input.staffMemberId,
      ],
    )
    return
  }

  if (candidates.rows.length > 1 || !input.phone.trim()) return

  await client.query(
    "SELECT pg_advisory_xact_lock(hashtextextended($1, 0))",
    [`saborflow-courier-id:${input.organizationId}`],
  )

  const next = await client.query(
    `
      SELECT COALESCE(MAX(id), 0)::int + 1 AS next_id
      FROM sf_couriers
      WHERE organization_id = $1
    `,
    [input.organizationId],
  )

  await client.query(
    `
      INSERT INTO sf_couriers (
        organization_id,
        id,
        name,
        phone,
        vehicle,
        active,
        staff_member_id,
        created_at,
        updated_at
      )
      VALUES ($1, $2, $3, $4, '', true, $5, now(), now())
    `,
    [
      input.organizationId,
      Number(next.rows[0]?.next_id || 1),
      input.name.trim(),
      input.phone.trim(),
      input.staffMemberId,
    ],
  )
}
export async function acceptTeamInvitation(
  token: string,
  password?: string,
) {
  const preview =
    await getInvitationPreview(token)

  if (!preview) {
    throw new Error(
      "Convite invÃ¡lido ou expirado.",
    )
  }

  const nextPassword =
    password?.trim() || ""

  if (
    !preview.passwordReady &&
    nextPassword.length < 12
  ) {
    throw new Error(
      "Crie uma senha com pelo menos 12 caracteres.",
    )
  }

  // O aceite tambÃ©m Ã© um bootstrap autenticado pelo token. O bypass fica
  // restrito a esta transaÃ§Ã£o e todas as mutaÃ§Ãµes usam simultaneamente o
  // token, user_id e organization_id obtidos da validaÃ§Ã£o anterior.
  await runWithRlsBypass(async () => {
    const client =
      await getPostgresPool().connect()

    try {
      await client.query("BEGIN")

      const consumed =
        await client.query(
          `
            UPDATE sf_auth_tokens
            SET used_at = now()
            WHERE id = $1
              AND user_id = $2
              AND organization_id = $3
              AND purpose = 'invite'
              AND used_at IS NULL
              AND expires_at > now()
            RETURNING id
          `,
          [
            preview.tokenId,
            preview.userId,
            preview.organizationId,
          ],
        )

      if (!consumed.rowCount) {
        throw new Error(
          "Este convite jÃ¡ foi usado ou expirou.",
        )
      }

      if (!preview.passwordReady) {
        await client.query(
          `
            UPDATE sf_users
            SET
              password_hash = $2,
              password_updated_at = now(),
              status = 'active',
              updated_at = now()
            WHERE id = $1
              AND status <> 'blocked'
          `,
          [
            preview.userId,
            hashAdminPassword(
              nextPassword,
            ),
          ],
        )
      } else {
        await client.query(
          `
            UPDATE sf_users
            SET
              status = 'active',
              updated_at = now()
            WHERE id = $1
              AND status <> 'blocked'
          `,
          [preview.userId],
        )
      }

      const membership =
        await client.query(
          `
            UPDATE sf_memberships
            SET
              status = 'active',
              accepted_at = now(),
              updated_at = now()
            WHERE organization_id = $1
              AND user_id = $2
              AND status = 'invited'
            RETURNING id
          `,
          [
            preview.organizationId,
            preview.userId,
          ],
        )

      if (!membership.rowCount) {
        throw new Error(
          "Este convite nÃ£o estÃ¡ mais pendente para esta empresa.",
        )
      }

      const staff =
        await client.query(
          `
            UPDATE sf_staff_members
            SET
              user_id = $3,
              active = true,
              updated_at = now()
            WHERE organization_id = $1
              AND lower(email) = lower($2)
            RETURNING id, role, name, phone
          `,
          [
            preview.organizationId,
            preview.email,
            preview.userId,
          ],
        )

      if (!staff.rowCount) {
        throw new Error(
          "O perfil do colaborador associado a este convite nÃ£o foi encontrado.",
        )
      }

      const acceptedStaff = staff.rows.find(
        (row) => row.role === preview.role,
      ) ?? staff.rows[0]

      await reconcileCourierProfileAfterInvitation(client, {
        organizationId: preview.organizationId,
        staffMemberId: Number(acceptedStaff.id),
        role: preview.role,
        name: String(acceptedStaff.name || preview.name),
        phone: String(acceptedStaff.phone || ""),
      })

      await client.query("COMMIT")
    } catch (error) {
      await client.query("ROLLBACK")
      throw error
    } finally {
      client.release()
    }
  })

  return {
    email: preview.email,
    organizationName:
      preview.organizationName,
    passwordCreated:
      !preview.passwordReady,
  }
}

export async function disableTeamAccess(
  organizationId: string,
  userId: string,
) {
  const client =
    await getPostgresPool().connect()

  try {
    await client.query("BEGIN")

    const membership =
      await client.query<{
        role: OrganizationRole
      }>(
        `
          SELECT role
          FROM sf_memberships
          WHERE organization_id = $1
            AND user_id = $2
          LIMIT 1
        `,
        [organizationId, userId],
      )

    if (!membership.rows[0]) {
      throw new Error(
        "Acesso nÃ£o encontrado.",
      )
    }

    if (
      membership.rows[0].role ===
      "owner"
    ) {
      throw new Error(
        "O proprietÃ¡rio da empresa nÃ£o pode ser desativado por esta tela.",
      )
    }

    await client.query(
      `
        UPDATE sf_memberships
        SET
          status = 'disabled',
          updated_at = now()
        WHERE organization_id = $1
          AND user_id = $2
      `,
      [organizationId, userId],
    )

    await client.query(
      `
        UPDATE sf_staff_members
        SET
          active = false,
          updated_at = now()
        WHERE organization_id = $1
          AND user_id = $2
      `,
      [organizationId, userId],
    )

    await client.query(
      `
        UPDATE sf_auth_tokens
        SET used_at = COALESCE(used_at, now())
        WHERE organization_id = $1
          AND user_id = $2
          AND used_at IS NULL
      `,
      [organizationId, userId],
    )

    await client.query("COMMIT")
  } catch (error) {
    await client.query("ROLLBACK")
    throw error
  } finally {
    client.release()
  }
}

export async function createTeamPasswordReset(
  input: {
    organizationId: string
    targetUserId: string
    createdByUserId: string
    actorRole: OrganizationRole
  },
) {
  const result =
    await getPostgresPool().query<{
      email: string
      role: OrganizationRole
      status:
        | "active"
        | "invited"
        | "disabled"
    }>(
      `
        SELECT
          u.email,
          m.role,
          m.status
        FROM sf_memberships m
        INNER JOIN sf_users u
          ON u.id = m.user_id
        WHERE m.organization_id = $1
          AND m.user_id = $2
        LIMIT 1
      `,
      [
        input.organizationId,
        input.targetUserId,
      ],
    )

  const target = result.rows[0]

  if (
    !target ||
    target.status !== "active"
  ) {
    throw new Error(
      "O usuÃ¡rio precisa ter acesso ativo para redefinir a senha.",
    )
  }

  if (
    target.role === "owner" &&
    input.actorRole !== "owner"
  ) {
    throw new Error(
      "Somente um proprietÃ¡rio pode gerar recuperaÃ§Ã£o para outro proprietÃ¡rio.",
    )
  }

  await revokeOutstandingAuthTokens(
    input.targetUserId,
    "password_reset",
    input.organizationId,
  )

  const created =
    await createAuthToken({
      userId:
        input.targetUserId,
      organizationId:
        input.organizationId,
      purpose:
        "password_reset",
      createdByUserId:
        input.createdByUserId,
      expiresInMinutes: 30,
      metadata: {
        email: target.email,
      },
    })

  return {
    email: target.email,
    token: created.token,
    expiresAt:
      created.expiresAt,
  }
}

==========================================================================================
ARQUIVO: lib\auth.ts
==========================================================================================
import { createHmac, timingSafeEqual } from "node:crypto"
import { cookies } from "next/headers"
import type { AdminTenantContext, OrganizationRole } from "@/lib/tenant-context"
import { demoOrganizationIsUsable } from "@/lib/demo-policy"
import { enterTenantRlsContext } from "@/lib/rls-context"

export const ADMIN_SESSION_COOKIE = "saborflow_admin_session"
export const SUPERADMIN_SESSION_COOKIE = "saborflow_superadmin_session"
// Mantido apenas para que logout/login removam cookies antigos do navegador.
export const LEGACY_ADMIN_SESSION_COOKIE = "cris_admin_session"

export const ADMIN_SESSION_IDLE_SECONDS = 60 * 60 * 24 * 10
const ADMIN_SESSION_IDLE_MS = ADMIN_SESSION_IDLE_SECONDS * 1000

/** @deprecated Mantido apenas para compatibilidade de componentes antigos. */
export const getAdminEmail = () => ""

export const getAdminLoginMode = () => "postgres" as const
export const legacyAdminLoginAllowed = () => false

export const isSessionSecretConfigured = () =>
  Boolean(process.env.SESSION_SECRET?.trim())

const getSessionSecret = () => {
  const configured = process.env.SESSION_SECRET?.trim()
  if (!configured) {
    throw new Error("SESSION_SECRET Ã© obrigatÃ³rio na Fase 25.")
  }
  return configured
}

export type AdminSession =
  | {
      mode: "tenant"
      sessionId: string
      userId: string
      email: string
      organizationId: string
      organizationName: string
      organizationSlug: string
      role: OrganizationRole
      sessionVersion: number
      issuedAt: number
      expiresAt: number
    }
  | {
      // Tipo mantido temporariamente para compatibilidade de componentes antigos.
      // getAdminSession() nunca produz esta variante apÃ³s a Fase 25.
      mode: "legacy"
      email: string
    }

type SuperadminSessionPayload = {
  v: 2
  purpose: "superadmin-cpf"
  sessionId: string
  userId: string
  issuedAt: number
  expiresAt: number
}

/** @deprecated Login legado foi desativado na Fase 25. */
export function credentialsAreValid(_email: string, _password: string) {
  return false
}

function sign(value: string) {
  return createHmac("sha256", getSessionSecret())
    .update(value)
    .digest("base64url")
}

function signaturesMatch(actual: string, expected: string) {
  const actualBuffer = Buffer.from(actual)
  const expectedBuffer = Buffer.from(expected)
  if (actualBuffer.length !== expectedBuffer.length) return false
  return timingSafeEqual(actualBuffer, expectedBuffer)
}

function tenantTokenFromSession(
  session: Pick<
    Extract<AdminSession, { mode: "tenant" }>,
    | "sessionId"
    | "userId"
    | "email"
    | "organizationId"
    | "organizationName"
    | "organizationSlug"
    | "role"
    | "sessionVersion"
  >,
) {
  const now = Date.now()
  const payload = {
    v: 4,
    sessionId: session.sessionId,
    userId: session.userId,
    email: session.email,
    organizationId: session.organizationId,
    organizationName: session.organizationName,
    organizationSlug: session.organizationSlug,
    role: session.role,
    sessionVersion: session.sessionVersion,
    issuedAt: now,
    expiresAt: now + ADMIN_SESSION_IDLE_MS,
  }

  const encoded = Buffer.from(JSON.stringify(payload)).toString("base64url")
  return `v4.${encoded}.${sign(encoded)}`
}

export function createSessionToken(
  context: AdminTenantContext,
  sessionId: string,
) {
  if (!context) {
    throw new Error("SessÃ£o administrativa exige contexto tenant PostgreSQL.")
  }

  const normalizedSessionId = sessionId.trim()
  if (!normalizedSessionId) {
    throw new Error("SessÃ£o administrativa exige identificador persistente.")
  }

  return tenantTokenFromSession({
    sessionId: normalizedSessionId,
    userId: context.userId,
    email: context.email,
    organizationId: context.organizationId,
    organizationName: context.organizationName,
    organizationSlug: context.organizationSlug,
    role: context.role,
    sessionVersion: context.sessionVersion,
  })
}

export function createSuperadminSessionToken(
  userId: string,
  sessionId: string,
) {
  const normalizedUserId = userId.trim()
  const normalizedSessionId = sessionId.trim()

  if (!normalizedUserId || !normalizedSessionId) {
    throw new Error(
      "SessÃ£o Superadmin exige usuÃ¡rio e sessÃ£o persistente vÃ¡lidos.",
    )
  }

  const now = Date.now()
  const payload: SuperadminSessionPayload = {
    v: 2,
    purpose: "superadmin-cpf",
    sessionId: normalizedSessionId,
    userId: normalizedUserId,
    issuedAt: now,
    expiresAt: now + ADMIN_SESSION_IDLE_MS,
  }

  const encoded = Buffer.from(JSON.stringify(payload)).toString("base64url")
  const signature = sign(`superadmin:${encoded}`)
  return `sa2.${encoded}.${signature}`
}

export function parseAdminSessionToken(
  token?: string | null,
): AdminSession | null {
  // A Etapa 13.6 invalida os antigos v2/v3 porque eles nÃ£o possuem sessionId
  // persistente e, portanto, nÃ£o podem ser revogados por dispositivo.
  if (!token || !token.startsWith("v4.")) {
    return null
  }

  const parts = token.split(".")
  if (parts.length !== 3) return null

  const [, encoded, signature] = parts
  if (!encoded || !signature) return null
  if (!signaturesMatch(signature, sign(encoded))) return null

  try {
    const payload = JSON.parse(
      Buffer.from(encoded, "base64url").toString("utf8"),
    ) as {
      v?: number
      sessionId?: string
      userId?: string
      email?: string
      organizationId?: string
      organizationName?: string
      organizationSlug?: string
      role?: OrganizationRole
      sessionVersion?: number
      issuedAt?: number
      expiresAt?: number
    }

    if (
      payload.v !== 4 ||
      !payload.sessionId ||
      !payload.userId ||
      !payload.email ||
      !payload.organizationId ||
      !payload.organizationName ||
      !payload.organizationSlug ||
      !payload.role ||
      !payload.sessionVersion ||
      !payload.issuedAt ||
      !payload.expiresAt ||
      payload.expiresAt <= Date.now()
    ) {
      return null
    }

    return {
      mode: "tenant",
      sessionId: payload.sessionId,
      userId: payload.userId,
      email: payload.email,
      organizationId: payload.organizationId,
      organizationName: payload.organizationName,
      organizationSlug: payload.organizationSlug,
      role: payload.role,
      sessionVersion: Number(payload.sessionVersion),
      issuedAt: payload.issuedAt,
      expiresAt: payload.expiresAt,
    }
  } catch {
    return null
  }
}

function parseSuperadminSessionToken(
  token?: string | null,
): SuperadminSessionPayload | null {
  if (!token || !token.startsWith("sa2.")) return null

  const parts = token.split(".")
  if (parts.length !== 3) return null

  const [, encoded, signature] = parts
  if (!encoded || !signature) return null

  const expectedSignature = sign(`superadmin:${encoded}`)
  if (!signaturesMatch(signature, expectedSignature)) return null

  try {
    const payload = JSON.parse(
      Buffer.from(encoded, "base64url").toString("utf8"),
    ) as Partial<SuperadminSessionPayload>

    if (
      payload.v !== 2 ||
      payload.purpose !== "superadmin-cpf" ||
      !payload.sessionId ||
      !payload.userId ||
      !payload.issuedAt ||
      !payload.expiresAt ||
      payload.expiresAt <= Date.now()
    ) {
      return null
    }

    return {
      v: 2,
      purpose: "superadmin-cpf",
      sessionId: payload.sessionId,
      userId: payload.userId,
      issuedAt: payload.issuedAt,
      expiresAt: payload.expiresAt,
    }
  } catch {
    return null
  }
}

export function refreshAdminSessionToken(token?: string | null) {
  const session = parseAdminSessionToken(token)
  if (!session || session.mode !== "tenant") return null

  return tenantTokenFromSession({
    sessionId: session.sessionId,
    userId: session.userId,
    email: session.email,
    organizationId: session.organizationId,
    organizationName: session.organizationName,
    organizationSlug: session.organizationSlug,
    role: session.role,
    sessionVersion: session.sessionVersion,
  })
}

export function refreshSuperadminSessionToken(token?: string | null) {
  const session = parseSuperadminSessionToken(token)
  if (!session) return null

  return createSuperadminSessionToken(
    session.userId,
    session.sessionId,
  )
}

export function sessionTokenIsValid(token?: string | null) {
  return Boolean(parseAdminSessionToken(token))
}

export async function hasSuperadminCpfSession(
  userId: string,
  sessionId: string,
) {
  const cookieStore = await cookies()
  const token = cookieStore.get(SUPERADMIN_SESSION_COOKIE)?.value
  const session = parseSuperadminSessionToken(token)

  return Boolean(
    session &&
      session.userId === userId.trim() &&
      session.sessionId === sessionId.trim(),
  )
}

export async function getAdminSession(): Promise<AdminSession | null> {
  const cookieStore = await cookies()
  const tenantToken = cookieStore.get(ADMIN_SESSION_COOKIE)?.value
  const tenantSession = parseAdminSessionToken(tenantToken)

  if (tenantSession?.mode !== "tenant") return null

  enterTenantRlsContext(
    tenantSession.organizationId,
    tenantSession.userId,
    "tenant-session",
  )

  if (await demoOrganizationIsUsable(tenantSession.organizationId)) {
    return tenantSession
  }

  return null
}

export async function isAdminAuthenticated() {
  return Boolean(await getAdminSession())
}

==========================================================================================
ARQUIVO: lib\superadmin-auth.ts
==========================================================================================
import {
  getAdminSession,
  hasSuperadminCpfSession,
} from "@/lib/auth"
import { getPostgresPool } from "@/lib/postgres"
import { validateAndTouchAdminSession } from "@/lib/security/admin-sessions"

export type PlatformAdminRole =
  | "owner"
  | "operator"
  | "support"
  | "finance"

export type SuperadminAccess = {
  platformAdminId: string
  userId: string
  email: string
  role: PlatformAdminRole
}

function primarySuperadminUserId() {
  return (
    process.env.SUPERADMIN_PRIMARY_USER_ID?.trim() ||
    ""
  )
}

export async function userCanReceiveSuperadminCpfSession(
  userId: string,
) {
  const primaryUserId =
    primarySuperadminUserId()

  if (
    !primaryUserId ||
    userId.trim() !== primaryUserId
  ) {
    return false
  }

  try {
    const result =
      await getPostgresPool().query<{
        id: string
      }>(
        `
          SELECT pa.id
          FROM sf_platform_admins pa
          INNER JOIN sf_users u
            ON u.id = pa.user_id
          WHERE pa.user_id = $1
            AND pa.status = 'active'
            AND u.status = 'active'
          LIMIT 1
        `,
        [userId],
      )

    return Boolean(result.rows[0])
  } catch (error) {
    const pgError =
      error as { code?: string }

    if (pgError?.code === "42P01") {
      return false
    }

    throw error
  }
}

export async function getSuperadminAccess():
  Promise<SuperadminAccess | null> {
  const session =
    await getAdminSession()

  if (
    !session ||
    session.mode !== "tenant"
  ) {
    return null
  }

  const persistentSessionValid =
    await validateAndTouchAdminSession({
      sessionId:
        session.sessionId,
      userId:
        session.userId,
      organizationId:
        session.organizationId,
      sessionVersion:
        session.sessionVersion,
    })

  if (!persistentSessionValid) {
    return null
  }

  const primaryUserId =
    primarySuperadminUserId()

  if (
    !primaryUserId ||
    session.userId !== primaryUserId
  ) {
    return null
  }

  if (
    !(await hasSuperadminCpfSession(
      session.userId,
      session.sessionId,
    ))
  ) {
    return null
  }

  try {
    const result =
      await getPostgresPool().query<{
        id: string
        user_id: string
        email: string
        role: PlatformAdminRole
      }>(
        `
          SELECT
            pa.id,
            pa.user_id,
            u.email,
            pa.role
          FROM sf_platform_admins pa
          INNER JOIN sf_users u
            ON u.id = pa.user_id
          WHERE pa.user_id = $1
            AND pa.status = 'active'
            AND u.status = 'active'
          LIMIT 1
        `,
        [session.userId],
      )

    const row = result.rows[0]

    if (!row) return null

    return {
      platformAdminId: row.id,
      userId: row.user_id,
      email: row.email,
      role: row.role,
    }
  } catch (error) {
    const pgError =
      error as { code?: string }

    if (pgError?.code === "42P01") {
      return null
    }

    throw error
  }
}

export function canManageCommercialState(
  role: PlatformAdminRole,
) {
  return (
    role === "owner" ||
    role === "operator" ||
    role === "finance"
  )
}

export function canManageSupport(
  role: PlatformAdminRole,
) {
  return (
    role === "owner" ||
    role === "operator" ||
    role === "support"
  )
}

export function canManagePlatformAdmins(
  role: PlatformAdminRole,
) {
  return role === "owner"
}

export function canReviewRegistrations(
  role: PlatformAdminRole,
) {
  return (
    role === "owner" ||
    role === "operator"
  )
}

export function canManagePlatformFinance(
  role: PlatformAdminRole,
) {
  return (
    role === "owner" ||
    role === "operator" ||
    role === "finance"
  )
}

==========================================================================================
ARQUIVO: lib\admin-user-db.ts
==========================================================================================
import {
  randomBytes,
  scryptSync,
  timingSafeEqual,
} from "node:crypto"
import { getPostgresPool } from "@/lib/postgres"
import {
  createAuthToken,
  getValidAuthToken,
  revokeOutstandingAuthTokens,
} from "@/lib/security-tokens"
import { runWithRlsBypass } from "@/lib/rls-context"
import { clearPersistentLoginFailuresForUser } from "@/lib/security/login-lockout"

const PREFIX = "scrypt$v1"
const MIN_ADMIN_PASSWORD_LENGTH = 12
const MAX_ADMIN_PASSWORD_LENGTH = 128
const PASSWORD_POLICY_MESSAGE =
  "A senha deve ter entre 12 e 128 caracteres, 1 letra maiÃºscula e 1 caractere especial."

const DUMMY_PASSWORD_HASH = `${PREFIX}$saborflow-auth-dummy$${scryptSync(
  "__saborflow_invalid_password__",
  "saborflow-auth-dummy",
  32,
).toString("hex")}`

type AdminUserCredentialRow = {
  id: string
  name: string
  email: string
  cpf: string | null
  password_hash: string | null
  status: string
}

export type AuthenticatedAdminUser = {
  id: string
  name: string
  email: string
}

function normalizeAdminLoginIdentifier(identifier: string) {
  const trimmed = identifier.trim()

  if (trimmed.includes("@")) {
    return {
      type: "email" as const,
      value: trimmed.toLowerCase(),
    }
  }

  return {
    type: "cpf" as const,
    value: trimmed.replace(/\D/g, ""),
  }
}

function hasUppercaseLetter(password: string) {
  return password !== password.toLocaleLowerCase("pt-BR")
}

function hasSpecialCharacter(password: string) {
  return /[^\p{L}\p{N}\s]/u.test(password)
}

export function validateAdminPasswordPolicy(password: string) {
  if (
    password.length < MIN_ADMIN_PASSWORD_LENGTH ||
    password.length > MAX_ADMIN_PASSWORD_LENGTH ||
    !hasUppercaseLetter(password) ||
    !hasSpecialCharacter(password)
  ) {
    throw new Error(PASSWORD_POLICY_MESSAGE)
  }

  return true
}

async function findAdminUserCredential(identifier: string) {
  const normalized = normalizeAdminLoginIdentifier(identifier)

  if (
    normalized.type === "cpf" &&
    normalized.value.length !== 11
  ) {
    return null
  }

  const result =
    await getPostgresPool().query<AdminUserCredentialRow>(
      `
        SELECT
          id,
          name,
          email,
          cpf,
          password_hash,
          status
        FROM sf_users
        WHERE (
          $2 = 'email'
          AND lower(email) = lower($1)
        )
        OR (
          $2 = 'cpf'
          AND regexp_replace(
            COALESCE(cpf, ''),
            '[^0-9]',
            '',
            'g'
          ) = $1
        )
        LIMIT 1
      `,
      [normalized.value, normalized.type],
    )

  return result.rows[0] || null
}

export function hashAdminPassword(password: string) {
  validateAdminPasswordPolicy(password)

  const salt = randomBytes(16).toString("hex")
  const digest = scryptSync(
    password,
    salt,
    32,
  ).toString("hex")

  return `${PREFIX}$${salt}$${digest}`
}

export function verifyAdminPassword(
  password: string,
  stored: string,
) {
  const [algorithm, version, salt, digest] =
    stored.split("$")

  if (
    algorithm !== "scrypt" ||
    version !== "v1" ||
    !salt ||
    !digest
  ) {
    return false
  }

  try {
    const actual = scryptSync(
      password,
      salt,
      32,
    )
    const expected = Buffer.from(
      digest,
      "hex",
    )

    return (
      actual.length === expected.length &&
      timingSafeEqual(actual, expected)
    )
  } catch {
    return false
  }
}

export async function getAdminUserCredentialState(
  identifier: string,
) {
  const row =
    await findAdminUserCredential(identifier)

  return row
    ? {
        userId: row.id,
        name: row.name,
        email: row.email,
        active: row.status === "active",
        passwordReady:
          Boolean(row.password_hash),
      }
    : null
}

export async function authenticateAdminUser(
  identifier: string,
  password: string,
): Promise<AuthenticatedAdminUser | null> {
  const row =
    await findAdminUserCredential(identifier)

  const passwordHash =
    row?.password_hash ||
    DUMMY_PASSWORD_HASH

  const passwordMatches =
    verifyAdminPassword(
      password,
      passwordHash,
    )

  if (
    !row ||
    row.status !== "active" ||
    !row.password_hash ||
    !passwordMatches
  ) {
    return null
  }

  return {
    id: row.id,
    name: row.name,
    email: row.email,
  }
}

export async function createSelfServicePasswordReset(
  identifier: string,
) {
  const row =
    await findAdminUserCredential(identifier)

  if (
    !row ||
    row.status !== "active" ||
    !row.email?.trim()
  ) {
    return null
  }

  await revokeOutstandingAuthTokens(
    row.id,
    "password_reset",
  )

  const created =
    await createAuthToken({
      userId: row.id,
      organizationId: null,
      purpose: "password_reset",
      expiresInMinutes: 30,
      metadata: {
        email: row.email,
        source: "self_service",
      },
    })

  return {
    email: row.email.trim().toLowerCase(),
    token: created.token,
    expiresAt: created.expiresAt,
  }
}

export async function authenticateAdminGoogleUser(
  googleSubject: string,
  email: string,
): Promise<AuthenticatedAdminUser | null> {
  const result =
    await getPostgresPool().query<AdminUserCredentialRow>(
      `
        SELECT
          id,
          name,
          email,
          cpf,
          password_hash,
          status
        FROM sf_users
        WHERE google_subject = $1
        LIMIT 1
      `,
      [googleSubject.trim()],
    )

  const row = result.rows[0]

  if (
    !row ||
    row.status !== "active" ||
    row.email.trim().toLowerCase() !==
      email.trim().toLowerCase()
  ) {
    return null
  }

  return {
    id: row.id,
    name: row.name,
    email: row.email,
  }
}

export async function upgradeLegacyAdminPassword(
  email: string,
  password: string,
) {
  const state =
    await getAdminUserCredentialState(email)

  if (
    !state ||
    !state.active ||
    state.passwordReady
  ) {
    return false
  }

  const hash =
    hashAdminPassword(password)

  const result =
    await getPostgresPool().query(
      `
        UPDATE sf_users
        SET
          password_hash = $2,
          password_updated_at = now(),
          last_login_at = now(),
          updated_at = now()
        WHERE id = $1
          AND password_hash IS NULL
        RETURNING id
      `,
      [state.userId, hash],
    )

  return Boolean(result.rowCount)
}

export async function changeAdminUserPassword(
  userId: string,
  currentPassword: string,
  newPassword: string,
) {
  const result =
    await getPostgresPool().query<{
      password_hash: string | null
      status: string
    }>(
      `
        SELECT
          password_hash,
          status
        FROM sf_users
        WHERE id = $1
        LIMIT 1
      `,
      [userId],
    )

  const row = result.rows[0]

  if (
    !row ||
    row.status !== "active" ||
    !row.password_hash ||
    !verifyAdminPassword(
      currentPassword,
      row.password_hash,
    )
  ) {
    throw new Error(
      "Senha atual incorreta.",
    )
  }

  if (
    currentPassword === newPassword
  ) {
    throw new Error(
      "A nova senha precisa ser diferente da atual.",
    )
  }

  const passwordHash =
    hashAdminPassword(newPassword)

  await getPostgresPool().query(
    `
      UPDATE sf_users
      SET
        password_hash = $2,
        password_updated_at = now(),
        session_version = session_version + 1,
        updated_at = now()
      WHERE id = $1
    `,
    [userId, passwordHash],
  )

  await revokeOutstandingAuthTokens(
    userId,
    "password_reset",
  )

  await clearPersistentLoginFailuresForUser(
    userId,
  )

  return true
}

export async function getPasswordResetPreview(
  token: string,
) {
  const valid =
    await getValidAuthToken(
      token,
      "password_reset",
    )

  if (!valid) return null

  const result =
    await getPostgresPool().query<{
      name: string
      email: string
      status: string
    }>(
      `
        SELECT
          name,
          email,
          status
        FROM sf_users
        WHERE id = $1
        LIMIT 1
      `,
      [valid.userId],
    )

  const user = result.rows[0]

  if (
    !user ||
    user.status === "blocked"
  ) {
    return null
  }

  return {
    tokenId: valid.id,
    userId: valid.userId,
    organizationId:
      valid.organizationId,
    name: user.name,
    email: user.email,
    expiresAt: valid.expiresAt,
  }
}

export async function resetAdminUserPassword(
  token: string,
  newPassword: string,
) {
  const preview =
    await getPasswordResetPreview(token)

  if (!preview) {
    throw new Error(
      "Link de recuperaÃ§Ã£o invÃ¡lido ou expirado.",
    )
  }

  const passwordHash =
    hashAdminPassword(newPassword)

  return runWithRlsBypass(
    async () => {
      const client =
        await getPostgresPool().connect()

      try {
        await client.query("BEGIN")

        const consumed =
          await client.query(
            `
              UPDATE sf_auth_tokens
              SET used_at = now()
              WHERE id = $1
                AND used_at IS NULL
                AND expires_at > now()
              RETURNING id
            `,
            [preview.tokenId],
          )

        if (!consumed.rowCount) {
          throw new Error(
            "Este link jÃ¡ foi usado ou expirou.",
          )
        }

        const updatedUser =
          await client.query(
            `
              UPDATE sf_users
              SET
                password_hash = $2,
                password_updated_at = now(),
                session_version =
                  session_version + 1,
                status = CASE
                  WHEN status = 'blocked'
                    THEN status
                  ELSE 'active'
                END,
                updated_at = now()
              WHERE id = $1
              RETURNING id
            `,
            [
              preview.userId,
              passwordHash,
            ],
          )

        if (!updatedUser.rowCount) {
          throw new Error(
            "UsuÃ¡rio nÃ£o encontrado para redefiniÃ§Ã£o de senha.",
          )
        }

        await client.query(
          `
            UPDATE sf_auth_tokens
            SET used_at =
              COALESCE(used_at, now())
            WHERE user_id = $1
              AND id <> $2
              AND used_at IS NULL
          `,
          [
            preview.userId,
            preview.tokenId,
          ],
        )

        await client.query(
          `
            DELETE FROM sf_auth_login_lockouts
            WHERE user_id = $1
          `,
          [preview.userId],
        )

        await client.query("COMMIT")
      } catch (error) {
        await client.query("ROLLBACK")
        throw error
      } finally {
        client.release()
      }

      return {
        email: preview.email,
      }
    },
  )
}

==========================================================================================
ARQUIVO: lib\organization-security-db.ts
==========================================================================================
import {
  createHash,
  randomBytes,
  randomUUID,
} from "node:crypto"
import { resolveTxt } from "node:dns/promises"
import { getPostgresPool } from "@/lib/postgres"
import {
  enterTenantRlsContext,
  runWithRlsBypass,
  runWithTenantRlsScope,
} from "@/lib/rls-context"
import {
  normalizePublicDomain,
} from "@/lib/organization-db"
import {
  cloudflareDnsInstructions,
  ensureCloudflareCustomHostname,
  removeCloudflareCustomHostname,
} from "@/lib/cloudflare-saas"

const DOMAIN_TXT_PREFIX =
  "saborflow-verification="

function hashToken(value: string) {
  return createHash("sha256")
    .update(value)
    .digest("hex")
}

export function isValidTimeZone(
  value: string,
) {
  try {
    new Intl.DateTimeFormat(
      "pt-BR",
      {
        timeZone: value,
      },
    ).format(new Date())

    return true
  } catch {
    return false
  }
}

export async function getOrganizationTimeZone(
  organizationId: string,
) {
  const result =
    await getPostgresPool().query<{
      timezone: string
    }>(
      `
        SELECT timezone
        FROM sf_organization_settings
        WHERE organization_id = $1
        LIMIT 1
      `,
      [organizationId],
    )

  return (
    result.rows[0]?.timezone ||
    "America/Sao_Paulo"
  )
}

export async function updateOrganizationTimeZone(
  organizationId: string,
  timeZone: string,
) {
  const clean = timeZone.trim()

  if (!isValidTimeZone(clean)) {
    throw new Error(
      "Timezone invÃ¡lido.",
    )
  }

  const result =
    await getPostgresPool().query<{
      timezone: string
    }>(
      `
        UPDATE sf_organization_settings
        SET
          timezone = $2,
          updated_at = now()
        WHERE organization_id = $1
        RETURNING timezone
      `,
      [organizationId, clean],
    )

  if (!result.rows[0]) {
    throw new Error(
      "ConfiguraÃ§Ãµes da organizaÃ§Ã£o nÃ£o encontradas.",
    )
  }

  return result.rows[0].timezone
}

export type OrganizationDomainStatus = {
  domain: string
  verified: boolean
  primary: boolean
  verificationMethod: "dns_txt"
  verificationRecordName: string
  verifiedAt: string | null
  lastCheckedAt: string | null
}

function mapDomainRow(
  row: {
    domain: string
    verified: boolean
    primary_domain: boolean
    verification_method: "dns_txt"
    verified_at: Date | string | null
    last_checked_at: Date | string | null
  },
): OrganizationDomainStatus {
  return {
    domain: row.domain,
    verified:
      Boolean(row.verified),
    primary:
      Boolean(
        row.primary_domain,
      ),
    verificationMethod:
      row.verification_method,
    verificationRecordName:
      `_saborflow.${row.domain}`,
    verifiedAt: row.verified_at
      ? new Date(
          row.verified_at,
        ).toISOString()
      : null,
    lastCheckedAt:
      row.last_checked_at
        ? new Date(
            row.last_checked_at,
          ).toISOString()
        : null,
  }
}

export async function listOrganizationDomains(
  organizationId: string,
) {
  return runWithTenantRlsScope(
    [organizationId],
    undefined,
    async () => {
      const result =
        await getPostgresPool().query<{
          domain: string
          verified: boolean
          primary_domain: boolean
          verification_method: "dns_txt"
          verified_at: Date | string | null
          last_checked_at: Date | string | null
        }>(
          `
            SELECT
              domain,
              verified,
              primary_domain,
              verification_method,
              verified_at,
              last_checked_at
            FROM sf_organization_domains
            WHERE organization_id = $1
            ORDER BY
              primary_domain DESC,
              verified DESC,
              domain ASC
          `,
          [organizationId],
        )

      return result.rows.map(
        mapDomainRow,
      )
    },
    "tenant-session",
  )
}

function isReservedHost(
  domain: string,
) {
  const railway = normalizePublicDomain(
    process.env.RAILWAY_PUBLIC_DOMAIN || "",
  )
  const platform = normalizePublicDomain(
    process.env.STOREFRONT_ROOT_DOMAIN ||
      process.env.NEXT_PUBLIC_STOREFRONT_ROOT_DOMAIN ||
      "",
  )

  return Boolean(
    (railway && domain === railway) ||
      (platform &&
        (domain === platform || domain.endsWith(`.${platform}`))) ||
      domain.endsWith(".up.railway.app"),
  )
}

export async function createDomainVerification(
  input: {
    organizationId: string
    domain: string
  },
) {
  const domain =
    normalizePublicDomain(
      input.domain,
    )

  if (
    !domain ||
    !domain.includes(".") ||
    domain.includes(" ") ||
    domain.includes("/")
  ) {
    throw new Error(
      "Informe um domÃ­nio vÃ¡lido, sem http:// ou caminhos.",
    )
  }

  if (isReservedHost(domain)) {
    throw new Error(
      "Este domÃ­nio pertence Ã  infraestrutura do SaborFlow e nÃ£o pode ser cadastrado como domÃ­nio de cliente.",
    )
  }

  const challenge =
    `${DOMAIN_TXT_PREFIX}${randomBytes(
      24,
    ).toString("base64url")}`

  const challengeHash =
    hashToken(challenge)

  try {
    await runWithTenantRlsScope(
      [input.organizationId],
      undefined,
      async () => {
        const client =
          await getPostgresPool().connect()

        try {
          await client.query("BEGIN")

          const existing =
            await client.query<{
              verified: boolean
            }>(
              `
                SELECT verified
                FROM sf_organization_domains
                WHERE organization_id = $1
                  AND domain = $2
                LIMIT 1
                FOR UPDATE
              `,
              [
                input.organizationId,
                domain,
              ],
            )

          const current =
            existing.rows[0]

          if (current?.verified) {
            throw new Error(
              "Este domÃ­nio jÃ¡ estÃ¡ verificado.",
            )
          }

          if (current) {
            await client.query(
              `
                UPDATE sf_organization_domains
                SET
                  verified = false,
                  primary_domain = false,
                  verification_method = 'dns_txt',
                  verification_token_hash = $3,
                  verified_at = NULL,
                  last_checked_at = NULL,
                  updated_at = now()
                WHERE organization_id = $1
                  AND domain = $2
              `,
              [
                input.organizationId,
                domain,
                challengeHash,
              ],
            )
          } else {
            await client.query(
              `
                INSERT INTO sf_organization_domains (
                  domain,
                  organization_id,
                  verified,
                  primary_domain,
                  verification_method,
                  verification_token_hash,
                  created_at,
                  updated_at
                )
                VALUES (
                  $1,
                  $2,
                  false,
                  false,
                  'dns_txt',
                  $3,
                  now(),
                  now()
                )
              `,
              [
                domain,
                input.organizationId,
                challengeHash,
              ],
            )
          }

          await client.query("COMMIT")
        } catch (error) {
          await client.query("ROLLBACK")
          throw error
        } finally {
          client.release()
        }
      },
      "tenant-session",
    )
  } catch (error) {
    const pgError = error as { code?: string }
    if (pgError?.code === "23505") {
      throw new Error(
        "Este domÃ­nio jÃ¡ estÃ¡ vinculado a outra empresa.",
      )
    }
    throw error
  }

  let cloudflare
  try {
    cloudflare = await ensureCloudflareCustomHostname(domain)
  } catch (error) {
    // Evita deixar um cadastro local que o cliente acredita estar roteado.
    await runWithTenantRlsScope(
      [input.organizationId],
      undefined,
      async () => {
        await getPostgresPool().query(
          `
            DELETE FROM sf_organization_domains
            WHERE organization_id = $1
              AND domain = $2
              AND verified = false
          `,
          [input.organizationId, domain],
        )
      },
      "tenant-session",
    ).catch(() => null)
    throw error
  }

  return {
    domain,
    recordName: `_saborflow.${domain}`,
    recordValue: challenge,
    method: "dns_txt" as const,
    routing: cloudflareDnsInstructions(cloudflare),
  }
}

export async function verifyOrganizationDomain(
  input: {
    organizationId: string
    domain: string
  },
) {
  const domain =
    normalizePublicDomain(
      input.domain,
    )

  const row = await runWithTenantRlsScope(
    [input.organizationId],
    undefined,
    async () => {
      const result =
        await getPostgresPool().query<{
          verification_token_hash:
            | string
            | null
          verified: boolean
        }>(
          `
            SELECT
              verification_token_hash,
              verified
            FROM sf_organization_domains
            WHERE organization_id = $1
              AND domain = $2
            LIMIT 1
          `,
          [
            input.organizationId,
            domain,
          ],
        )

      return result.rows[0]
    },
    "tenant-session",
  )

  if (!row) {
    throw new Error(
      "DomÃ­nio nÃ£o encontrado nesta empresa.",
    )
  }

  if (row.verified) {
    const cloudflare = await ensureCloudflareCustomHostname(domain)
    return {
      verified: true,
      domain,
      routing: cloudflareDnsInstructions(cloudflare),
    }
  }

  if (
    !row.verification_token_hash
  ) {
    throw new Error(
      "Gere novamente o registro de verificaÃ§Ã£o deste domÃ­nio.",
    )
  }

  const recordName =
    `_saborflow.${domain}`

  let records: string[][] = []

  try {
    records =
      await resolveTxt(
        recordName,
      )
  } catch {
    records = []
  }

  const values = records.map(
    (parts) => parts.join(""),
  )

  const matched =
    values.some(
      (value) =>
        hashToken(value) ===
        row.verification_token_hash,
    )

  await runWithTenantRlsScope(
    [input.organizationId],
    undefined,
    async () => {
      await getPostgresPool().query(
        `
          UPDATE sf_organization_domains
          SET
            last_checked_at = now(),
            updated_at = now()
          WHERE organization_id = $1
            AND domain = $2
        `,
        [
          input.organizationId,
          domain,
        ],
      )
    },
    "tenant-session",
  )

  if (!matched) {
    throw new Error(
      `Registro TXT ainda nÃ£o encontrado em ${recordName}.`,
    )
  }

  // Garante que o hostname tambÃ©m existe no Cloudflare for SaaS antes de
  // ativar o vÃ­nculo local da empresa.
  const cloudflare = await ensureCloudflareCustomHostname(domain)

  await runWithTenantRlsScope(
    [input.organizationId],
    undefined,
    async () => {
      const client =
        await getPostgresPool().connect()

      try {
        await client.query("BEGIN")

        const hasPrimary =
          await client.query(
            `
              SELECT 1
              FROM sf_organization_domains
              WHERE organization_id = $1
                AND verified = true
                AND primary_domain = true
                AND domain <> $2
              LIMIT 1
            `,
            [
              input.organizationId,
              domain,
            ],
          )

        await client.query(
          `
            UPDATE sf_organization_domains
            SET
              verified = true,
              verified_at = now(),
              last_checked_at = now(),
              primary_domain = $3,
              updated_at = now()
            WHERE organization_id = $1
              AND domain = $2
          `,
          [
            input.organizationId,
            domain,
            !hasPrimary.rowCount,
          ],
        )

        await client.query("COMMIT")
      } catch (error) {
        await client.query("ROLLBACK")
        throw error
      } finally {
        client.release()
      }
    },
    "tenant-session",
  )

  return {
    verified: true,
    domain,
    routing: cloudflareDnsInstructions(cloudflare),
  }
}

export async function removeOrganizationDomain(
  input: {
    organizationId: string
    domain: string
  },
) {
  const domain =
    normalizePublicDomain(
      input.domain,
    )

  if (isReservedHost(domain)) {
    throw new Error(
      "O domÃ­nio padrÃ£o da plataforma nÃ£o pode ser removido por esta tela.",
    )
  }

  // Confirma a posse local antes de remover o hostname no Cloudflare.
  // Isso impede que uma organizaÃ§Ã£o tente remover o domÃ­nio de outra.
  const owned = await runWithTenantRlsScope(
    [input.organizationId],
    undefined,
    async () => {
      const result = await getPostgresPool().query<{ domain: string }>(
        `
          SELECT domain
          FROM sf_organization_domains
          WHERE organization_id = $1
            AND domain = $2
          LIMIT 1
        `,
        [
          input.organizationId,
          domain,
        ],
      )

      return Boolean(result.rows[0])
    },
    "tenant-session",
  )

  if (!owned) {
    throw new Error(
      "DomÃ­nio nÃ£o encontrado.",
    )
  }

  await removeCloudflareCustomHostname(domain)

  await runWithTenantRlsScope(
    [input.organizationId],
    undefined,
    async () => {
      const client =
        await getPostgresPool().connect()

      try {
        await client.query("BEGIN")

        const deleted =
          await client.query<{
            primary_domain: boolean
          }>(
            `
              DELETE FROM sf_organization_domains
              WHERE organization_id = $1
                AND domain = $2
              RETURNING primary_domain
            `,
            [
              input.organizationId,
              domain,
            ],
          )

        if (!deleted.rows[0]) {
          throw new Error(
            "DomÃ­nio nÃ£o encontrado.",
          )
        }

        if (
          deleted.rows[0]
            .primary_domain
        ) {
          await client.query(
            `
              UPDATE sf_organization_domains
              SET
                primary_domain = true,
                updated_at = now()
              WHERE domain = (
                SELECT domain
                FROM sf_organization_domains
                WHERE organization_id = $1
                  AND verified = true
                ORDER BY verified_at ASC, domain ASC
                LIMIT 1
              )
            `,
            [input.organizationId],
          )
        }

        await client.query("COMMIT")
      } catch (error) {
        await client.query("ROLLBACK")
        throw error
      } finally {
        client.release()
      }
    },
    "tenant-session",
  )

  return true
}



export type PrintAgentSummary = {
  id: string
  name: string
  active: boolean
  lastSeenAt: string | null
  createdAt: string
}

function printTokenHash(
  token: string,
) {
  return hashToken(token)
}

export async function listPrintAgents(
  organizationId: string,
) {
  const result =
    await getPostgresPool().query<{
      id: string
      name: string
      active: boolean
      last_seen_at: Date | string | null
      created_at: Date | string
    }>(
      `
        SELECT
          id,
          name,
          active,
          last_seen_at,
          created_at
        FROM sf_print_agents
        WHERE organization_id = $1
        ORDER BY created_at DESC
      `,
      [organizationId],
    )

  return result.rows.map(
    (row): PrintAgentSummary => ({
      id: row.id,
      name: row.name,
      active:
        Boolean(row.active),
      lastSeenAt:
        row.last_seen_at
          ? new Date(
              row.last_seen_at,
            ).toISOString()
          : null,
      createdAt:
        new Date(
          row.created_at,
        ).toISOString(),
    }),
  )
}

export async function createPrintAgent(
  input: {
    organizationId: string
    name: string
    createdByUserId: string
  },
) {
  const name =
    input.name.trim()

  if (!name) {
    throw new Error(
      "Informe um nome para o agente de impressÃ£o.",
    )
  }

  const token =
    `sfpa_${randomBytes(
      32,
    ).toString("base64url")}`

  const id = randomUUID()

  await getPostgresPool().query(
    `
      INSERT INTO sf_print_agents (
        id,
        organization_id,
        name,
        token_hash,
        active,
        created_by_user_id,
        created_at
      )
      VALUES (
        $1,
        $2,
        $3,
        $4,
        true,
        $5,
        now()
      )
    `,
    [
      id,
      input.organizationId,
      name,
      printTokenHash(token),
      input.createdByUserId,
    ],
  )

  return {
    id,
    name,
    token,
  }
}

export async function revokePrintAgent(
  organizationId: string,
  id: string,
) {
  const result =
    await getPostgresPool().query(
      `
        UPDATE sf_print_agents
        SET
          active = false,
          revoked_at = now()
        WHERE organization_id = $1
          AND id = $2
          AND active = true
        RETURNING id
      `,
      [organizationId, id],
    )

  return Boolean(result.rowCount)
}

export async function authenticatePrintAgent(
  token: string,
) {
  if (!token) return null

  const result =
    await runWithRlsBypass(() =>
      getPostgresPool().query<{
        id: string
        organization_id: string
        name: string
        organization_name: string
        organization_slug: string
      }>(
        `
          SELECT
            a.id,
            a.organization_id,
            a.name,
            o.trade_name AS organization_name,
            o.slug AS organization_slug
          FROM sf_print_agents a
          INNER JOIN sf_organizations o
            ON o.id = a.organization_id
           AND o.status IN ('active', 'trial')
          WHERE a.token_hash = $1
            AND a.active = true
          LIMIT 1
        `,
        [printTokenHash(token)],
      ),
    )

  const row = result.rows[0]
  if (!row) return null

  enterTenantRlsContext(
    row.organization_id,
    undefined,
    "privileged-backend",
  )

  await getPostgresPool().query(
    `
      UPDATE sf_print_agents
      SET last_seen_at = now()
      WHERE id = $1
    `,
    [row.id],
  )

  return {
    agentId: row.id,
    agentName: row.name,
    organizationId:
      row.organization_id,
    organizationName:
      row.organization_name,
    organizationSlug:
      row.organization_slug,
  }
}


export async function getRlsRolloutStats() {
  const result =
    await getPostgresPool().query<{
      table_name: string
      prepared: boolean
      enforcement:
        | "prepared"
        | "enabled"
      row_security_enabled: boolean
    }>(
      `
        SELECT
          r.table_name,
          r.prepared,
          r.enforcement,
          COALESCE(c.relrowsecurity, false) AS row_security_enabled
        FROM sf_rls_rollout r
        LEFT JOIN pg_class c
          ON c.oid = to_regclass(
            'public.' || r.table_name
          )
        ORDER BY r.table_name ASC
      `,
    )

  const tables = result.rows.map(
    (row) => ({
      table: row.table_name,
      prepared:
        Boolean(row.prepared),
      enforcement:
        row.enforcement,
      rowSecurityEnabled:
        Boolean(
          row.row_security_enabled,
        ),
    }),
  )

  return {
    prepared:
      tables.length > 0 &&
      tables.every(
        (row) => row.prepared,
      ),
    preparedCount:
      tables.filter(
        (row) => row.prepared,
      ).length,
    enabledCount:
      tables.filter(
        (row) =>
          row.rowSecurityEnabled,
      ).length,
    tables,
  }
}

==========================================================================================
ARQUIVO: lib\security\admin-sessions.ts
==========================================================================================
import {
  createHash,
  randomUUID,
} from "node:crypto"
import {
  ADMIN_SESSION_IDLE_SECONDS,
} from "@/lib/auth"
import { getPostgresPool } from "@/lib/postgres"
import {
  requestIp,
} from "@/lib/security/request-security"
import type {
  TwoFactorAuthSource,
} from "@/lib/security/two-factor-challenge"

export type AdminSessionAuthSource =
  | TwoFactorAuthSource
  | "commercial"
  | "demo"

const MAX_ACTIVE_SESSIONS_PER_USER = 20

type ActiveSessionRow = {
  id: string
  organization_id: string
  organization_name: string
  auth_source: AdminSessionAuthSource
  superadmin_authorized: boolean
  device_label: string
  created_at: Date | string
  last_seen_at: Date | string
  expires_at: Date | string
}

export type AdminSessionSummary = {
  id: string
  organizationId: string
  organizationName: string
  authSource: AdminSessionAuthSource
  superadminAuthorized: boolean
  deviceLabel: string
  createdAt: string
  lastSeenAt: string
  expiresAt: string
  current: boolean
}

function hashSignal(
  namespace: string,
  value: string,
) {
  if (!value.trim()) {
    return null
  }

  return createHash("sha256")
    .update(
      `saborflow:admin-session:${namespace}:v1:${value}`,
    )
    .digest("hex")
}

function browserName(
  userAgent: string,
) {
  if (/Edg\//i.test(userAgent)) return "Microsoft Edge"
  if (/OPR\//i.test(userAgent)) return "Opera"
  if (/Firefox\//i.test(userAgent)) return "Firefox"
  if (/Chrome\//i.test(userAgent) || /CriOS\//i.test(userAgent)) {
    return "Google Chrome"
  }
  if (/Safari\//i.test(userAgent) && !/Chrome\//i.test(userAgent)) {
    return "Safari"
  }
  return "Navegador"
}

function operatingSystemName(
  userAgent: string,
) {
  if (/Windows/i.test(userAgent)) return "Windows"
  if (/iPhone/i.test(userAgent)) return "iPhone"
  if (/iPad/i.test(userAgent)) return "iPad"
  if (/Android/i.test(userAgent)) return "Android"
  if (/Mac OS X|Macintosh/i.test(userAgent)) return "macOS"
  if (/Linux/i.test(userAgent)) return "Linux"
  return "dispositivo desconhecido"
}

function deviceLabel(
  userAgent: string,
) {
  if (!userAgent.trim()) {
    return "Dispositivo nÃƒÂ£o identificado"
  }

  return `${browserName(userAgent)} no ${operatingSystemName(userAgent)}`
}

function iso(
  value: Date | string,
) {
  return new Date(value).toISOString()
}

export async function createAdminSessionRecord(
  input: {
    userId: string
    organizationId: string
    sessionVersion: number
    authSource: AdminSessionAuthSource
    superadminAuthorized: boolean
    request: Request
    maxAgeSeconds?: number
  },
) {
  const id = randomUUID()
  const maxAgeSeconds =
    Math.max(
      60,
      Math.min(
        ADMIN_SESSION_IDLE_SECONDS,
        Math.floor(
          input.maxAgeSeconds ??
            ADMIN_SESSION_IDLE_SECONDS,
        ),
      ),
    )
  const userAgent =
    input.request.headers.get(
      "user-agent",
    ) || ""
  const ip =
    requestIp(input.request)

  const client =
    await getPostgresPool().connect()

  try {
    await client.query("BEGIN")

    await client.query(
      `
        INSERT INTO sf_admin_sessions (
          id,
          user_id,
          organization_id,
          session_version,
          auth_source,
          superadmin_authorized,
          device_label,
          user_agent_hash,
          ip_hash,
          created_at,
          last_seen_at,
          expires_at
        )
        VALUES (
          $1,
          $2,
          $3,
          $4,
          $5,
          $6,
          $7,
          $8,
          $9,
          now(),
          now(),
          now() +
            ($10::integer *
              interval '1 second')
        )
      `,
      [
        id,
        input.userId,
        input.organizationId,
        input.sessionVersion,
        input.authSource,
        input.superadminAuthorized,
        deviceLabel(userAgent),
        hashSignal(
          "ua",
          userAgent,
        ),
        hashSignal(
          "ip",
          ip,
        ),
        maxAgeSeconds,
      ],
    )

    await client.query(
      `
        UPDATE sf_admin_sessions
        SET
          revoked_at = now(),
          revoked_reason =
            'active_session_limit'
        WHERE user_id = $1
          AND revoked_at IS NULL
          AND id IN (
            SELECT id
            FROM sf_admin_sessions
            WHERE user_id = $1
              AND revoked_at IS NULL
            ORDER BY
              last_seen_at DESC,
              created_at DESC
            OFFSET $2
          )
      `,
      [
        input.userId,
        MAX_ACTIVE_SESSIONS_PER_USER,
      ],
    )

    await client.query("COMMIT")
  } catch (error) {
    await client.query("ROLLBACK")
    throw error
  } finally {
    client.release()
  }

  return {
    id,
  }
}

export async function validateAndTouchAdminSession(
  input: {
    sessionId: string
    userId: string
    organizationId: string
    sessionVersion: number
  },
) {
  const result =
    await getPostgresPool()
      .query<{ id: string }>(
        `
          UPDATE sf_admin_sessions AS s
          SET
            last_seen_at = now(),
            expires_at =
              now() +
              ($5::integer *
                interval '1 second')
          FROM sf_users AS u
          WHERE s.id = $1
            AND s.user_id = $2
            AND s.organization_id = $3
            AND s.session_version = $4
            AND s.revoked_at IS NULL
            AND s.expires_at > now()
            AND u.id = s.user_id
            AND u.status = 'active'
            AND u.session_version =
              s.session_version
          RETURNING s.id
        `,
        [
          input.sessionId,
          input.userId,
          input.organizationId,
          input.sessionVersion,
          ADMIN_SESSION_IDLE_SECONDS,
        ],
      )

  return Boolean(
    result.rowCount,
  )
}

export async function listAdminSessions(
  userId: string,
  currentSessionId: string,
) {
  const result =
    await getPostgresPool()
      .query<ActiveSessionRow>(
        `
          SELECT
            s.id,
            s.organization_id,
            o.trade_name AS organization_name,
            s.auth_source,
            s.superadmin_authorized,
            s.device_label,
            s.created_at,
            s.last_seen_at,
            s.expires_at
          FROM sf_admin_sessions AS s
          INNER JOIN sf_users AS u
            ON u.id = s.user_id
          INNER JOIN sf_organizations AS o
            ON o.id = s.organization_id
          WHERE s.user_id = $1
            AND s.revoked_at IS NULL
            AND s.expires_at > now()
            AND s.session_version =
              u.session_version
            AND u.status = 'active'
          ORDER BY
            (s.id = $2) DESC,
            s.last_seen_at DESC,
            s.created_at DESC
        `,
        [
          userId,
          currentSessionId,
        ],
      )

  return result.rows.map(
    (
      row,
    ): AdminSessionSummary => ({
      id: row.id,
      organizationId:
        row.organization_id,
      organizationName:
        row.organization_name,
      authSource:
        row.auth_source,
      superadminAuthorized:
        Boolean(
          row.superadmin_authorized,
        ),
      deviceLabel:
        row.device_label,
      createdAt:
        iso(row.created_at),
      lastSeenAt:
        iso(row.last_seen_at),
      expiresAt:
        iso(row.expires_at),
      current:
        row.id ===
        currentSessionId,
    }),
  )
}


export async function moveAdminSessionToOrganization(
  input: {
    sessionId: string
    userId: string
    organizationId: string
    sessionVersion: number
  },
) {
  const result =
    await getPostgresPool()
      .query<{ id: string }>(
        `
          UPDATE sf_admin_sessions AS s
          SET
            organization_id = $3,
            last_seen_at = now(),
            expires_at =
              now() +
              ($5::integer *
                interval '1 second')
          FROM sf_users AS u
          WHERE s.id = $1
            AND s.user_id = $2
            AND s.session_version = $4
            AND s.revoked_at IS NULL
            AND s.expires_at > now()
            AND u.id = s.user_id
            AND u.status = 'active'
            AND u.session_version =
              s.session_version
          RETURNING s.id
        `,
        [
          input.sessionId,
          input.userId,
          input.organizationId,
          input.sessionVersion,
          ADMIN_SESSION_IDLE_SECONDS,
        ],
      )

  return Boolean(
    result.rowCount,
  )
}

export async function revokeAdminSession(
  input: {
    sessionId: string
    userId: string
    reason:
      | "logout"
      | "user_revoked"
      | "revoke_others"
      | "security_reset"
  },
) {
  const result =
    await getPostgresPool()
      .query<{ id: string }>(
        `
          UPDATE sf_admin_sessions
          SET
            revoked_at =
              COALESCE(
                revoked_at,
                now()
              ),
            revoked_reason =
              COALESCE(
                revoked_reason,
                $3
              )
          WHERE id = $1
            AND user_id = $2
          RETURNING id
        `,
        [
          input.sessionId,
          input.userId,
          input.reason,
        ],
      )

  return Boolean(
    result.rowCount,
  )
}

export async function revokeOtherAdminSessions(
  userId: string,
  currentSessionId: string,
) {
  const result =
    await getPostgresPool()
      .query(
        `
          UPDATE sf_admin_sessions
          SET
            revoked_at = now(),
            revoked_reason =
              'revoke_others'
          WHERE user_id = $1
            AND id <> $2
            AND revoked_at IS NULL
        `,
        [
          userId,
          currentSessionId,
        ],
      )

  return result.rowCount || 0
}

export async function revokeAllAdminSessionsForUser(
  userId: string,
) {
  const result =
    await getPostgresPool()
      .query(
        `
          UPDATE sf_admin_sessions
          SET
            revoked_at = now(),
            revoked_reason =
              'security_reset'
          WHERE user_id = $1
            AND revoked_at IS NULL
        `,
        [userId],
      )

  return result.rowCount || 0
}

==========================================================================================
ARQUIVO: app\api\admin\organization-security\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  canManageSecurity,
} from "@/lib/admin-access"
import {
  getOrganizationTimeZone,
  listOrganizationDomains,
  listPrintAgents,
  updateOrganizationTimeZone,
} from "@/lib/organization-security-db"

export const dynamic = "force-dynamic"

export async function GET() {
  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        error:
          "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  const timeZone =
    await getOrganizationTimeZone(
      session.organizationId,
    )

  if (
    !canManageSecurity(
      session.role,
      session.operationalPermissions,
    )
  ) {
    return NextResponse.json({
      organization: {
        id:
          session.organizationId,
        name:
          session.organizationName,
        slug:
          session.organizationSlug,
      },
      role: session.role,
      timeZone,
      domains: [],
      printAgents: [],
      canManageSecurity: false,
    })
  }

  const [
    domains,
    printAgents,
  ] = await Promise.all([
    listOrganizationDomains(
      session.organizationId,
    ),
    listPrintAgents(
      session.organizationId,
    ),
  ])

  return NextResponse.json({
    organization: {
      id:
        session.organizationId,
      name:
        session.organizationName,
      slug:
        session.organizationSlug,
    },
    role: session.role,
    timeZone,
    domains,
    printAgents,
    canManageSecurity: true,
  })
}

export async function PATCH(
  request: Request,
) {
  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        error:
          "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  if (
    !canManageSecurity(
      session.role,
      session.operationalPermissions,
    )
  ) {
    return NextResponse.json(
      {
        error:
          "Seu perfil nÃ£o pode alterar a configuraÃ§Ã£o da organizaÃ§Ã£o.",
      },
      { status: 403 },
    )
  }

  const body = (await request
    .json()
    .catch(() => null)) as
    | { timeZone?: string }
    | null

  if (!body?.timeZone) {
    return NextResponse.json(
      {
        error:
          "Informe o timezone.",
      },
      { status: 400 },
    )
  }

  try {
    const timeZone =
      await updateOrganizationTimeZone(
        session.organizationId,
        body.timeZone,
      )

    return NextResponse.json({
      ok: true,
      timeZone,
    })
  } catch (error) {
    return NextResponse.json(
      {
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel atualizar.",
      },
      { status: 400 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\organizations\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  ADMIN_SESSION_COOKIE,
  ADMIN_SESSION_IDLE_SECONDS,
  createSessionToken,
} from "@/lib/auth"
import {
  BILLING_SESSION_COOKIE,
  clearCommercialBillingCookieOptions,
  getCommercialBillingSession,
} from "@/lib/billing-commercial-session"
import {
  billingErrorStatus,
  getBillingSnapshotForUser,
} from "@/lib/billing-db"
import { createOrganizationForUser } from "@/lib/organization-onboarding"
import { listOrganizationMembershipsForUserId } from "@/lib/tenant-context"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import {
  createAdminSessionRecord,
  moveAdminSessionToOrganization,
} from "@/lib/security/admin-sessions"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()
  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  const organizations = await listOrganizationMembershipsForUserId(session.userId)
  return NextResponse.json({
    activeOrganizationId: session.organizationId,
    organizations,
  })
}

export async function POST(request: Request) {
  const tenantSession = await getVerifiedTenantSession().catch(() => null)
  const commercialSession = tenantSession ? null : await getCommercialBillingSession()

  let userId = ""
  let email = ""
  let shouldClearCommercialSession = false

  if (tenantSession) {
    if (tenantSession.role !== "owner") {
      return NextResponse.json({
        error: "Somente o proprietÃ¡rio da conta contratante pode adicionar outra loja.",
      }, { status: 403 })
    }
    userId = tenantSession.userId
    email = tenantSession.email
  } else if (commercialSession) {
    const billing = await getBillingSnapshotForUser(commercialSession.userId)
    if (
      billing.account?.id !== commercialSession.billingAccountId ||
      billing.subscription?.status !== "active" ||
      !billing.capacity.canCreateOrganization
    ) {
      return NextResponse.json({
        error: "O pagamento precisa estar confirmado e o plano deve possuir vaga para uma loja.",
      }, { status: 402 })
    }
    userId = commercialSession.userId
    email = commercialSession.email
    shouldClearCommercialSession = true
  } else {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  const body = await request.json().catch(() => null) as
    | {
        personType?: "PF" | "PJ"
        document?: string
        tradeName?: string
        legalName?: string
        industry?: string
        phone?: string
        email?: string
        city?: string
        state?: string
      }
    | null

  if (!body?.document || !body.tradeName) {
    return NextResponse.json({ error: "Documento e nome da empresa sÃ£o obrigatÃ³rios." }, { status: 400 })
  }

  try {
    const context = await createOrganizationForUser(userId, email, {
      personType: body.personType === "PF" ? "PF" : "PJ",
      document: body.document,
      tradeName: body.tradeName,
      legalName: body.legalName,
      industry: body.industry,
      phone: body.phone,
      email: body.email,
      city: body.city,
      state: body.state,
    })

    let sessionId = ""

    if (tenantSession) {
      const moved = await moveAdminSessionToOrganization({
        sessionId: tenantSession.sessionId,
        userId: tenantSession.userId,
        organizationId: context.organizationId,
        sessionVersion: context.sessionVersion,
      })

      if (!moved) {
        return NextResponse.json({
          error: "Sua sessÃ£o expirou. Entre novamente.",
        }, { status: 401 })
      }

      sessionId = tenantSession.sessionId
    } else {
      const createdSession = await createAdminSessionRecord({
        userId,
        organizationId: context.organizationId,
        sessionVersion: context.sessionVersion,
        authSource: "commercial",
        superadminAuthorized: false,
        request,
      })
      sessionId = createdSession.id
    }

    const response = NextResponse.json({
      ok: true,
      organization: {
        id: context.organizationId,
        name: context.organizationName,
        slug: context.organizationSlug,
        role: context.role,
        publicOrderingEnabled: false,
      },
      switched: true,
      onboardingRequired: true,
      onboardingUrl: "/onboarding",
    }, { status: 201 })

    response.cookies.set(
      ADMIN_SESSION_COOKIE,
      createSessionToken(context, sessionId),
      {
        httpOnly: true,
        sameSite: "lax",
        secure: process.env.NODE_ENV === "production",
        path: "/",
        maxAge: ADMIN_SESSION_IDLE_SECONDS,
        priority: "high",
      },
    )

    if (shouldClearCommercialSession) {
      response.cookies.set(BILLING_SESSION_COOKIE, "", clearCommercialBillingCookieOptions)
    }

    return response
  } catch (error) {
    return NextResponse.json({
      error: error instanceof Error ? error.message : "NÃ£o foi possÃ­vel criar a empresa.",
    }, { status: billingErrorStatus(error) })
  }
}

==========================================================================================
ARQUIVO: app\api\admin\switch-organization\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  ADMIN_SESSION_COOKIE,
  ADMIN_SESSION_IDLE_SECONDS,
  createSessionToken,
} from "@/lib/auth"
import {
  getOrganizationContextForUser,
} from "@/lib/tenant-context"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  moveAdminSessionToOrganization,
} from "@/lib/security/admin-sessions"

export async function POST(
  request: Request,
) {
  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        error:
          "NÃ£o autorizado.",
      },
      { status: 401 },
    )
  }

  const body = (await request
    .json()
    .catch(() => null)) as
    | {
        organizationId?: string
      }
    | null

  if (!body?.organizationId) {
    return NextResponse.json(
      {
        error:
          "Empresa invÃ¡lida.",
      },
      { status: 400 },
    )
  }

  const context =
    await getOrganizationContextForUser(
      session.userId,
      body.organizationId,
    )

  if (!context) {
    return NextResponse.json(
      {
        error:
          "VocÃª nÃ£o possui acesso a esta empresa.",
      },
      { status: 403 },
    )
  }

  const moved =
    await moveAdminSessionToOrganization({
      sessionId:
        session.sessionId,
      userId:
        session.userId,
      organizationId:
        context.organizationId,
      sessionVersion:
        context.sessionVersion,
    })

  if (!moved) {
    return NextResponse.json(
      {
        error:
          "Sua sessÃ£o expirou. Entre novamente.",
      },
      { status: 401 },
    )
  }

  const response =
    NextResponse.json({
      ok: true,
      organization: {
        id: context.organizationId,
        name:
          context.organizationName,
        slug:
          context.organizationSlug,
        role: context.role,
      },
    })

  response.cookies.set(
    ADMIN_SESSION_COOKIE,
    createSessionToken(
      context,
      session.sessionId,
    ),
    {
      httpOnly: true,
      sameSite: "lax",
      secure:
        process.env.NODE_ENV ===
        "production",
      path: "/",
      maxAge:
        ADMIN_SESSION_IDLE_SECONDS,
      priority: "high",
    },
  )

  return response
}

==========================================================================================
ARQUIVOS COM DEFINICAO EXPLICITA DE ROLES
==========================================================================================

==========================================================================================
ARQUIVO: app\admin\page.tsx
==========================================================================================
import { redirect } from "next/navigation"
import { hasCurrentLegalAcceptance } from "@/lib/legal-db"
import { AdminDashboard } from "@/components/admin/admin-dashboard"
import { getAdminEmail, getAdminSession } from "@/lib/auth"
import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
import { getOperationalAccessForSession } from "@/lib/operational-rbac"
import { getDefaultOperationalPath } from "@/lib/operational-home"
import { getCommercialOnboardingSnapshot } from "@/lib/commercial-onboarding"
import { demoOrganizationIsUsable, getDemoEnvironmentForOrganization } from "@/lib/demo-policy"

export const dynamic = "force-dynamic"

export default async function AdminPage() {
  const session = await getAdminSession()
  if (!session) redirect("/login")

  let demoEnvironment = null
  if (session.mode === "tenant") {
    if (!(await demoOrganizationIsUsable(session.organizationId))) {
      redirect("/demo?expired=1")
    }
    demoEnvironment = await getDemoEnvironmentForOrganization(session.organizationId)
  }

  if (session.mode === "tenant" && !demoEnvironment && !(await hasCurrentLegalAcceptance(session.userId, session.organizationId))) {
    redirect("/legal/aceite")
  }

  if (session.mode === "tenant" && session.role === "owner") {
    const onboarding = await getCommercialOnboardingSnapshot(session.organizationId)
    if (onboarding && !onboarding.state.completed) redirect("/onboarding")
  }

  const access = session.mode === "tenant"
    ? await getOperationalAccessForSession(session)
    : null

  if (session.mode === "tenant" && access) {
    const operationalHome = getDefaultOperationalPath(
      session.role,
      access.permissions,
    )
    if (operationalHome !== "/admin") redirect(operationalHome)
  }

  return (
    <AdminDashboard
      adminEmail={session.email || getAdminEmail()}
      adminRole={session.mode === "tenant" ? session.role : "owner"}
      operationalPermissions={access?.permissions || []}
      demoEnvironment={demoEnvironment ? {
        kind: demoEnvironment.kind,
        expiresAt: demoEnvironment.expiresAt,
      } : null}
      organizationSlug={session.mode === "tenant" ? session.organizationSlug : null}
      initialData={await getTenantAwareAdminData(session, access?.permissions)}
    />
  )
}

==========================================================================================
ARQUIVO: app\api\admin\catalog-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getTenantCatalogStats,
  isCurrentDeploymentOrganization,
} from "@/lib/catalog-db"
import {
  getCategories as getLegacyCategories,
  getProducts as getLegacyProducts,
} from "@/lib/db"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { ok: false, error: "SessÃ£o multiempresa invÃ¡lida." },
      { status: 401 },
    )
  }

  try {
    const stats = await getTenantCatalogStats(session.organizationId)
    const mirrorEnabled = await isCurrentDeploymentOrganization(
      session.organizationId,
    )

    let legacy: { categories: number; products: number } | null = null

    if (mirrorEnabled) {
      const [categories, products] = await Promise.all([
        getLegacyCategories({ includeInactive: true }),
        getLegacyProducts({ includeInactive: true }),
      ])

      legacy = {
        categories: categories.length,
        products: products.length,
      }
    }

    return NextResponse.json({
      ok: stats.ready,
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      role: session.role,
      catalog: stats,
      transition: {
        legacyMirrorEnabled: mirrorEnabled,
        legacy,
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel consultar o catÃ¡logo.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\corporate-group\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  addCorporateMember,
  addCorporateUnit,
  createCorporateGroup,
  getCorporateOverview,
  removeCorporateUnit,
  renameCorporateGroup,
  setCorporateHeadquarters,
  setCorporateMemberStatus,
  type CorporateGroupRole,
} from "@/lib/corporate-db"
import {
  corporateRequestIp,
  corporateRequestIsSameOrigin,
} from "@/lib/corporate-request"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()
  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  try {
    return NextResponse.json(await getCorporateOverview(session))
  } catch (error) {
    return NextResponse.json(
      {
        error: error instanceof Error ? error.message : "NÃ£o foi possÃ­vel carregar a gestÃ£o corporativa.",
      },
      { status: 500 },
    )
  }
}

type ActionBody =
  | {
      action: "createGroup"
      name?: string
      headquartersOrganizationId?: string
      organizationIds?: string[]
    }
  | { action: "renameGroup"; groupId?: string; name?: string }
  | {
      action: "addUnit"
      groupId?: string
      organizationId?: string
      unitCode?: string
      costCenter?: string
    }
  | { action: "setHeadquarters"; groupId?: string; organizationId?: string }
  | { action: "removeUnit"; groupId?: string; organizationId?: string }
  | {
      action: "addMember"
      groupId?: string
      email?: string
      role?: CorporateGroupRole
    }
  | {
      action: "setMemberStatus"
      groupId?: string
      memberId?: string
      status?: "active" | "disabled"
    }

export async function POST(request: Request) {
  const session = await getVerifiedTenantSession()
  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  if (!corporateRequestIsSameOrigin(request)) {
    return NextResponse.json({ error: "Origem da requisiÃ§Ã£o nÃ£o permitida." }, { status: 403 })
  }

  const body = (await request.json().catch(() => null)) as ActionBody | null
  if (!body?.action) {
    return NextResponse.json({ error: "AÃ§Ã£o invÃ¡lida." }, { status: 400 })
  }

  const ip = corporateRequestIp(request)

  try {
    if (body.action === "createGroup") {
      await createCorporateGroup(
        session,
        {
          name: body.name || "",
          headquartersOrganizationId: body.headquartersOrganizationId || "",
          organizationIds: Array.isArray(body.organizationIds) ? body.organizationIds : [],
        },
        ip,
      )
    } else if (body.action === "renameGroup") {
      if (!body.groupId) throw new Error("Grupo invÃ¡lido.")
      await renameCorporateGroup(session, body.groupId, body.name || "", ip)
    } else if (body.action === "addUnit") {
      if (!body.groupId || !body.organizationId) throw new Error("Unidade invÃ¡lida.")
      await addCorporateUnit(
        session,
        body.groupId,
        {
          organizationId: body.organizationId,
          unitCode: body.unitCode,
          costCenter: body.costCenter,
        },
        ip,
      )
    } else if (body.action === "setHeadquarters") {
      if (!body.groupId || !body.organizationId) throw new Error("Unidade invÃ¡lida.")
      await setCorporateHeadquarters(session, body.groupId, body.organizationId, ip)
    } else if (body.action === "removeUnit") {
      if (!body.groupId || !body.organizationId) throw new Error("Unidade invÃ¡lida.")
      await removeCorporateUnit(session, body.groupId, body.organizationId, ip)
    } else if (body.action === "addMember") {
      if (!body.groupId || !body.email || !body.role) throw new Error("Membro invÃ¡lido.")
      await addCorporateMember(session, body.groupId, { email: body.email, role: body.role }, ip)
    } else if (body.action === "setMemberStatus") {
      if (!body.groupId || !body.memberId || !body.status) throw new Error("Membro invÃ¡lido.")
      await setCorporateMemberStatus(session, body.groupId, body.memberId, body.status, ip)
    }

    return NextResponse.json({ ok: true, data: await getCorporateOverview(session) })
  } catch (error) {
    return NextResponse.json(
      { error: error instanceof Error ? error.message : "NÃ£o foi possÃ­vel concluir a aÃ§Ã£o corporativa." },
      { status: 400 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\customers-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getCustomerAccounts,
  getCustomers as getLegacyCustomers,
} from "@/lib/db"
import {
  getTenantCustomersStats,
} from "@/lib/customer-db"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  isCurrentDeploymentOrganization,
} from "@/lib/catalog-db"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { ok: false, error: "SessÃ£o multiempresa invÃ¡lida." },
      { status: 401 },
    )
  }

  try {
    const stats = await getTenantCustomersStats(
      session.organizationId,
    )

    const mirrorEnabled =
      await isCurrentDeploymentOrganization(
        session.organizationId,
      )

    let legacy:
      | {
          accounts: number
          crmCustomers: number
        }
      | null = null

    if (mirrorEnabled) {
      const [accounts, customers] = await Promise.all([
        getCustomerAccounts({ includeInactive: true }),
        getLegacyCustomers(),
      ])

      legacy = {
        accounts: accounts.length,
        crmCustomers: customers.length,
      }
    }

    const countsMatch =
      !legacy ||
      (legacy.accounts === stats.accounts &&
        legacy.crmCustomers === stats.crmCustomers)

    return NextResponse.json({
      ok: stats.ready && countsMatch,
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      role: session.role,
      customers: stats,
      transition: {
        legacyMirrorEnabled: mirrorEnabled,
        legacy,
        countsMatch,
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel consultar os clientes.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\delivery-dispatch-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { getDeliveryDispatchHealth } from "@/lib/delivery-dispatch-db"
import { permissionListHas } from "@/lib/operational-permissions"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: 401 },
    )
  }

  const mayInspect =
    session.role === "courier" ||
    permissionListHas(session.operationalPermissions, "delivery.manage")

  if (!mayInspect) {
    return NextResponse.json(
      { error: "Seu perfil nÃ£o pode acessar a expediÃ§Ã£o." },
      { status: 403 },
    )
  }

  try {
    const health = await getDeliveryDispatchHealth({
      organizationId: session.organizationId,
      userId: session.userId,
      role: session.role,
    })

    return NextResponse.json({
      ok: health.schemaReady,
      phase: "25.7.2-courier-rls-session-scope",
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      schemaReady: health.schemaReady,
      counts: health.counts,
      currentCourier: health.currentCourier
        ? {
            id: health.currentCourier.id,
            name: health.currentCourier.name,
            staffMemberId: health.currentCourier.staffMemberId ?? null,
            loginLinked: Boolean(health.currentCourier.linkedUserId),
          }
        : null,
      capabilities: {
        courierStaffIdentityLink: true,
        managerCourierAssignment: true,
        courierReceivesAssignedOrdersOnly: true,
        backendAssignmentOwnershipCheck: true,
        dedicatedCourierWorkspace: true,
        courierRouteNavigation: true,
        realtimeCustomerTracking: true,
        postgresRlsPreserved: true,
        courierLookupUsesExplicitTenantRlsScope: true,
        courierPageUsesExplicitTenantRlsScope: true,
      },
      boundaries: {
        unlinkedCourierCanRemainOperationalWithoutAppLogin: true,
        courierAppRequiresLinkedStaffLogin: true,
        courierCannotOperateAnotherCourierOrder: true,
        courierRouteNavigationActive: true,
        realtimeCustomerTrackingActive: true,
      },
    }, { status: health.schemaReady ? 200 : 503 })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        phase: "25.7.2-courier-rls-session-scope",
        error: error instanceof Error
          ? error.message
          : "Falha ao validar expediÃ§Ã£o.",
      },
      { status: 500 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\delivery-tracking-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { getDeliveryTrackingHealth } from "@/lib/delivery-tracking-db"
import { getTenantSettings } from "@/lib/organization-db"
import { permissionListHas } from "@/lib/operational-permissions"
import { runWithTenantRlsScope } from "@/lib/rls-context"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  const mayInspect =
    session.role === "courier" ||
    permissionListHas(session.operationalPermissions, "delivery.manage")

  if (!mayInspect) {
    return NextResponse.json(
      { error: "Seu perfil nÃ£o pode acessar a operaÃ§Ã£o de entrega." },
      { status: 403 },
    )
  }

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () => {
      try {
        const [health, settings] = await Promise.all([
          getDeliveryTrackingHealth(session.organizationId),
          getTenantSettings(session.organizationId),
        ])

        return NextResponse.json(
          {
            ok: health.schemaReady,
            phase: "25.4-25.5-delivery-routing-live-tracking",
            organization: {
              id: session.organizationId,
              name: session.organizationName,
              slug: session.organizationSlug,
            },
            schemaReady: health.schemaReady,
            settings: {
              trackingEnabled: settings?.deliveryTrackingEnabled !== false,
            },
            counts: {
              activeRoutes: health.activeRoutes,
              freshLocations: health.freshLocations,
              staleLocations: health.staleLocations,
            },
            publicUrls: {
              appBaseUrlConfigured: Boolean(process.env.APP_BASE_URL?.trim()),
              railwayPublicDomainConfigured: Boolean(
                process.env.RAILWAY_PUBLIC_DOMAIN?.trim(),
              ),
            },
            capabilities: {
              routeFromCurrentLocationToCustomer: true,
              browserGpsWatch: true,
              screenWakeLockWhileRouteIsOpen: true,
              oneActiveDeliveryPerCourier: true,
              liveCustomerMap: true,
              trackingCanBeDisabledPerOrganization: true,
              postgresTenantAware: true,
              postgresRlsPreserved: true,
            },
            privacy: {
              locationOnlyForActiveOrder: true,
              otherDeliveryLocationHidden: true,
              waitingCustomerSeesOtherDeliveryMessageOnly: true,
              locationClearedWhenDeliveryFinishes: true,
              staleLocationNotExposedAfterTwoMinutes: true,
              browserBackgroundTrackingIsBestEffort: true,
            },
          },
          { status: health.schemaReady ? 200 : 503 },
        )
      } catch (error) {
        return NextResponse.json(
          {
            ok: false,
            phase: "25.4-25.5-delivery-routing-live-tracking",
            error:
              error instanceof Error
                ? error.message
                : "Falha ao validar rastreamento de entregas.",
          },
          { status: 500 },
        )
      }
    },
    "tenant-session",
  )
}

==========================================================================================
ARQUIVO: app\api\admin\multiempresa-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getAdminLoginMode,
} from "@/lib/auth"
import {
  getAdminUserCredentialState,
} from "@/lib/admin-user-db"
import {
  getOrganizationOrderingReadiness,
} from "@/lib/organization-onboarding"
import {
  listOrganizationMembershipsForUserId,
} from "@/lib/tenant-context"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        ok: false,
        error:
          "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  try {
    const [
      credential,
      organizations,
      checkout,
    ] = await Promise.all([
      getAdminUserCredentialState(
        session.email,
      ),
      listOrganizationMembershipsForUserId(
        session.userId,
      ),
      getOrganizationOrderingReadiness(
        session.organizationId,
      ),
    ])

    const databasePasswordReady =
      Boolean(
        credential?.passwordReady,
      )

    return NextResponse.json({
      ok:
        databasePasswordReady &&
        organizations.length > 0 &&
        checkout.runtimeReady &&
        checkout.catalogReady &&
        checkout.ordersReady &&
        checkout.operationsReady &&
        checkout.customersReady,
      auth: {
        mode: getAdminLoginMode(),
        databasePasswordReady,
        sessionSecretConfigured:
          Boolean(
            process.env
              .SESSION_SECRET,
          ),
      },
      user: {
        id: session.userId,
        email: session.email,
      },
      activeOrganization: {
        id:
          session.organizationId,
        name:
          session.organizationName,
        slug:
          session.organizationSlug,
        role: session.role,
      },
      organizations: {
        count:
          organizations.length,
        items:
          organizations.map(
            (organization) => ({
              id:
                organization.organizationId,
              name:
                organization.organizationName,
              slug:
                organization.organizationSlug,
              role:
                organization.role,
              publicOrderingEnabled:
                organization.publicOrderingEnabled,
            }),
          ),
      },
      checkout,
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel verificar a Fase 9.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\operations-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getCashSessions,
  getCoupons,
  getCouriers,
  getDeliveryZones,
  getFeedbacks,
  getFinancialEntries,
} from "@/lib/db"
import {
  getTenantOperationsStats,
} from "@/lib/operations-db"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  isCurrentDeploymentOrganization,
} from "@/lib/catalog-db"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

function signedFinancialTotal(
  entries: Awaited<ReturnType<typeof getFinancialEntries>>,
) {
  return Number(
    entries
      .reduce(
        (sum, entry) =>
          sum +
          (entry.type === "income"
            ? entry.amount
            : -entry.amount),
        0,
      )
      .toFixed(2),
  )
}

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        ok: false,
        error: "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  try {
    const stats = await getTenantOperationsStats(
      session.organizationId,
    )

    const mirrorEnabled =
      await isCurrentDeploymentOrganization(
        session.organizationId,
      )

    let legacy:
      | {
          coupons: number
          feedbacks: number
          cashSessions: number
          financialEntries: number
          deliveryZones: number
          couriers: number
          financialSignedTotal: number
        }
      | null = null

    if (mirrorEnabled) {
      const [
        coupons,
        feedbacks,
        cashSessions,
        financialEntries,
        deliveryZones,
        couriers,
      ] = await Promise.all([
        getCoupons({ includeInactive: true }),
        getFeedbacks(),
        getCashSessions(),
        getFinancialEntries(),
        getDeliveryZones({
          includeInactive: true,
        }),
        getCouriers({ includeInactive: true }),
      ])

      legacy = {
        coupons: coupons.length,
        feedbacks: feedbacks.length,
        cashSessions: cashSessions.length,
        financialEntries: financialEntries.length,
        deliveryZones: deliveryZones.length,
        couriers: couriers.length,
        financialSignedTotal:
          signedFinancialTotal(financialEntries),
      }
    }

    const countsMatch =
      !legacy ||
      (legacy.coupons === stats.coupons &&
        legacy.feedbacks === stats.feedbacks &&
        legacy.cashSessions === stats.cashSessions &&
        legacy.financialEntries ===
          stats.financialEntries &&
        legacy.deliveryZones ===
          stats.deliveryZones &&
        legacy.couriers === stats.couriers &&
        Math.abs(
          legacy.financialSignedTotal -
            stats.financialSignedTotal,
        ) < 0.01)

    return NextResponse.json({
      ok: stats.ready && countsMatch,
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      role: session.role,
      operations: stats,
      transition: {
        legacyMirrorEnabled: mirrorEnabled,
        legacy,
        countsMatch,
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel consultar a operaÃ§Ã£o.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\order-flow-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { canUpdateOrderStatus } from "@/lib/admin-access"
import { getDefaultOperationalPath } from "@/lib/operational-home"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { ok: false, error: "SessÃ£o multiempresa invÃ¡lida." },
      { status: 401 },
    )
  }

  const canChangeStatus = canUpdateOrderStatus(
    session.role,
    session.operationalPermissions,
  )
  const canManagePreparation =
    canChangeStatus && session.role !== "courier"
  const canCancel =
    canChangeStatus &&
    session.role !== "courier" &&
    session.role !== "kitchen"

  return NextResponse.json({
    ok: true,
    phase: "25.7.6-scheduled-order-management-session-guard",
    organization: {
      id: session.organizationId,
      name: session.organizationName,
      slug: session.organizationSlug,
    },
    currentSession: {
      email: session.email,
      role: session.role,
      workspace: getDefaultOperationalPath(
        session.role,
        session.operationalPermissions,
      ),
      canManagePreparation,
      canCancelOrders: canCancel,
    },
    scheduledOrders: {
      scheduledFlagBlocksPreparation: false,
      scheduledFlagBlocksCancellation: false,
      managementCanStartPreparationManually: true,
      managementCanCancelBeforeCompletion: true,
    },
    protections: {
      courierCannotAcceptOrPrepareOrders: true,
      courierCannotCancelOrders: true,
      courierOnlyAdvancesReadyToInRouteToCompleted: true,
      staleAdminScreenDetectsSessionRoleChange: true,
      managementAndCourierShouldUseSeparateBrowserProfilesOrDevices: true,
    },
  })
}

==========================================================================================
ARQUIVO: app\api\admin\orders-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getOrders as getLegacyOrders,
} from "@/lib/db"
import {
  getTenantOrdersStats,
  isTenantOrdersReady,
} from "@/lib/order-db"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  isCurrentDeploymentOrganization,
} from "@/lib/catalog-db"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

function moneyTotal(orders: Awaited<ReturnType<typeof getLegacyOrders>>) {
  return Number(
    orders
      .reduce((sum, order) => sum + Number(order.total || 0), 0)
      .toFixed(2),
  )
}

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { ok: false, error: "SessÃ£o multiempresa invÃ¡lida." },
      { status: 401 },
    )
  }

  try {
    const stats = await getTenantOrdersStats(session.organizationId)
    const mirrorEnabled = await isCurrentDeploymentOrganization(
      session.organizationId,
    )

    let legacy:
      | {
          orders: number
          items: number
          totalAmount: number
        }
      | null = null

    if (mirrorEnabled) {
      const orders = await getLegacyOrders()

      legacy = {
        orders: orders.length,
        items: orders.reduce(
          (sum, order) => sum + order.items.length,
          0,
        ),
        totalAmount: moneyTotal(orders),
      }
    }

    const countsMatch =
      !legacy ||
      (legacy.orders === stats.orders &&
        legacy.items === stats.items &&
        Math.abs(legacy.totalAmount - stats.totalAmount) < 0.01)

    return NextResponse.json({
      ok: stats.ready && countsMatch,
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      role: session.role,
      orders: stats,
      transition: {
        legacyMirrorEnabled: mirrorEnabled,
        legacy,
        countsMatch,
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel consultar os pedidos.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\phase25-6-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { legacyStoreRuntimeEnabled } from "@/lib/db"
import { isTenantOperationsReady } from "@/lib/operations-db"
import { getPostgresPool, getRlsRuntimeBridgeStatus } from "@/lib/postgres"
import { runWithTenantRlsScope } from "@/lib/rls-context"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { ok: false, error: "SessÃ£o multiempresa invÃ¡lida." },
      { status: 401 },
    )
  }

  try {
    return await runWithTenantRlsScope(
      [session.organizationId],
      session.userId,
      async () => {
        const [operationsReady, bridge, counts] = await Promise.all([
          isTenantOperationsReady(session.organizationId),
          getRlsRuntimeBridgeStatus(),
          getPostgresPool().query<{
            cash_sessions: number
            financial_entries: number
            pending_invites: number
          }>(
            `
              SELECT
                (
                  SELECT COUNT(*)::int
                  FROM sf_cash_sessions
                  WHERE organization_id = $1
                ) AS cash_sessions,
                (
                  SELECT COUNT(*)::int
                  FROM sf_financial_entries
                  WHERE organization_id = $1
                ) AS financial_entries,
                (
                  SELECT COUNT(*)::int
                  FROM sf_auth_tokens
                  WHERE organization_id = $1
                    AND purpose = 'invite'
                    AND used_at IS NULL
                    AND expires_at > now()
                ) AS pending_invites
            `,
            [session.organizationId],
          ),
        ])

        const row = counts.rows[0] || {
          cash_sessions: 0,
          financial_entries: 0,
          pending_invites: 0,
        }
        const legacyDisabled = !legacyStoreRuntimeEnabled()
        const ok =
          operationsReady &&
          bridge.roleAvailable &&
          legacyDisabled

        return NextResponse.json(
          {
            ok,
            phase: "25.6-finance-cash-invitation-hardening",
            organization: {
              id: session.organizationId,
              name: session.organizationName,
              slug: session.organizationSlug,
            },
            authority: {
              database: "postgresql",
              operationsReady,
              legacyStoreRuntimeEnabled: !legacyDisabled,
              financialLegacyFallback: false,
              cashLegacyFallback: false,
            },
            rls: {
              runtimeRole: bridge.role,
              roleAvailable: bridge.roleAvailable,
              failClosedWhenUnscoped: bridge.failClosedWhenUnscoped,
            },
            counts: {
              cashSessions: Number(row.cash_sessions || 0),
              financialEntries: Number(row.financial_entries || 0),
              pendingInvites: Number(row.pending_invites || 0),
            },
            capabilities: {
              expensePostgresWrite: true,
              otherIncomePostgresWrite: true,
              cashPostgresWrite: true,
              financialValidation: true,
              cashValidation: true,
              invitationTokenBootstrapIndependentFromAdminSession: true,
              invitationAcceptanceAtomic: true,
              financeAndCashFeedbackSeparatedInUi: true,
            },
          },
          { status: ok ? 200 : 503 },
        )
      },
      "tenant-session",
    )
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        phase: "25.6-finance-cash-invitation-hardening",
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel validar a Fase 25.6.",
      },
      { status: 500 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\rbac-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  CUSTOM_PERMISSION_MARKER,
  OPERATIONAL_PERMISSION_CATALOG,
  ROLE_PERMISSION_PRESETS,
} from "@/lib/operational-permissions"
import { getPostgresPool } from "@/lib/postgres"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: 401 },
    )
  }

  const result = await getPostgresPool().query<{
    staff_members: number
    linked_logins: number
    custom_profiles: number
  }>(
    `
      SELECT
        COUNT(*)::int AS staff_members,
        COUNT(*) FILTER (WHERE user_id IS NOT NULL)::int AS linked_logins,
        COUNT(*) FILTER (WHERE permissions ? $2)::int AS custom_profiles
      FROM sf_staff_members
      WHERE organization_id = $1
    `,
    [session.organizationId, CUSTOM_PERMISSION_MARKER],
  )

  const counts = result.rows[0] || {
    staff_members: 0,
    linked_logins: 0,
    custom_profiles: 0,
  }

  return NextResponse.json({
    ok: true,
    phase: "25.1-operational-rbac",
    organization: {
      id: session.organizationId,
      name: session.organizationName,
      slug: session.organizationSlug,
    },
    currentAccess: {
      userId: session.userId,
      role: session.role,
      mode: session.operationalPermissionMode,
      staffMemberId: session.staffMemberId,
      permissionCount: session.operationalPermissions.length,
      permissions: session.operationalPermissions,
    },
    counts: {
      staffMembers: Number(counts.staff_members || 0),
      linkedLogins: Number(counts.linked_logins || 0),
      customProfiles: Number(counts.custom_profiles || 0),
    },
    catalog: {
      permissions: OPERATIONAL_PERMISSION_CATALOG.length,
      rolePresets: Object.fromEntries(
        Object.entries(ROLE_PERMISSION_PRESETS).map(([role, permissions]) => [
          role,
          permissions.length,
        ]),
      ),
    },
    capabilities: {
      rolePresets: true,
      perEmployeeOverrides: true,
      backendAuthorization: true,
      adminNavigationFiltered: true,
      dedicatedOperationalWorkspaces: true,
      deliveryRouteNavigation: true,
      deliveryRealtimeTracking: true,
      postgresTenantIsolationPreserved: true,
    },
    boundaries: {
      ownerUsesFullPreset: true,
      managerHasFullOperationalPreset: true,
      accessGovernanceReservedByDefaultToOwnerAndAdmin: true,
      billingRemainsGovernanceControlled: true,
      customPermissionsNeverBypassRls: true,
      dedicatedRolePagesActive: true,
      deliveryTrackingActiveWithOrderPrivacy: true,
    },
  })
}

==========================================================================================
ARQUIVO: app\api\admin\rls-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { getPostgresPool, getRlsRuntimeBridgeStatus } from "@/lib/postgres"
import { runWithTenantRlsScope } from "@/lib/rls-context"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

type RolloutRow = {
  total: string | number
  enabled: string | number
  forced: string | number
  policies: string | number
}

export async function GET() {
  const session = await getVerifiedTenantSession()
  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  try {
    const pool = getPostgresPool()

    const [bridge, rollout, exemptions, scopedVisibility, unscopedVisibility] =
      await Promise.all([
        getRlsRuntimeBridgeStatus(),
        pool.query<RolloutRow>(`
          SELECT
            COUNT(*)::int AS total,
            COUNT(*) FILTER (
              WHERE r.enforcement = 'enabled'
                AND COALESCE(c.relrowsecurity, false)
            )::int AS enabled,
            COUNT(*) FILTER (
              WHERE r.enforcement = 'enabled'
                AND COALESCE(c.relrowsecurity, false)
                AND COALESCE(c.relforcerowsecurity, false)
            )::int AS forced,
            COUNT(*) FILTER (
              WHERE EXISTS (
                SELECT 1
                FROM pg_policies p
                WHERE p.schemaname = 'public'
                  AND p.tablename = r.table_name
                  AND p.policyname = 'sf_tenant_guard'
              )
            )::int AS policies
          FROM sf_rls_rollout r
          LEFT JOIN pg_class c
            ON c.oid = to_regclass('public.' || r.table_name)
        `),
        pool.query<{ table_name: string; scope: string; reason: string }>(`
          SELECT table_name, scope, reason
          FROM sf_rls_exemptions
          ORDER BY table_name ASC
        `).catch(() => ({ rows: [] })),
        runWithTenantRlsScope(
          [session.organizationId],
          session.userId,
          () => pool.query<{ count: string }>(
            `SELECT COUNT(*)::text AS count FROM sf_organization_settings WHERE organization_id = $1`,
            [session.organizationId],
          ),
          "tenant-session",
        ),
        runWithTenantRlsScope(
          [],
          session.userId,
          () => pool.query<{ count: string }>(
            `SELECT COUNT(*)::text AS count FROM sf_organization_settings WHERE organization_id = $1`,
            [session.organizationId],
          ),
          "tenant-session",
        ),
      ])

    const stats = rollout.rows[0]
    const total = Number(stats?.total || 0)
    const enabled = Number(stats?.enabled || 0)
    const forced = Number(stats?.forced || 0)
    const policies = Number(stats?.policies || 0)
    const currentTenantVisible = Number(scopedVisibility.rows[0]?.count || 0) > 0
    const sameTenantHiddenWithoutScope = Number(unscopedVisibility.rows[0]?.count || 0) === 0

    const ready =
      bridge.roleAvailable &&
      total > 0 &&
      total === enabled &&
      total === forced &&
      total === policies &&
      currentTenantVisible &&
      sameTenantHiddenWithoutScope

    return NextResponse.json({
      ok: ready,
      phase: "24-definitive-postgres-rls",
      schemaReady: total > 0,
      runtimeRole: bridge,
      rollout: {
        total,
        enabled,
        forced,
        policies,
        enforcement: ready ? "enabled-and-forced" : "incomplete",
      },
      isolationProbe: {
        currentTenantVisible,
        sameTenantHiddenWithoutScope,
        failClosedWhenUnscoped: bridge.failClosedWhenUnscoped,
      },
      exemptions: exemptions.rows,
      boundaries: {
        postgresRlsIsEnforced: ready,
        tableOwnerBypassNeutralizedByRuntimeRole: bridge.roleAvailable,
        requestTenantContextRequired: true,
        corporateReportsUseExplicitMultiTenantScope: true,
        privilegedBackendBypassIsExplicit: true,
        browserCannotSetRlsBypass: true,
        controlPlaneTablesRemainBackendAuthorized: true,
      },
    }, { status: ready ? 200 : 503 })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        phase: "24-definitive-postgres-rls",
        error: error instanceof Error ? error.message : "Falha ao validar RLS.",
      },
      { status: 500 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\security-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getAdminLoginMode,
  isSessionSecretConfigured,
} from "@/lib/auth"
import {
  getAdminUserCredentialState,
} from "@/lib/admin-user-db"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  getOrganizationTimeZone,
  getRlsRolloutStats,
  isValidTimeZone,
  listOrganizationDomains,
  listPrintAgents,
} from "@/lib/organization-security-db"
import {
  listTeamAccess,
} from "@/lib/team-access-db"
import {
  canViewSecurity,
  canViewTeam,
} from "@/lib/tenant-permissions"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        ok: false,
        error:
          "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  if (!canViewSecurity(session.role)) {
    return NextResponse.json(
      { ok: false, error: "Seu perfil nÃ£o pode acessar a Ã¡rea de seguranÃ§a." },
      { status: 403 },
    )
  }

  try {
    const [
      credential,
      timeZone,
      domains,
      printAgents,
      rls,
      teamAccess,
    ] = await Promise.all([
      getAdminUserCredentialState(
        session.email,
      ),
      getOrganizationTimeZone(
        session.organizationId,
      ),
      listOrganizationDomains(
        session.organizationId,
      ),
      listPrintAgents(
        session.organizationId,
      ),
      getRlsRolloutStats(),
      canViewTeam(session.role)
        ? listTeamAccess(
            session.organizationId,
          )
        : Promise.resolve([]),
    ])

    const databasePasswordReady =
      Boolean(
        credential?.passwordReady,
      )

    const sessionSecretConfigured =
      isSessionSecretConfigured()

    const timeZoneValid =
      isValidTimeZone(
        timeZone,
      )

    const team = {
      profiles:
        teamAccess.length,
      activeLogins:
        teamAccess.filter(
          (item) =>
            item.membershipStatus ===
            "active",
        ).length,
      pendingInvites:
        teamAccess.filter(
          (item) =>
            item.membershipStatus ===
            "invited",
        ).length,
      disabledLogins:
        teamAccess.filter(
          (item) =>
            item.membershipStatus ===
            "disabled",
        ).length,
    }

    const domainStats = {
      total:
        domains.length,
      verified:
        domains.filter(
          (item) =>
            item.verified,
        ).length,
      pending:
        domains.filter(
          (item) =>
            !item.verified,
        ).length,
    }

    const printStats = {
      total:
        printAgents.length,
      active:
        printAgents.filter(
          (item) =>
            item.active,
        ).length,
    }

    const rlsPreparedSafely =
      rls.prepared &&
      rls.enabledCount === 0

    const rlsDefinitive =
      rls.prepared &&
      rls.preparedCount > 0 &&
      rls.enabledCount === rls.preparedCount

    const rlsHealthy =
      rlsPreparedSafely || rlsDefinitive

    return NextResponse.json({
      ok:
        databasePasswordReady &&
        sessionSecretConfigured &&
        timeZoneValid &&
        rlsHealthy,
      phase: 10,
      auth: {
        mode:
          getAdminLoginMode(),
        databasePasswordReady,
        sessionSecretConfigured,
        sessionVersion:
          session.sessionVersion,
      },
      organization: {
        id:
          session.organizationId,
        name:
          session.organizationName,
        slug:
          session.organizationSlug,
        role:
          session.role,
        timeZone,
        timeZoneValid,
      },
      team,
      domains: domainStats,
      printAgents:
        printStats,
      rls: {
        prepared:
          rls.prepared,
        safePrepared:
          rlsPreparedSafely,
        definitive:
          rlsDefinitive,
        preparedCount:
          rls.preparedCount,
        enabledCount:
          rls.enabledCount,
        enforcement:
          rlsDefinitive
            ? "enabled"
            : rls.enabledCount > 0
              ? "partial"
              : "prepared-only",
        note:
          rlsDefinitive
            ? "O enforcement tenant RLS estÃ¡ ativo. A validaÃ§Ã£o de FORCE RLS e do papel restrito do runtime Ã© feita em /api/admin/rls-health."
            : "As polÃ­ticas RLS estÃ£o preparadas e o enforcement ainda nÃ£o foi concluÃ­do.",
      },
      legacy: {
        dataFile: null,
        storeMirror: "disabled",
        adminLoginFallback: false,
        note:
          "Fase 25: store.json e login administrativo legado nÃ£o participam mais do runtime.",
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        phase: 10,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel verificar a seguranÃ§a da Fase 10.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\staff-profile-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getTenantStaffMembers,
  isTenantRuntimeReady,
} from "@/lib/organization-db"
import { runWithTenantRlsScope } from "@/lib/rls-context"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { canViewTeam } from "@/lib/tenant-permissions"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  if (!canViewTeam(session.role)) {
    return NextResponse.json(
      { error: "Seu perfil nÃ£o pode visualizar a equipe." },
      { status: 403 },
    )
  }

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () => {
      const runtimeReady = await isTenantRuntimeReady(session.organizationId)

      try {
        const staffMembers = runtimeReady
          ? await getTenantStaffMembers(session.organizationId, {
              includeInactive: true,
            })
          : []

        return NextResponse.json({
          ok: runtimeReady,
          phase: "25.3.2-staff-profile-management",
          schemaReady: runtimeReady,
          organization: {
            id: session.organizationId,
            role: session.role,
          },
          counts: {
            staffMembers: staffMembers.length,
            active: staffMembers.filter((item) => item.active).length,
            withEmail: staffMembers.filter((item) => Boolean(item.email)).length,
            withPhone: staffMembers.filter((item) => Boolean(item.phone)).length,
            withHireDate: staffMembers.filter((item) => Boolean(item.hireDate)).length,
            withEmploymentType: staffMembers.filter((item) => Boolean(item.employmentType)).length,
            withNotes: staffMembers.filter((item) => Boolean(item.notes)).length,
          },
          capabilities: {
            editOperationalProfile: true,
            editContactData: true,
            editEmploymentDetails: true,
            changeRoleRequiresAccessManage: true,
            perEmployeePermissionsPreserved: true,
            invitationFlowPreserved: true,
            passwordRecoveryPreserved: true,
            accessRevocationPreserved: true,
            postgresTenantAware: true,
          },
          boundaries: {
            storeJsonFallbackDisabled: true,
            employeeProfileDoesNotBypassRls: true,
            activeLoginEmailIsNotSilentlyRewritten: true,
            ownerCannotBeCreatedAsStaffRole: true,
          },
        })
      } catch (error) {
        const pgError = error as { code?: string }
        const schemaReady = pgError?.code !== "42703"

        return NextResponse.json(
          {
            ok: false,
            phase: "25.3.2-staff-profile-management",
            schemaReady,
            runtimeReady,
            error:
              pgError?.code === "42703"
                ? "Migration 026_staff_profile_details ainda nÃ£o foi aplicada."
                : error instanceof Error
                  ? error.message
                  : "NÃ£o foi possÃ­vel validar os perfis da equipe.",
          },
          { status: 503 },
        )
      }
    },
    "tenant-session",
  )
}

==========================================================================================
ARQUIVO: app\api\admin\team\access\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { resolvePublicAppOrigin } from "@/lib/public-origin"
import { runWithTenantRlsScope } from "@/lib/rls-context"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  canManageAccess,
  canManageTeam,
  canViewTeam,
} from "@/lib/tenant-permissions"
import {
  createTeamInvitation,
  createTeamPasswordReset,
  disableTeamAccess,
  listTeamAccess,
} from "@/lib/team-access-db"
import { assertCanAddUser, billingErrorStatus } from "@/lib/billing-db"

export const dynamic = "force-dynamic"

export async function GET() {
  const session =
    await getVerifiedTenantSession()

  if (
    !session ||
    !canViewTeam(session.role)
  ) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: session ? 403 : 401 },
    )
  }

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () =>
      NextResponse.json({
        access:
          await listTeamAccess(
            session.organizationId,
          ),
      }),
    "tenant-session",
  )
}

export async function POST(
  request: Request,
) {
  const session =
    await getVerifiedTenantSession()

  if (
    !session ||
    !canManageAccess(session.role)
  ) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: session ? 403 : 401 },
    )
  }

  const body = (await request
    .json()
    .catch(() => null)) as
    | {
        action?:
          | "invite"
          | "password-reset"
        staffMemberId?: number
        userId?: string
      }
    | null

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () => {
      try {
        const origin = resolvePublicAppOrigin(request)

        if (
          body?.action ===
          "password-reset"
        ) {
          if (!body.userId) {
            throw new Error(
              "UsuÃ¡rio invÃ¡lido.",
            )
          }

          const reset =
            await createTeamPasswordReset({
              organizationId:
                session.organizationId,
              targetUserId:
                body.userId,
              createdByUserId:
                session.userId,
              actorRole:
                session.role,
            })

          return NextResponse.json({
            reset: {
              email:
                reset.email,
              expiresAt:
                reset.expiresAt,
              url:
                `${origin}/recuperar-senha/${encodeURIComponent(
                  reset.token,
                )}`,
            },
          })
        }

        const staffMemberId =
          Number(
            body?.staffMemberId,
          )

        if (
          !Number.isInteger(
            staffMemberId,
          ) ||
          staffMemberId <= 0
        ) {
          throw new Error(
            "Colaborador invÃ¡lido.",
          )
        }

        const currentAccess = await listTeamAccess(session.organizationId)
        const targetAccess = currentAccess.find((item) => item.staffMemberId === staffMemberId)
        if (!targetAccess || !["active", "invited"].includes(String(targetAccess.membershipStatus))) {
          await assertCanAddUser(session.organizationId)
        }

        const invitation =
          await createTeamInvitation({
            organizationId:
              session.organizationId,
            staffMemberId,
            invitedByUserId:
              session.userId,
          })

        return NextResponse.json({
          invitation: {
            ...invitation,
            url:
              invitation.token
                ? `${origin}/convite/${encodeURIComponent(
                    invitation.token,
                  )}`
                : null,
            token: undefined,
          },
        })
      } catch (error) {
        return NextResponse.json(
          {
            error:
              error instanceof Error
                ? error.message
                : "NÃ£o foi possÃ­vel criar o acesso.",
          },
          { status: billingErrorStatus(error) },
        )
      }
    },
    "tenant-session",
  )
}

export async function DELETE(
  request: Request,
) {
  const session =
    await getVerifiedTenantSession()

  if (
    !session ||
    !canManageAccess(session.role)
  ) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: session ? 403 : 401 },
    )
  }

  const body = (await request
    .json()
    .catch(() => null)) as
    | { userId?: string }
    | null

  if (!body?.userId) {
    return NextResponse.json(
      { error: "UsuÃ¡rio invÃ¡lido." },
      { status: 400 },
    )
  }

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () => {
      try {
        await disableTeamAccess(
          session.organizationId,
          body.userId!,
        )

        return NextResponse.json({
          ok: true,
        })
      } catch (error) {
        return NextResponse.json(
          {
            error:
              error instanceof Error
                ? error.message
                : "NÃ£o foi possÃ­vel desativar o acesso.",
          },
          { status: 400 },
        )
      }
    },
    "tenant-session",
  )
}

==========================================================================================
ARQUIVO: app\api\admin\tenant-context\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getAdminSession,
} from "@/lib/auth"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  listOrganizationMembershipsForUserId,
} from "@/lib/tenant-context"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const rawSession =
    await getAdminSession()

  if (!rawSession) {
    return NextResponse.json(
      {
        ok: false,
        error:
          "NÃ£o autorizado.",
      },
      { status: 401 },
    )
  }

  if (
    rawSession.mode === "legacy"
  ) {
    return NextResponse.json({
      ok: true,
      sessionMode: "legacy",
      email: rawSession.email,
      organization: null,
      organizations: [],
      message:
        "Login ainda estÃ¡ em modo legado.",
    })
  }

  const session =
    await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        ok: false,
        error:
          "A membership ativa nÃ£o existe mais.",
      },
      { status: 403 },
    )
  }

  const organizations =
    await listOrganizationMembershipsForUserId(
      session.userId,
    )

  return NextResponse.json({
    ok: true,
    sessionMode: "tenant",
    user: {
      id: session.userId,
      email: session.email,
      role: session.role,
    },
    organization: {
      id:
        session.organizationId,
      name:
        session.organizationName,
      slug:
        session.organizationSlug,
    },
    activeMembership: true,
    organizations:
      organizations.map(
        (organization) => ({
          id:
            organization.organizationId,
          name:
            organization.organizationName,
          slug:
            organization.organizationSlug,
          role:
            organization.role,
          publicOrderingEnabled:
            organization.publicOrderingEnabled,
        }),
      ),
  })
}

==========================================================================================
ARQUIVO: app\api\admin\tenant-runtime-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  getStaffMembers as getLegacyStaffMembers,
  getSettings as getLegacySettings,
} from "@/lib/db"
import {
  getTenantDomains,
  getTenantRuntimeStats,
  getTenantSettings,
  getTenantStaffMembers,
} from "@/lib/organization-db"
import {
  getVerifiedTenantSession,
} from "@/lib/tenant-access"
import {
  isCurrentDeploymentOrganization,
} from "@/lib/catalog-db"

export const runtime = "nodejs"
export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      {
        ok: false,
        error: "SessÃ£o multiempresa invÃ¡lida.",
      },
      { status: 401 },
    )
  }

  try {
    const [
      stats,
      settings,
      staffMembers,
      domains,
    ] = await Promise.all([
      getTenantRuntimeStats(
        session.organizationId,
      ),
      getTenantSettings(
        session.organizationId,
      ),
      getTenantStaffMembers(
        session.organizationId,
        { includeInactive: true },
      ),
      getTenantDomains(
        session.organizationId,
      ),
    ])

    const isCurrent =
      await isCurrentDeploymentOrganization(
        session.organizationId,
      )

    let legacy:
      | {
          storeName: string
          staff: number
        }
      | null = null

    if (isCurrent) {
      const [
        legacySettings,
        legacyStaff,
      ] = await Promise.all([
        getLegacySettings(),
        getLegacyStaffMembers(),
      ])

      legacy = {
        storeName: legacySettings.storeName,
        staff: legacyStaff.length,
      }
    }

    const countsMatch =
      !legacy ||
      (legacy.storeName ===
        settings?.storeName &&
        legacy.staff ===
          staffMembers.length)

    return NextResponse.json({
      ok:
        stats.ready &&
        Boolean(settings) &&
        countsMatch,
      organization: {
        id: session.organizationId,
        name: session.organizationName,
        slug: session.organizationSlug,
      },
      role: session.role,
      runtime: stats,
      settings: settings
        ? {
            storeName: settings.storeName,
            city: settings.city,
            state: settings.state,
          }
        : null,
      staff: {
        count: staffMembers.length,
      },
      domains,
      publicPaths: {
        slug: `/loja/${session.organizationSlug}`,
        railwayDomain:
          process.env.RAILWAY_PUBLIC_DOMAIN ||
          null,
      },
      transition: {
        legacyMirrorEnabled: isCurrent,
        legacy,
        countsMatch,
      },
    })
  } catch (error) {
    return NextResponse.json(
      {
        ok: false,
        error:
          error instanceof Error
            ? error.message
            : "NÃ£o foi possÃ­vel consultar o runtime da empresa.",
      },
      { status: 503 },
    )
  }
}

==========================================================================================
ARQUIVO: app\api\admin\workspaces-health\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import {
  canAccessOperationalWorkspace,
  getDefaultOperationalPath,
} from "@/lib/operational-home"
import { getVerifiedTenantSession } from "@/lib/tenant-access"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: 401 },
    )
  }

  return NextResponse.json({
    ok: true,
    phase: "25.2-dedicated-operational-workspaces",
    organization: {
      id: session.organizationId,
      name: session.organizationName,
      slug: session.organizationSlug,
    },
    currentAccess: {
      role: session.role,
      permissionMode: session.operationalPermissionMode,
      defaultPath: getDefaultOperationalPath(
        session.role,
        session.operationalPermissions,
      ),
    },
    workspaces: {
      manager: {
        path: "/gerente",
        allowed: canAccessOperationalWorkspace("manager", session.role, session.operationalPermissions),
      },
      pdv: {
        path: "/pdv",
        allowed: canAccessOperationalWorkspace("pdv", session.role, session.operationalPermissions),
      },
      kitchen: {
        path: "/cozinha",
        allowed: canAccessOperationalWorkspace("kitchen", session.role, session.operationalPermissions),
      },
      courier: {
        path: "/entregador",
        allowed: canAccessOperationalWorkspace("courier", session.role, session.operationalPermissions),
      },
    },
    capabilities: {
      roleAwareAdminRedirect: true,
      dedicatedManagerWorkspace: true,
      dedicatedPdvWorkspace: true,
      dedicatedKitchenWorkspace: true,
      dedicatedCourierWorkspace: true,
      courierAssignmentIdentity: true,
      courierRouteNavigation: true,
      realtimeCustomerTracking: true,
      backendRbacPreserved: true,
      postgresRlsPreserved: true,
    },
    boundaries: {
      pdvReceivesOrdersWithoutKitchenControls: true,
      kitchenControlsPreparationOnly: true,
      courierControlsDeliveryStatusOnly: true,
      courierAssignmentIdentityActive: true,
      courierRouteNavigationActive: true,
      realtimeCustomerTrackingActive: true,
    },
  })
}

==========================================================================================
ARQUIVO: app\api\dashboard\live\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { getCourierWorkspaceSnapshot } from "@/lib/delivery-dispatch-db"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { getDefaultOperationalPath } from "@/lib/operational-home"
import { canReadOrders } from "@/lib/admin-access"
import { getTenantRecentOrders } from "@/lib/order-db"
import { runWithTenantRlsScope } from "@/lib/rls-context"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()
  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  const sessionContext = {
    email: session.email,
    role: session.role,
    workspace: getDefaultOperationalPath(
      session.role,
      session.operationalPermissions,
    ),
  }

  if (!canReadOrders(session.role, session.operationalPermissions)) {
    return NextResponse.json({ orders: [], sessionContext })
  }

  const orders = await runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    () => getTenantRecentOrders(session.organizationId, 120),
    "tenant-session",
  )

  if (session.role !== "courier") {
    return NextResponse.json({ orders, sessionContext })
  }

  const dispatch = await getCourierWorkspaceSnapshot({
    organizationId: session.organizationId,
    userId: session.userId,
    role: session.role,
    permissions: session.operationalPermissions,
  })
  const visibleOrderIds = new Set(dispatch.orders.map((order) => order.id))

  return NextResponse.json({
    orders: orders.filter((order) => visibleOrderIds.has(order.id)),
    sessionContext,
  })
}

==========================================================================================
ARQUIVO: app\api\dashboard\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { getCourierWorkspaceSnapshot } from "@/lib/delivery-dispatch-db"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { getTenantAwareAdminData } from "@/lib/tenant-admin-data"
import { getDefaultOperationalPath } from "@/lib/operational-home"

export const dynamic = "force-dynamic"

export async function GET() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json(
      { error: "NÃ£o autorizado." },
      { status: 401 },
    )
  }

  const data = await getTenantAwareAdminData(
    session,
    session.operationalPermissions,
  )
  const sessionContext = {
    email: session.email,
    role: session.role,
    workspace: getDefaultOperationalPath(
      session.role,
      session.operationalPermissions,
    ),
  }

  if (session.role !== "courier") {
    return NextResponse.json({
      ...data,
      sessionContext,
    })
  }

  const dispatch = await getCourierWorkspaceSnapshot({
    organizationId: session.organizationId,
    userId: session.userId,
    role: session.role,
    permissions: session.operationalPermissions,
  })

  const visibleOrderIds = new Set(dispatch.orders.map((order) => order.id))

  return NextResponse.json({
    ...data,
    sessionContext,
    orders: data.orders.filter((order) => visibleOrderIds.has(order.id)),
    couriers: dispatch.courier ? [dispatch.courier] : [],
    courierIdentity: dispatch.courier
      ? {
          id: dispatch.courier.id,
          name: dispatch.courier.name,
          staffMemberId: dispatch.courier.staffMemberId ?? null,
        }
      : null,
  })
}

==========================================================================================
ARQUIVO: app\api\orders\accept-pending\route.ts
==========================================================================================
import { NextResponse } from "next/server"
import { canUpdateOrderStatus } from "@/lib/admin-access"
import { getTenantOrders, isTenantOrdersReady, updateTenantOrder } from "@/lib/order-db"
import { getVerifiedTenantSession } from "@/lib/tenant-access"
import { runWithTenantRlsScope } from "@/lib/rls-context"

export async function POST() {
  const session = await getVerifiedTenantSession()

  if (!session) {
    return NextResponse.json({ error: "NÃ£o autorizado." }, { status: 401 })
  }

  if (
    session.role === "courier" ||
    !canUpdateOrderStatus(session.role, session.operationalPermissions)
  ) {
    return NextResponse.json(
      { error: "Seu perfil nÃ£o pode aceitar pedidos." },
      { status: 403 },
    )
  }

  return runWithTenantRlsScope(
    [session.organizationId],
    session.userId,
    async () => {
      const ready = await isTenantOrdersReady(session.organizationId).catch(() => false)
      if (!ready) {
        return NextResponse.json(
          { error: "Pedidos PostgreSQL desta empresa nÃ£o estÃ£o disponÃ­veis." },
          { status: 503 },
        )
      }

      const pending = (await getTenantOrders(session.organizationId)).filter(
        (order) => order.status === "pending",
      )

      const updated = []
      for (const order of pending) {
        const next = await updateTenantOrder(session.organizationId, order.id, {
          status: "accepted",
        })
        if (next) updated.push(next)
      }

      return NextResponse.json({
        acceptedCount: updated.length,
        orders: updated,
      })
    },
    "tenant-session",
  )
}
