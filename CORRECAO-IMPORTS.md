# Correção de import ausente

Este pacote é a atualização de cidade/bairros dinâmicos com as dependências de localização incluídas.

Antes de copiar:
1. Pare `npm run dev` com Ctrl+C.
2. Copie todo o conteúdo para a raiz do projeto, aceitando substituir.
3. Apague a pasta `.next`.
4. Rode `npm run dev` novamente.

Arquivos adicionados para corrigir o erro:
- `lib/address-location-client.ts`
- `lib/google-maps-client.ts`
